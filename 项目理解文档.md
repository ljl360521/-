# 项目理解文档：Android IL2CPP 游戏辅助菜单（"句号的辅助"）

> 本文档由 11 个模块通读小组的笔记综合而成，供团队长期参考。文中所有结论均可追溯到具体源码文件与行号（详见各节及文末速查表）。

---

## 一、项目定位与整体架构

### 一句话定位

这是一个**通过 SO 注入到特定 Unity/IL2CPP 手游（`com.ztgame.bob`，球球大作战 BOB / Agar.io 类吞噬球游戏）进程内、以透明 ImGui 悬浮层呈现的作弊菜单**，核心手法是 **IL2CPP 反射调用 + 托管对象内存直写**（并非 inline hook，也非 FPS 外挂），并配套一整套 Android 触摸注入、软键盘桥接、卡密授权、防录屏与内嵌音乐播放器。

### 重要前提澄清（纠正若干常见误判）

- **不做 inline hook**：项目带有 `dobby.h`（Dobby hook 框架声明），但**全项目零调用、零 include**，是"挂而未用"的死头文件。真正的底层能力靠 **xdl**（增强版 dlsym，突破 Android linker namespace）+ **IL2CPP 官方 C API**。
- **不是 FPS 外挂**：没有 ESP 透视 / 自瞄 / 无后座 / W2S 坐标投影。实际功能是分身、吐球、合球、视野、改名、名字缩放等 `.io` 吞噬球玩法。
- **t3sdk 不是引擎访问 SDK**：它是第三方"T3验证"发卡平台的**卡密授权客户端**，与 il2cpp/内存读写无关。
- **ImGenie 不是控件库**：它是第三方 MIT 库，做窗口的 macOS 神灯（Genie）开合动画。
- **ImCoolBar 是装饰性 Dock 条**，`imguiSimpleToast.hpp` 是**未接线的死代码**。

### 分层架构

```
┌─────────────────────────────────────────────────────────────┐
│ 注入 / 入口层   Main.cpp（constructor→pthread→反射拿JVM/Activity  │
│                →DexClassLoader加载Java侧ImGui→回调建视图）        │
├─────────────────────────────────────────────────────────────┤
│ 渲染桥接层     imguijni.cpp（GLES3 JNI后端：init/step/resize，   │
│                每帧驱动 触摸→NewFrame→绘制→Render→Capture）       │
├─────────────────────────────────────────────────────────────┤
│ UI / 菜单层    主界面绘制 · 主题样式 · ImGenie(窗口动画) ·         │
│                ImCoolBar(Dock彩蛋) · 音频标签页                  │
├─────────────────────────────────────────────────────────────┤
│ 逆向功能层     游戏函数调用.cpp（IL2CPP反射+内存直写：分身/吐球/    │
│                视野/粘合/改名/名字缩放/三角合球/摇杆）             │
├─────────────────────────────────────────────────────────────┤
│ 授权 / 防护层  卡密验证(T3门禁) + t3sdk(RSA/MD5/curl) + 防录屏     │
│                (SECURE EGL Surface + 内存dex)                   │
├─────────────────────────────────────────────────────────────┤
│ 输入体系       触摸输入 · 触摸窗口(穿透) · 输入法桥接 · 音量键缩放  │
├─────────────────────────────────────────────────────────────┤
│ 音乐子系统     MusicPlayer（miniaudio+curl+FFT，与游戏完全无关）   │
├─────────────────────────────────────────────────────────────┤
│ 中枢 / 基础    全局状态 · Config(pugixml) · 配置初始化 · 日志      │
└─────────────────────────────────────────────────────────────┘
```

产物为共享库 **`libhook.so`**（arm64-v8a），被外部注入器 dlopen 后自启动。

---

## 二、目录 / 模块地图

### `jni/App/` —— 入口与渲染桥接

| 文件 | 职责 |
|---|---|
| `Main.cpp` | 注入引导：SO 构造函数起线程 → xdl 拿 JavaVM → 反射爬 Activity → 释放内嵌 dex 并 DexClassLoader 加载 Java 侧 `ImGui` 类 → 提取自身 so → 回调 Java 建立 ImGui 视图 |
| `imguijni.cpp` | ImGui 的 Android/OpenGL ES3 渲染后端 JNI 层：`GLES3JNIView.init/resize/step/shutdown`、触摸/音量键回调、窗口矩形查询；每帧渲染循环核心 |
| `log.h` | 日志宏 `LOGD/I/W/E/F`，双写 logcat + 落盘（10MB 轮转） |
| `dobby.h` | Dobby hook 框架 API 声明（**未启用，死头文件**） |
| `classes_dex.h` / `ime_dex_data.h` / `uploaded_font.h` / `zt_ttf.h` | 内嵌数据：注入用 dex（10644B）、输入法辅助 dex（12948B）、中文字体（fz.ttf ~6.5MB / zt.ttf 13MB） |

### `jni/res/` —— 业务主体（中文命名模块）

| 文件 | 职责 |
|---|---|
| `全局状态.{h,cpp}` | **全项目中枢**：所有功能开关、运行时变量、T3 硬编码常量、日志系统的集中定义（被 13 个文件 include） |
| `Config.{h,cpp}` | 基于 pugixml 的持久化引擎（`ConfigManager` 静态类，XML 读写 + 键注册表 + 原子 tmp+rename 写入） |
| `配置初始化.{h,cpp}` | 启动编排：JNI 反射发现外部目录 → 递归建目录 → 注册 8 个持久化字段 → Load/Save |
| `游戏函数调用.{h,cpp}` | **逆向核心**：封装 IL2CPP C API，实现分身/吐球(3路径)/视野/粘合/全员改名/名字缩放/三角合球/摇杆移动 |
| `主界面绘制.{h,cpp}` | 菜单主 UI：标签页布局、功能开关控件；每帧驱动内存修改功能的 tick 逻辑 |
| `主题样式.{h,cpp}` | 5 套主题（经典/纯净白/芭比粉/液态玻璃/音乐UI）的配色与样式 |
| `触摸输入.{h,cpp}` | 触摸槽位管理、多指状态机（主指=控件、副指=拖窗）、注入 ImGuiIO、点按/拖动判定、`BeginMainTabItem` |
| `触摸窗口.{h,cpp}` | 每帧登记可交互窗口矩形、带 padding 命中检测、合并 bounds，供 Java 触摸穿透判断 |
| `输入法桥接.{h,cpp}` | JNI 互操作中枢：ActivityThread 反射拿 Activity、内存 dex 加载 `com.mxp.Helper` 调起软键盘、字符/按键回传 ImGui、打开 URL |
| `音量键缩放.{h,cpp}` | 内存 dex 加载 `com.mxp.VolumeKeyHelper`，把音量键变成调 UI 缩放的快捷键 |
| `卡密验证.{h,cpp}` | T3 授权 App 层封装：初始化、异步登录、心跳、自动登录、ImGui 登录界面 `DrawT3AuthUI` |
| `防录屏.{h,cpp}` | 反截屏：内存 dex 加载 `com.mxp.SecureSurfaceHelper` → 建 SECURE EGL Surface → 菜单单独渲染到安全层 |
| `ImGenie.{h,cpp}` + `ImGenie捕获.{h,cpp}` | 第三方窗口神灯动画库 + 本项目自写的 GLES3 FBO 离屏捕获胶水 |
| `ImCoolBar.{h,cpp}` | 第三方 macOS Dock 风格悬浮条（本项目仅作"另一个窗口"里的彩蛋） |
| `imguiSimpleToast.hpp` | Toast 通知库（**完整但全项目未 include，死代码**） |
| `音频标签页.{h,cpp}` | 音乐标签页外壳：节奏联动雪花粒子背景 + 音乐/设置二级导航 |
| `music/MusicPlayer.{h,cpp}` | 联网点播音乐播放器 + FFT 频谱 + 鼓点检测（miniaudio+curl+json，独立子系统） |

### 其它目录

| 目录/文件 | 职责 |
|---|---|
| `jni/t3sdk/t3sdk.{h,cpp}` | T3 卡密验证 SDK：自带 MD5/大数RSA/自定义Base64/极简JSON/libcurl HTTPS 客户端 |
| `jni/xdl/` | 增强版 dlopen/dlsym（`xdl_open/xdl_sym/xdl_dsym`），突破 linker namespace |
| `jni/ImGui/` | Dear ImGui 核心 + 预编译 `libimgui.a` |
| `jni/thirdparty/pugixml/` | XML 解析库（配置持久化用） |
| `res/{volume,secure}/*_dex_data.h` | 音量键(5160B)、防录屏(4584B)内嵌 dex 字节数组 |
| `res/music/curl/` | 预编译 libcurl/libssl/libcrypto 静态库 + 内嵌 CA 证书 `证书.h` |
| `Android.mk` / `Application.mk` | ndk-build 构建脚本 |

---

## 三、核心数据流（完整链路）

### 3.1 注入启动链（一次性）

```
SO 被 dlopen/System.load 加载
  → __attribute__((constructor)) _init  [Main.cpp:879]
  → pthread_create(native_thread_func) + detach   （构造函数会被调两次，靠 env var 去重）
  → native_thread_func [Main.cpp:786]：
      1. check_and_create_init_flag()  进程内 IMGUI_INITIALIZED 去重
      2. 循环50×200ms getJavaVMViaReflection()
           → xdl_open("libandroid_runtime.so") → xdl_sym/xdl_dsym 找
             JNI_GetCreatedJavaVMs → 取 g_JavaVM
      3. 循环50×200ms getCurrentActivityInstance()
           → 反射 ActivityThread.currentActivityThread().mActivities
             → 遍历 ActivityClientRecord.activity，优先 UnityPlayerActivity
      4. initMainThreadHandler()  建主线程 Handler
      5. loadImGuiClassFromDex()  释放 injected.dex → DexClassLoader
             加载 com.example.imgui.ImGui
      6. extract_current_so()  读 /proc/self/maps 自定位，拷 so 到 cache
      7. callImGuiSetupView()  → Java setupImGuiViewOnMainThread(Activity)
  → Java 主线程创建 GLES3JNIView(GLSurfaceView) 挂到 Activity
  → 回调 native GLES3JNIView_init / step / ...
```

### 3.2 渲染与触摸建立

```
GLES3JNIView_init [imguijni.cpp:78]：
  首次 → XmlConfigCompileProbe → InitAppConfigOnce → CreateContext
       → ImGenie::CreateContext + 注入捕获回调(捕获_创建/销毁纹理) + SetCaptureFlipV(true)
       → io.IniFilename=NULL → ApplyOldProjectTheme(默认 液态玻璃)
       → ImGui_ImplAndroid_Init + ImGui_ImplOpenGL3_Init("#version 300 es")
       → LoadVolumeDex + SetVolumeScaleEnabled
       → 加载中文字体(45px, GetGlyphRangesChineseFull) → g_Initialized=true

触摸建立：Java MotionEvent 拆单指
  → GLES3JNIView_MotionEventClick [imguijni.cpp:245]
  → TouchHandlePointer(pointerId, down, pos)  维护 5 槽位状态机
```

### 3.3 每帧渲染循环（`GLES3JNIView_step`）

```
env->GetJavaVM(&g_ime_jvm) + CaptureCurrentClassLoader + InitAppConfigOnce(懒)
ImGui_ImplOpenGL3_NewFrame + ImGui_ImplAndroid_NewFrame
触摸_更新ImGuiIO(io)      // 主指坐标→MousePos，有指按住→MouseDown[0]=true
ImGui::NewFrame
触摸_帧后处理(io)         // 副指多指拖窗
DrawFloatingWindow()     // ← 主界面绘制：门禁→登录窗 或 主菜单，并每帧 tick 内存功能
ImeUpdateByImGui()       // 输入法：WantTextInput 边沿→显/隐软键盘
触摸_清除点击事件()
ImGui::Render() → ImGenie::Capture()  // 离屏捕获做动画的窗口，从主DrawData移除真窗
处理 g_pending_secure_switch（防录屏切换）
if 防录屏开启：普通层只清透明，UI 画到 SECURE EGL surface
else：           glClearColor(0,0,0,0) 透明清屏 + RenderDrawData（叠加式覆盖层）
```

### 3.4 用户改开关 → 游戏函数生效

菜单统一采用 **"UI 只置 flag，`DrawFloatingWindow` 每帧 apply，关闭沿用 `*_was_enabled` 只 reset 一次"** 模式：

```
用户勾选 g_view_enabled / 拖 g_view_scale 滑块（主界面绘制.cpp:内存tab）
  → DrawFloatingWindow 每帧检测：开启且 scale>1.0
  → GameCall_SetViewScale(scale)
  → 游戏函数调用.cpp：
       EnsureIl2CppApi（dlopen/xdl 解析 22 个 il2cpp 符号）
       → AttachCurrentThreadIfNeeded（渲染线程非脚本线程，必须 attach）
       → 解析 GameCoreCenter.get_instance() 拿单例
       → il2cpp_field_get_offset("ViewScaleFactor") 拿偏移
       → *(float*)((char*)gccInstance+offset) = 0.5f*scale   // 内存直写
  → 每帧重写以对抗游戏 Update2D() 的 Lerp 拉回
```

按钮型功能（分身/吐球/三角合球）走一次性 `runtime_invoke` 调用游戏 C# 方法。

---

## 四、逆向能力细节

### 4.1 "hook 框架"实情

- **无 inline hook**：`游戏函数调用.cpp:2037` 明确注释"当前内容是 IL2CPP/runtime_invoke 调试，不再是 Dobby hook"，函数名保留 `Hook` 字样仅为兼容 UI。
- **Dobby 完全未启用**：`dobby.h` 声明齐全（各架构 RegisterContext、`DobbyHook/Instrument/SymbolResolver/GOTReplace`），但无人 include。
- **实际底层能力 = xdl + IL2CPP C API**。

### 4.2 IL2CPP / xDL 访问机制

**符号解析双通道**（`ResolveSym`）：先 `dlsym`，失败走 `xdl_sym → xdl_dsym`。xDL 能读到 `libil2cpp.so` 的导出/动态符号，突破 Android 7+ linker namespace 限制。

**IL2CPP 基础设施**（匿名 namespace）：
- `EnsureIl2CppApi`：dlopen 三段兜底（`RTLD_NOW|NOLOAD` → `RTLD_NOW` → `RTLD_LAZY`）+ `xdl_open`，惰性解析 22 个 il2cpp 导出函数（`domain_get / domain_get_assemblies / assembly_get_image / class_from_name / class_get_method_from_name / field_get_offset / runtime_invoke / thread_attach / string_new / array_new / field_static_get/set_value` 等）。
- `FindClassEverywhere`：跨全部 assembly 暴力扫描找类，不假设类在哪个 image。
- `AttachCurrentThreadIfNeeded`：**关键坑**——ImGui/GL 渲染在 JNI native 线程执行，非 Unity 脚本线程，`runtime_invoke` 前必须 `il2cpp_thread_attach`，否则闪退。
- `RuntimeInvoke`：统一封装 + `Il2CppException` 异常检查。

**反射调用范式**：`domain → assemblies → image → class → method/field → runtime_invoke`。参数用 `void*[]` 装地址，值类型（Vector3/int/bool）传栈变量地址，**bool 以 int32=1 传**。

### 4.3 关键功能实现要点

| 功能 | 目标类/方法 | 实现要点 |
|---|---|---|
| **分身** | `GameCoreCenter.SendDevide()` | `get_instance()` 拿单例 → runtime_invoke，不 hook FixedUpdate |
| **吐球（3 路径）** | `SetFreeTypeFlag(1)`+`FreeTypeClick(true)` / 循环 N 次 / `NetworkUpdater.ReqFreeType(0,1)` | 路径3最激进，**明写绕过 `HaveSelfBall/DisableFeedControl/BtnIsFeeding/SpitingDeltaTime/spitCTTime` 全部反作弊检查**，直达 UDP FeedType |
| **视野** | `GameCoreCenter.ViewScaleFactor`(float, 默认0.5) | 内存直写 `0.5*scale`，每帧对抗 Lerp 拉回 |
| **粘合** | `DrawCircle.ATime_SettingOffet`(静态float, 默认0) | 公式 `ATimeFen1 = 0.588 + offset`，反推写入 |
| **全员改名** | `PlayerDic` → `PlayerBase.Name` 直写 | 每轮改16人、4秒周期；优先 `NetPlayer.LastRank` 否则 `ID` 生成唯一 rank 名；可 NGUI 颜色前缀 `[RRGGBB]`；**绝不调 Rename/UpdateLable（渲染线程会崩）** |
| **名字缩放** | `NameText(LeLable).SelfTF.localScale` | 每帧写 `coeff/1.875`，根节点仍由游戏控制；`coeff=0` 为 restore 模式 |
| **三角合球** | 后台 std::thread 脚本化 | 左移+分身→右移+分身→上移循环20次分身；启动时临时关吐球加速避免并发 runtime_invoke 崩溃 |
| **摇杆** | `Move(Vector3)` + `ForceSendMove()` | 只更新 `JostickDir`，停止时额外 ForceSendMove 防方向残留 |

### 4.4 内存偏移的组织方式

- **运行时优先**：绝大多数字段用 `il2cpp_field_get_offset` 动态解析后覆盖硬编码默认值。
- **硬编码兜底**（强绑定当前游戏版本 + IL2CPP 版本 + 64位布局）：
  - PlayerBase：`ID@16, Name@24, OriName@32, NameLables@72, NetPlayer@464`
  - `NetPlayer.LastRank@64`（纯硬编码无兜底）
  - il2cpp String：`length@16, chars@20`（UTF-16，不处理代理对）
  - il2cpp Array：`length@24, 首元素@32, 元素@32+i*8`
  - 装箱值类型（boxed Int32）：真值 `@16`
  - 游戏公式系数：名字缩放 `1.875`、粘合基准 `0.588`、视野默认 `0.5`
- **护栏**：`LooksLikeValidManagedObject`（仅判指针≥0x10000 + klass 非空，很弱）；上限硬夹（dictCount≤200、len≤200、labels≤24）。

### 4.5 风险点（逆向层）

- **GC 安全隐患**：`il2cpp_string_new` 的托管 String 指针直写字段无 write barrier，靠每帧/每4秒重写"续命"。
- **并发脆弱**：多线程 `runtime_invoke` 是已知崩溃源，全靠 `g_call_mutex` 串行化（三角合球临时关吐球加速、名字缩放用 `try_lock` 非阻塞）。
- **强版本绑定**：游戏更新或换 32 位必崩/错乱。

---

## 五、输入体系

Android ImGui 无原生输入能力，且 native 回调多为 static（拿不到 Context），本项目用一整套 JNI 反射 + 内存 dex 补齐。

### 5.1 触摸注入（`触摸输入`）

- **5 槽位状态机**：`MAX_TOUCH_SLOTS=5`，每指一槽，`pointer_id` 对应 Java MotionEvent。
- **主指/副指分工**：第一根按下的指=主指，恒定注入 `MousePos + MouseDown[0]=true` 驱动 ImGui 控件；副指负责多指拖窗（`SetWindowPos(窗口名, ...)`）。
- **点按 vs 拖动**：`TAP_MAX_DIST_PX=25` 阈值，累积 `max_dist_sq` 区分；tap 为单帧脉冲。
- **主指释放补丁**：`g_zero_mouse_delta_next` 清 delta 防松手跳变；主指身份转交下一个仍按住的槽。
- `BeginMainTabItem`：触摸友好的标签页封装，命中判定 = 鼠标点击 ∪ 主指点按 ∪ 副指点按。

### 5.2 触摸穿透（`触摸窗口`）

- 每帧 `ClearTouchWindows` → Begin 后 `RegisterTouchWindow` 登记可交互窗口。
- `kTouchResizePadding=48px`（历史坑：旧值 160px 会把窗口外透明圈纳入拦截，导致游戏按钮点不到）。
- Java 触摸预处理调 `isImGuiComponentTouched(x,y)` / `nativeGetImGuiWindowBounds()`：无登记窗口→放行给游戏；有则按命中/合并 bounds 返回。实现"菜单吃触摸、菜单外区域放行给游戏"。

### 5.3 输入法桥接（`输入法桥接`）—— JNI 基础设施中枢

- **Activity 发现**：`ActivityThread.currentActivityThread().mActivities`（先 ArrayMap 后 HashMap 兼容）→ 遍历取 `!paused` 的前台 Activity（GlobalRef）。
- **内存 dex 加载键盘桥**：`InMemoryDexClassLoader` 加载 `imgui_dex`(12948B) 里的 `com.mxp.Helper` → `init(Activity)` 建隐藏 EditText + WindowManager 挂载 + TextWatcher + InputMethodManager。
- **文本回传闭环**：用户敲键 → 隐藏 EditText → TextWatcher → `Helper.nativeAddChar(codepoint)` → `ImGui::GetIO().AddInputCharacter`；功能键（Backspace/Enter/方向/Delete，硬编码 keyCode 67/66/21/22/112）经 `nativeKeyEvent` → `AddKeyEvent`。
- `ImeUpdateByImGui`：每帧读 `WantTextInput`，边沿变化时显/隐软键盘。
- 顺带承载 `OpenUrlByActivity`（系统浏览器开链接）、`CaptureCurrentClassLoader`、`GetClassFromCurrentLoader`（供防录屏拿宿主 ImGui 类）。

### 5.4 音量键（`音量键缩放`）

- `InMemoryDexClassLoader` 加载 `volume_key_dex`(5160B) 里的 `com.mxp.VolumeKeyHelper` → `init(Activity)` hook 音量键 → `RegisterNatives` 绑 `nativeVolumeKey`。
- 音量键 → `ApplyVolumeKeyUiScaleDelta`：调 `g_ui_font_scale ± 0.05`（clamp `[0.50,1.00]`）→ 写 `FontGlobalScale` + 保存。
- 功能关闭时手动 `AudioManager.adjustStreamVolume` 把音量还给系统（因 Java 侧已拦截按键）。

### 5.5 四个内嵌 DEX 辅助类作用汇总

| DEX 数据 | 大小 | Java 类 | 加载方式 | 作用 |
|---|---|---|---|---|
| `classes_dex.h` | 10644B | `com.example.imgui.ImGui` / `GLES3JNIView` | Main.cpp DexClassLoader 落盘加载 | **注入主 dex**：建 GLSurfaceView、生命周期回调、`setupImGuiViewOnMainThread`、`setSecureMode`、音量键分发 |
| `ime_dex_data.h` | 12948B | `com.mxp.Helper` | InMemoryDexClassLoader 内存加载 | **软键盘桥**：隐藏 EditText + IMM + TextWatcher，字符/按键回传 |
| `volume_key_dex_data.h` | 5160B | `com.mxp.VolumeKeyHelper` | InMemoryDexClassLoader 内存加载 | **音量键 hook**：拦截音量键转 native 回调 |
| `secure_dex_data.h` | 4584B | `com.mxp.SecureSurfaceHelper` | InMemoryDexClassLoader 内存加载 | **防录屏**：创建带 FLAG_SECURE 的 Surface 供菜单渲染 |

> 内存 dex（`InMemoryDexClassLoader`，要求 API 26+）**不落地磁盘、不进 APK 清单**，是刻意规避静态分析的手法。

---

## 六、授权与防护

### 6.1 卡密验证流程（T3 发卡平台）

服务器：`https://w.t3yanzheng.com/`（第三方"T3验证"SaaS）。

```
DrawT3AuthUI（每帧，门禁未过时替代主菜单）
  → T3_InitOnce：new T3Verify → initRSA(登录码,公告码,版本码,心跳码,appkey,RSA公钥)
       采用 RSA 编码模式；成功后异步拉版本/公告
  → T3_TryAutoLoginFromConfig：读 g_saved_kami 自动登录（一会话一次）
  → 用户输入卡密点"验证" → T3_LoginAsync（detach 线程）
       → T3Verify::login(kami, g_t3_machine_code)
            encodeParams：每值 RSA 加密转 hex → 拼 k=v&...&appkey → MD5 得签名 s
            → httpPost → httpPostRaw（libcurl HTTPS，SSL_VERIFYPEER/HOST，内嵌 CA blob）
            → decodeResponse（跳首行、去\r\n、删末尾一个'0'、RSA解密、JSON取code）
            校验：code==200 + |now-resp.time|≤5s 防重放 + token 验签
                  (md5(id+appkey+sOriginal+end_time+localtime("%Y%m%d%H%M")))
       → 成功：保存卡密落盘 + g_t3_authed=true + T3_StartHeartbeat（60s 周期）
```

**t3sdk 自实现的密码学**（刻意不调 OpenSSL API）：MD5(RFC1321)、纯 C++ 大数 RSA（`BN_MAX=72` 字, `m^e mod n`, PKCS#1 v1.5 填充）、手写 ASN.1/PEM 解析、字母表置换式 Base64 混淆、极简 JSON（基于字符串查找）。

**授权体系的弱点**：
- 机器码写死 `g_t3_machine_code = "android_inject_device"`（`getMachineCode()` 存在但从不调用）——**设备绑定形同虚设**，一卡可跨设备。
- appkey / 4 个调用码 / RSA 公钥全部明文硬编码在 `全局状态.cpp`，`strings` 即可提取；请求侧用公钥加密，可离线复现/伪造请求。
- 卡密明文落盘到 XML `<卡密>` 标签，无加密。
- 版本比较用字符串 `>`（`"999">"1000"` 为真，逻辑坑）。

### 6.2 防录屏（SECURE Surface）

核心思路：给普通 GLSurfaceView 加 FLAG_SECURE 会导致自身黑屏，故**另建带 SECURE 标志的独立 Surface，把 ImGui 菜单单独渲染到该安全层**，游戏画面留在普通层——截屏/录屏工具捕获不到 SECURE 层。

```
UI 勾选"无权限防录屏" → SetJavaSecureSurfaceMode（反射 ImGui.setSecureMode，
   须用创建 display 的同一 ClassLoader 否则 static 字段不共享）
  → 置 g_pending_secure_switch → 渲染循环消费
  → RebindNativeWindowForSecure → SecureLoadDex（内存加载 secure_dex）
  → SecureGetNativeWindow：SecureSurfaceHelper.getSurface + ANativeWindow_fromSurface
  → EnsureSecureEglSurface：FindCurrentEglConfig（共享主 context 的 config）
     + eglCreateWindowSurface
  → 每帧 RenderDrawDataToSecureSurface：保存 EGL 状态 → eglMakeCurrent 切安全层
     → 透明清屏 + RenderDrawData + eglSwapBuffers → 切回原 surface
  失败路径完整回退普通渲染，防界面全黑
```

---

## 七、第三方依赖清单及用途

| 依赖 | 用途 | 位置/形式 |
|---|---|---|
| **Dear ImGui** | 全部菜单 UI（立即模式）+ OpenGL3/Android 后端 | `jni/ImGui/`，预编译 `libimgui.a` |
| **ImGenie** (MIT, Aiekick 0.1.0) | 窗口 macOS 神灯开合动画 + 拖拽果冻抖动 | `res/ImGenie.*` |
| **ImCoolBar** (Aiekick/NewYaroslav) | macOS Dock 风格悬浮条（装饰彩蛋） | `res/ImCoolBar.*` |
| **xDL** | 增强 dlopen/dlsym，突破 linker namespace 读 il2cpp/android_runtime 符号 | `jni/xdl/`（源码编入） |
| **Dobby** | hook 框架 API 声明（**未启用**） | `App/dobby.h` |
| **libcurl** | HTTPS：T3 卡密验证 + 音乐 API/下载 | `res/music/curl/libcurl.a`（与音乐共用） |
| **OpenSSL** (libssl/libcrypto) | 给 curl 提供 TLS（t3sdk 自身刻意不调 OpenSSL） | `res/music/curl/libssl.a`+`libcrypto.a` |
| **内嵌 CA 证书** | curl `CURLOPT_CAINFO_BLOB`，CA 级 TLS 校验不依赖系统证书链 | `res/music/curl/include/curl/证书.h`（中文文件名） |
| **miniaudio** | 音频解码 + 播放设备回调（单头库，唯一实现单元在 MusicPlayer.cpp） | header-only |
| **nlohmann/json** | 音乐 API 响应解析（容错 parse） | `res/music/nlohmann/` |
| **pugixml** | 配置文件 XML 读写 | `jni/thirdparty/pugixml/` |
| **字体数据** | 中文字形（fz.ttf ~6.5MB，45px + `GetGlyphRangesChineseFull`；zt.ttf 13MB 未直接引用） | `App/uploaded_font.h`、`App/zt_ttf.h` |
| **T3验证 SDK** | 第三方发卡平台协议客户端（自研 C++ 实现） | `jni/t3sdk/` |
| **oiapi.net** | 音乐播放器依赖的第三方网易云 API（外部服务，版权/可用性风险） | 运行时 HTTP |

---

## 八、构建方式

### `Application.mk`（全局构建参数）

```
APP_ABI      := arm64-v8a      # 仅 64 位
APP_PLATFORM := android-21
APP_STL      := c++_static
APP_OPTIM    := release
```

### `Android.mk`（模块 `hook` → `libhook.so` 共享库）

**编译标志**：
- C：`-Wno-error=format-security -fvisibility=hidden -fexceptions`
- C++：追加 `-std=c++17 -Wno-error=c++11-narrowing -fpermissive -Wall -fexceptions`
- `-fvisibility=hidden` 隐藏符号（隐蔽），注入构造函数不受影响。

**include 路径**：`App`、`xdl/include`、`ImGui` + `ImGui/imgui`、`t3sdk`、`res`、`res/music`（含 `nlohmann`、`curl/include`）、`thirdparty/pugixml`。

**源文件**（wildcard，`filter-out %.bak`）：`xdl/src/*.c`、`App/*.{cpp,cc,c}`、`res/*.cpp`、`res/music/*.cpp`、显式 `t3sdk/t3sdk.cpp` + `thirdparty/pugixml/pugixml.cpp`、ImGui 核心 `ImGui/*.cpp` + backends（`imgui_impl_android` / `imgui_impl_opengl3`）。

**链接系统库**：`-llog -landroid -lEGL -lGLESv1_CM -lGLESv2 -lGLESv3 -lm -ldl -lz`

**预编译静态库**：`LOCAL_STATIC_LIBRARIES := imgui musiccurl musicssl musiccrypto`
- `ImGui/imgui/arm64-v8a/libimgui.a`
- `res/music/curl/{libcurl,libssl,libcrypto}.a`

---

## 九、关键文件速查表

| 你想了解… | 看这个文件 | 关键锚点 |
|---|---|---|
| **注入怎么启动** | `jni/App/Main.cpp` | `_init`(879)、`native_thread_func`(786)、`getJavaVMViaReflection`(107)、`getCurrentActivityInstance`(158) |
| **每帧渲染循环** | `jni/App/imguijni.cpp` | `GLES3JNIView_step`(172)、`init`(78) |
| **游戏功能怎么实现** | `jni/res/游戏函数调用.cpp` | IL2CPP 基础设施(18–522)、吐球直调(741)、改名(1320)、名字缩放(1765)、三角合球(1913) |
| **菜单长什么样/有哪些开关** | `jni/res/主界面绘制.cpp` | `DrawOldMainUI`(81)、每帧 tick `DrawFloatingWindow`(268) |
| **所有全局开关/T3常量在哪定义** | `jni/res/全局状态.cpp/.h` | T3 硬编码(16–48)、机器码写死(48)、作弊开关(功能表)、日志(119–198) |
| **配置怎么存/存哪** | `jni/res/Config.cpp` + `配置初始化.cpp` | `SimpleTagForKey`(Config:42)、注册8字段(配置初始化)、路径 `<app>/files/句号的辅助/天天开心.xml` |
| **触摸怎么注入/穿透** | `jni/res/触摸输入.cpp` + `触摸窗口.cpp` | `TouchHandlePointer`(117)、`触摸_更新ImGuiIO`(160)、`kTouchResizePadding=48` |
| **软键盘怎么弹** | `jni/res/输入法桥接.cpp` | `ImeLoadDex`(288)、`ImeFindActivityByActivityThread`(47)、`ImeNativeAddChar`(141) |
| **音量键怎么调UI** | `jni/res/音量键缩放.cpp` | `LoadVolumeDex`(104)、`ApplyVolumeKeyUiScaleDelta`(65) |
| **卡密验证流程** | `jni/res/卡密验证.cpp` + `jni/t3sdk/t3sdk.cpp` | `T3_LoginAsync`(75)、`T3Verify::login`(844)、`httpPostRaw`(651)、`encodeParams`(813) |
| **防录屏怎么做** | `jni/res/防录屏.cpp` | `SecureLoadDex`(47)、`RenderDrawDataToSecureSurface`(238)、`EnsureSecureEglSurface`(190) |
| **窗口开合动画** | `jni/res/ImGenie.cpp` + `ImGenie捕获.cpp` | `Allow` 状态机(855)、`Capture`(1364)、`捕获_创建纹理`(12) |
| **音乐播放器** | `jni/res/music/MusicPlayer.cpp` | `AudioCallback`(167,鼓点检测204–338)、`PlayThread`(601)、`音乐窗口`(818) |
| **构建配置** | `jni/Android.mk` + `jni/Application.mk` | ABI/链接库/静态库 |
| **内嵌 dex（4个）** | `App/classes_dex.h`、`App/ime_dex_data.h`、`res/volume/volume_key_dex_data.h`、`res/secure/secure_dex_data.h` | 各 Java 辅助类字节数组 |

---

## 十、给负责人的综合风险提示

1. **反作弊绕过明确成文**：吐球直调路径注释直写绕过 `HaveSelfBall/DisableFeedControl/spitCTTime` 等全部检查，是外挂性质最强部分。
2. **授权体系可绕过**：机器码写死 + 敏感常量全明文 + 卡密明文落盘 → 设备绑定失效、请求可离线伪造。
3. **强版本绑定**：大量硬编码偏移/游戏公式系数（16/24/32/72/464/64、1.875、0.588、0.5）绑定当前游戏版本 + IL2CPP + 64位；游戏更新必崩。
4. **opsec 泄露面**：日志默认全开落盘到世界可读 `/storage/emulated/0/Android/data/com.ztgame.bob/ndk.log`；源码含飞机盘链接、窗口标题"句号的辅助"、T3 指纹，易溯源。
5. **并发脆弱**：IL2CPP 多线程 runtime_invoke 是已知崩溃源，靠 `g_call_mutex` 串行化 + 临时关功能规避。
6. **死代码需清理**：`dobby.h`、`imguiSimpleToast.hpp`、`MainDexLoad/MainDexGetClass`、`ParseConfigFloat`、视野模块大量解析未用字段、日志 tab 过滤/发送空壳、`CleanupMusicPlayer` 从不调用。
7. **持久化范围有限**：仅 UI 偏好 + 卡密自动登录落盘；游戏作弊开关一律不持久化，每次启动默认关闭（有意为之，需向用户说明）。
