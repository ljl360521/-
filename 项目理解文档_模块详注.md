# 附录：各模块逐行通读详注

> 本附录是 11 个通读小组提交的原始详细笔记，作为主文档《项目理解文档.md》的证据支撑。



---

## 模块：`game-function-calls`

我已完整读取两个目标文件（`游戏函数调用.cpp` 全 2070 行分两遍读取，`游戏函数调用.h` 全 64 行），并核对了依赖模块 `全局状态.h` 与调用方 `主界面绘制.cpp`。以下是通读技术笔记。

---

# 通读技术笔记：`jni/res/游戏函数调用.*`

## 0. 结论先行（重要澄清）

尽管头文件与 `GameCall_GetHookDebugInfo` 的函数名里带 "Hook" 字样，**本组代码实际上不做任何 inline hook / Dobby / PLT hook**。第 2037 行明确注释："函数名保留兼容 UI，当前内容是 IL2CPP/runtime_invoke 调试，不再是 Dobby hook"。全部逆向手法是：**通过 IL2CPP 官方 C API（`il2cpp_runtime_invoke` + 反射）调用游戏 C# 方法，或用 `il2cpp_field_get_offset` 拿到字段偏移后直接读写托管对象内存**。

这也不是 FPS 外挂：**没有 ESP 透视、自瞄、无后座、世界到屏幕投影（W2S）**。游戏是 **Agar.io/球球大作战 类**（`.io` 吞噬球游戏）——证据：`SendDevide`(分身)、`FreeTypeClick`/吐球(feed)、`Move`(摇杆)、`ViewScaleFactor`(视野)、合球、`TopPlayers` 排行榜。任务里提到的「透视/自瞄/无后座/坐标转换」在本组文件中**不存在**，本笔记如实标注。

---

## 1. 文件作用与代码规模

| 文件 | 行数 | 作用 |
|---|---|---|
| `游戏函数调用.h` | 64 | 对外 API 声明层。声明 13 个 `GameCall_*` 导出函数，供 `主界面绘制.cpp` 的 ImGui 菜单调用。每个声明带详细中文注释说明用途与实现路径 |
| `游戏函数调用.cpp` | 2070 | 逆向核心实现。封装 IL2CPP C API，解析游戏类/方法/字段，实现分身、吐球（3 种路径）、三角合球、视野、粘合、全员改名、球上名字缩放等功能 |

`.cpp` 结构（按注释分节）：
- L18–522：匿名 namespace 内的 IL2CPP 基础设施（API 结构体、符号解析、类/方法查找、runtime_invoke 封装、Move 内部实现）
- L524–804：分身 / 吐球（3 种实现）
- L806–961：视野修改
- L963–1029：粘合修改
- L1031–1620：排名改名（全员独特 rank）+ 恢复原名
- L1623–1904：球上名字大小（独立功能）
- L1907–2070：三角合球（后台线程）+ Move/Stop + 状态/调试查询

---

## 2. IL2CPP 基础设施（匿名 namespace，L18–522）

### 2.1 类型别名与函数指针（L20–50）
把所有 IL2CPP 句柄类型 `typedef` 成 `void`（`Il2CppDomain/Assembly/Image/Class/Method/Object/Field/Thread` 等），只当不透明指针用。为 22 个 IL2CPP 导出函数定义了函数指针类型，如：
- `il2cpp_domain_get` / `il2cpp_domain_get_assemblies`
- `il2cpp_class_from_name(image, namespace, name)`
- `il2cpp_class_get_method_from_name(class, name, argc)`
- `il2cpp_class_get_field_from_name` / `il2cpp_field_get_offset`
- `il2cpp_runtime_invoke(method, obj, params, exc*)`
- `il2cpp_thread_attach` / `il2cpp_thread_current`
- `il2cpp_string_new` / `il2cpp_array_new` / `il2cpp_object_get_class`
- `il2cpp_field_static_get_value` / `il2cpp_field_static_set_value` / `il2cpp_field_get_value`

### 2.2 `struct Il2CppApi`（L52–76）+ `static Il2CppApi g_api`（L84）
全局单例，缓存两个句柄（`dl_handle` 来自 `dlopen`，`xdl_handle` 来自 `xDL`）+ 22 个解析出的函数指针。

### 2.3 `struct UnityVector3 { float x,y,z; }`（L78–82）
构造传给 `Move(Vector3)` 的参数，按 IL2CPP 值类型约定，`void* params[1] = { &dir }` 传地址。

### 2.4 符号解析：`ResolveSym`（L140–151）
**双通道解析**：先 `dlsym(dl_handle)`；失败再走 xDL 的 `xdl_sym` → `xdl_dsym`。xDL（`#include "xdl.h"`）是绕过 Android 7+ linker namespace 限制、能读到 `libil2cpp.so` 导出/动态符号的增强 dlsym。这是能在受限命名空间里拿到 il2cpp 符号的关键。

### 2.5 `EnsureIl2CppApi`（L168–230）
- **dlopen 三段兜底**（L170–183）：`RTLD_NOW|RTLD_NOLOAD` → `RTLD_NOW` → `RTLD_LAZY`；同时 `xdl_open("libil2cpp.so", XDL_DEFAULT)`。注意 L14–16 对 `RTLD_NOLOAD` 未定义时置 0 的兼容处理。
- 惰性解析全部 22 个符号（仅在指针为空时解析，可重入）。
- 只对 6 个核心符号（domain_get / domain_get_assemblies / assembly_get_image / class_from_name / class_get_method_from_name / runtime_invoke）做缺失校验（L217–227），用 `AppendMissing` 拼出缺失清单。

### 2.6 类查找 `FindClassEverywhere`（L250–278）
遍历 `il2cpp_domain_get_assemblies` 返回的全部程序集，逐个 `assembly_get_image` + `class_from_name(img, ns, klass)`，命中即记录 `g_found_image_name`。**跨全部 assembly 暴力扫描**，不假设类在哪个 image 里。失败返回详细文案（含已扫描程序集数）。

### 2.7 线程 attach `AttachCurrentThreadIfNeeded`（L240–248）
**关键坑**：ImGui/GL 渲染是在 JNI native 线程执行，不是 Unity 脚本线程。`runtime_invoke` 前若 `il2cpp_thread_current()` 为空，必须 `il2cpp_thread_attach(domain)`，否则跨线程调用闪退。每次 attach 递增 `g_attach_count`。

### 2.8 `RuntimeInvoke` 封装（L280–293）
统一 `il2cpp_runtime_invoke` + 异常检查（`Il2CppException* exc`），异常时把 `exc=%p` 写入 `out`。所有游戏方法调用都过这层。

---

## 3. 分身 / 吐球（3 种实现路径）

### 3.1 分身 `GameCall_InvokeDevide`（L524–577）
调用链：`EnsureIl2CppApi → GetDomain → AttachCurrentThreadIfNeeded → ResolveGameCoreMembers → GetGameCoreInstance → RuntimeInvoke(SendDevide)`。
- `ResolveGameCoreBase`（L295–306）解析 `GameCoreCenter` 类 + `get_instance` 属性 getter。
- `GetGameCoreInstance`（L375–405）：优先 `get_instance()`（静态属性 getter，obj=null）；兜底遍历静态字段名 `{"instance", "<instance>k__BackingField"}` 用 `field_static_get_value` 读单例。
- 直接 `runtime_invoke(SendDevide)`，注释强调"不再读取/直调 methodPointer，也不再 hook FixedUpdate，避免调用约定猜错闪退"。

### 3.2 吐球（正常路径）`GameCall_InvokeSpitBall`（L579–657）
两步：
1. `SetFreeTypeFlag(1)` —— flag=1 是吐球方向（0 是分身方向）。
2. `FreeTypeClick(true)` —— `force=true` **跳过 `DisableFeedControl` 检查**。IL2CPP 里 bool 以 int32 传，值 1=true。运行时用 `class_get_method_from_name(cls,"FreeTypeClick",1)` 探测有无 force 参数（L632），无则调 0 参重载。内部走 `ReqFreeType(0, freeTypeFlag=1)` → UDP FeedType。

### 3.3 吐球加速 `GameCall_InvokeSpitBallFast`（L659–739）
`count` 夹在 1–20。一次 `SetFreeTypeFlag(1)` 后**一帧内循环 N 次 `FreeTypeClick(true)`**。注释：无客户端时间限制，**不触发 `spitCTTime` 反作弊计数器**。

### 3.4 吐球直调（绕过全部检查）`GameCall_InvokeSpitBallDirect`（L741–804）
最激进路径：解析 `NetworkUpdater` 类（`ResolveNetworkUpdaterMembers` L407–432），拿 `get_instance` + `ReqFreeType(int type,int flag)`，直接循环 `ReqFreeType(0,1)` N 次。注释明说**绕过 `HaveSelfBall()`、`DisableFeedControl`、`BtnIsFeeding`、`SpitingDeltaTime` 所有检查**，直达 UDP FeedType。这是明显的反作弊绕过点。

### 3.5 摇杆移动 `InvokeMoveInternal`（L450–520）/ `GameCall_InvokeMove` / `GameCall_StopMove`（L2024–2030）
- `ResolveMoveMembers`（L332–346）解析 `Move(Vector3)`（1 参）与 `ForceSendMove()`（0 参）。
- `Move` 只更新 `JostickDir`，游戏自身 `FixedUpdate/SendMove` 按 `SendOffset` 节流发包。
- **停止时**（`forceSend=true`，即 `StopMove` 传 0,0,0）额外调 `ForceSendMove()`，避免松手后方向残留。
- `GameCoreCenter.JostickDir` 字段在 `ResolveGameCoreMembers`（L319–324）解析（带 `_JostickDir` 兜底名），但代码里只解析未主动读回。

---

## 4. 视野修改（L806–961）

- 状态变量（L808–829）：`CameraFollow` 类及一堆字段 `selfCamera / GrassCamera / Size / Width / SizeFen1 / Rect / ScaleSize / view / xUpdate`；`UnityEngine.Camera.set_orthographicSize`；`CameraFollow.get_instance`、`GameOverPullUp(float,float,Ease)`；`GameCoreCenter.IsGameOverPause`。
- `ResolveViewMembers`（L831–897）解析上述全部（很多字段解析了但当前实现未用，属早期方案遗留）。
- **实际生效路径极简**：`GameCall_SetViewScale`（L919–961）—— 拿 `GameCoreCenter.ViewScaleFactor`（实例 float 字段，默认 0.5）的偏移，**直接写内存** `*(float*)((char*)gccInstance+offset) = 0.5f*scale`。注释说明 view.z 公式含 `ViewScaleFactor*curviewscale`（L93–95），值越大视野越大。
- `GameCall_ResetViewPullup`（L899–917）把 `ViewScaleFactor` 写回 0.5、清 `g_view_pullup_done`。
- **由 `主界面绘制.cpp` 每帧调用**以对抗游戏 `Update2D()` 的 Lerp 拉回（头文件 L33–34）。

---

## 5. 粘合修改（L963–1029）

- 目标：`DrawCircle.ATime_SettingOffet`（**静态 float 字段**，默认 0）。
- 公式（L98/L1022）：`ATimeFen1 = 0.588 + ATime_SettingOffet`，值越大粘合（合球贴合）越快。
- `GameCall_SetMerge(value)`（L1002–1029）：用户传的 value 是 ATimeFen1 目标值，反推 `offset = value - 0.588f`，用 `field_static_set_value` 写。
- `GameCall_ResetMerge`（L988–1000）写回 0。

---

## 6. 排名改名（全员独特 rank，L1031–1620）——本组最复杂逻辑

### 6.1 硬编码字段偏移（L1039–1069）
| 字段 | 默认偏移 | 来源 |
|---|---|---|
| `PlayerBase.ID` | 16 | 运行时 `field_get_offset` 覆盖 |
| `PlayerBase.Name` | 24 | 同上 |
| `PlayerBase.OriName` | 32 | dump 注释 `OriName@32` |
| `PlayerBase.NameLables` | 72 | dump 注释 `NameLables@72` |
| `PlayerBase.NetPlayer` | 464 | 运行时覆盖 |
| `NetPlayer.LastRank` | 64 | 硬编码 |

`kRenameBatch = 16`（L1069）：每轮最多改 16 人，`g_rename_cursor` 续跑，避免一帧改太多。`g_rename_running` 原子锁防重入。

### 6.2 托管对象内存布局知识（硬编码，版本相关）
- **il2cpp String**（`ReadIl2CppStringUtf8` L1254–1278）：`+0 klass, +8 monitor, +16 length(int32), +20 chars(UTF-16)`。手写 UTF-16→UTF-8，只处理 BMP（≤3 字节），**不处理代理对**，长度截断 200。
- **il2cpp Array**：长度在 `+24`，首元素在 `+32`，元素按指针数组 `+32+i*8`（L1395/L1446/L1578/L1868）。
- **装箱值类型**（boxed Int32，如 Dictionary Count）：真值在 `+2*sizeof(void*)`=+16（L1364/L1549/L1841）。

### 6.3 核心流程 `GameCall_RenameToRank`（L1320–1503）
1. `GameCoreCenter.instance.PlayerDic`（Dictionary）用偏移读出。
2. 反射调 `get_Count` / `get_Values`；`array_new(PlayerBase, count)` + `ValueCollection.CopyTo(arr,0)` 把玩家拷进数组（dictCount 夹 200）。
3. `get_SelfPlayer()` 拿自己，`selfId = ReadPlayerUniqueId`。
4. **"显示自我"三态机**（L1422–1442）：关→开备份原名（`BackupSelfOriginalName` 优先读 OriName 再读 Name）；开→关立即 `RestoreSelfOriginalName`；持续关每轮把自己写回备份，防被误改。
5. 逐个玩家 `BuildUniqueRankString`（L1205–1240）生成唯一 rank 名：**优先 `NetPlayer.LastRank`（>0 且 ≤100000），否则用 `PlayerBase.ID`**；用 `FormatRankDisplay`（L1170–1203）粗略化（1000→1千 / 150032→15万 / 1亿）或详细数字；若开色用 NGUI 颜色前缀 `[RRGGBB]`（从 `g_rename_name_color` 转字节）。
6. `WriteNameFieldOnly`（L1242–1251）：`il2cpp_string_new` 造新 String，**直接把指针写进 `PlayerBase.Name` 字段偏移**。
7. 注释三令五申（L1034–1036）：**绝不调 `Rename/UpdateLable/HandleName`（渲染线程会崩）、绝不用 `TopPlayers` 名次**。

### 6.4 恢复 `GameCall_RestoreAllNames`（L1506–1620）
同样遍历 PlayerDic，读每人 `OriName` 写回 `Name`；Self 用备份/OriName 恢复；复位游标与备份状态。

### 6.5 有效性护栏 `LooksLikeValidManagedObject`（L1071–1079）
只检查：指针 ≥0x10000、`*p`（klass 指针）非空且 ≥0x10000。**非常弱**——遇到脏内存仍可能崩。

---

## 7. 球上名字大小（独立功能，L1623–1904）

- 逆向结论（L1623–1637 注释）：真正可见的名字变换是 `NameText(LeLable).SelfTF`，不是 `TargetNameScale2` 根节点；游戏根节点公式 `SelfTF.localScale = Clamp(...)*1.875`（`kGameNameScaleCoeff = 1.875f`, L1639）。
- 做法：每帧只改 `NameText.SelfTF.localScale = coeff/1.875`，根节点仍由游戏控制 → 世界缩放 = `Clamp(...)*coeff`，**不复制第二份名字**。
- `GameCall_TickNameScale(coeff)`（L1765–1904）：
  - `coeff<=0.0001` 为 restore 模式，强制 localScale=1。coeff 夹 0.3–8.0。
  - 遍历 `PlayerDic → PlayerBase.NameLables(List) → 每个 TargetNameScale2`，`NS_ApplyOneTns`（L1731–1762）解析 `TargetNameScale2.NameText` → `LeLable.SelfTF`，设 localScale，并按 `LeLable.Length` 做 X 居中补偿 `pos.x = -Length*0.5*s`（因游戏原本用未缩放的 `-Length*0.5` 居中）。
  - 字段偏移全部运行时 `field_get_offset` 解析（`NS_ResolveNameTextOff/NS_ResolveLeSelfTF/NS_ResolveLeLength`）。
  - 用 `g_call_mutex.try_lock()`（L1780）**非阻塞**获取锁，抢不到直接返回，避免卡渲染线程。

---

## 8. 三角合球（后台线程，L1907–2022）

- `GameCall_TriangleMerge`（L1913）：`g_triangle_running` 已开则停；否则前置检查后 `detach` 一个 `std::thread`。
- 线程内先 `AttachCurrentThreadIfNeeded`，然后脚本化操作：左移摇杆+ForceSendMove+分身 → 右移+分身 → 上移后循环 20 次分身（每步 `sleep 50–80ms`），最后 Move(0,0,0)+ForceSendMove 停摇杆。
- 角度→方向：`x=sin(angle), z=cos(angle)`（头文件 L26，本文件用固定 ±1 向量）。
- **并发保护**：启动时把 `g_spitball_accelerate` 暂存并关闭（L1948–1949），结束恢复，避免与每帧吐球并发 `runtime_invoke` 崩溃；每个子操作都 `lock_guard(g_call_mutex)`。

---

## 9. 全局变量 / 状态一览

**本文件内 static（L84–124 等）**：`g_api`、`g_call_mutex`/`g_status_mutex`、`g_last_status`、缓存的类/方法/字段指针（`g_game_core_class`、`g_send_devide_method`、`g_move_method`、`g_network_updater_class` 等）、`g_last_move_dir`、`g_found_image_name`、`g_last_instance/g_last_thread`、原子计数器 `g_button_click_count/g_move_call_count/g_success_count/g_fail_count/g_attach_count`。

**从 `全局状态.h`（extern）读取的开关**（依赖注入点）：
- 吐球加速：`g_spitball_accelerate`、`g_spitball_speed`
- 三角合球：`g_triangle_running`
- 改名：`g_rename_use_color`、`g_rename_name_color`(ImVec4)、`g_rename_detailed_rank`、`g_rename_show_self`

注意：本文件用到的 `g_rename_show_self` 等虽在 `全局状态.h` 声明，但 `g_rename_detailed_rank/g_rename_use_color/g_rename_name_color/g_rename_show_self` 在头里也有 extern 声明（L113–116）。

---

## 10. 用到的关键技术与调用链

- **IL2CPP 反射调用**（核心）：`domain → assemblies → image → class → method/field → runtime_invoke`。参数用 `void*[]` 装地址，值类型（Vector3/int/bool）传栈变量地址，bool 用 int32=1。
- **xDL**（`xdl.h`，`xdl_open/xdl_sym/xdl_dsym`）：突破 Android linker namespace，拿到 `libil2cpp.so` 符号；配合 `dlopen` 三级兜底。
- **直接内存读写**：`il2cpp_field_get_offset` 得偏移后 `*(T*)((char*)obj+off)` 读写，绕过 `il2cpp_field_(static_)set_value`（视野、粘合、改名、名字缩放大量使用）。
- **线程管理**：`il2cpp_thread_attach` 解决渲染线程非脚本线程问题；`std::thread + detach` 跑三角合球；`std::mutex`/`try_lock`/`atomic` 做并发保护。
- **未使用**：本文件没有 OpenGL/miniaudio/curl/Dobby/JNI 直接调用（JNI/EGL 在 `全局状态.h` 里是别的模块用的；T3 授权、curl、音频等都不在本组）。

---

## 11. 模块依赖关系

**本组引用（include）**：`游戏函数调用.h`、`全局状态.h`（拿功能开关）、`xdl.h`、`dlfcn.h` + STL。间接依赖 ImGui（`g_rename_name_color` 是 `ImVec4`，经 `全局状态.h` 传入）。

**被谁引用**：仅 `主界面绘制.cpp`。调用方式两类（见该文件）：
- **按钮触发**（一次性）：`InvokeMove`(L56)、`InvokeDevide`(L107)、`InvokeSpitBall`(L112)、`TriangleMerge`(L128)、`RestoreAllNames`(L151)、`RenameToRank`(L161)。
- **每帧渲染循环轮询**（持续对抗游戏）：`InvokeSpitBallDirect`(L361)、`SetViewScale`/`ResetViewPullup`(L367/L370)、`SetMerge`/`ResetMerge`(L377/L380)、`TickNameScale`(L389/392/394)、每 4 秒 `RenameToRank`(L405)、总开关关闭时 `RestoreAllNames`(L412)。

`GameCall_GetLastStatus`(L2032) 与 `GameCall_GetHookDebugInfo`(L2037) 供 UI 显示状态/调试（后者 dump 全部句柄、类/方法指针、计数器）。

---

## 12. 值得注意 / 风险点

1. **反作弊绕过明确成文**：`FreeTypeClick(true)` 的 `force` 跳过 `DisableFeedControl`；`ReqFreeType(0,1)` 直调绕过 `HaveSelfBall/BtnIsFeeding/SpitingDeltaTime/spitCTTime`。注释直接写明目的，是外挂性质最强的部分（L784/L710/L626）。
2. **GC 安全隐患**：`WriteNameFieldOnly`（L1249）把 `il2cpp_string_new` 返回的托管 String 指针**直接写进字段，无 GC write barrier**。理论上新 String 可能在下次 GC 被回收/移动，造成悬垂或错乱。改名靠每帧/每 4 秒重写来"续命"。
3. **大量硬编码偏移**（16/24/32/72/464/64、String +16/+20、Array +24/+32、boxed +16、1.875、0.588、0.5）——**强绑定当前游戏版本 + IL2CPP 版本 + 64 位布局**；游戏更新或换 32 位必崩/错乱。虽有 `field_get_offset` 运行时覆盖，但 `NetPlayer.LastRank=64`、String/Array 布局是纯硬编码无兜底。
4. **弱指针校验**：`LooksLikeValidManagedObject` 只判 `>0x10000` + klass 非空，不查类型不查存活，脏数据仍会崩。上限截断（dictCount≤200、len≤200、labels≤24）是防御式硬夹。
5. **脏代码**：L1493–1497 `SetMsg` 里的 `endIdx - (endIdx>0 ? ...)` 表达式明显是废弃的绕弯写法，随即被 L1498–1502 的 `clearMsg` 覆盖，属未清理的死逻辑。
6. **UTF-16 转换不完整**：`ReadIl2CppStringUtf8` 不处理代理对（emoji/生僻字），名字含这类字符会截断/乱码。
7. **并发脆弱**：作者反复注释 IL2CPP 调用不能并发（三角合球要临时关吐球加速、`TickNameScale` 用 `try_lock`），说明多线程 `runtime_invoke` 是已知崩溃源，全靠 `g_call_mutex` 串行化。
8. **视野/相机字段大量解析但未使用**（`GrassCamera/SizeFen1/Rect/ScaleSize/view/xUpdate`、`GameOverPullUp`、`IsGameOverPause`、`Camera.set_orthographicSize`）——是"用相机 orthographicSize 改视野"早期方案的遗留，最终实现改成写 `ViewScaleFactor` 单字段，旧解析代码没删，是维护噪声。
9. **头文件与实现名不符**：`GameCall_GetHookDebugInfo` / `GameCall_TickNameScale(float mul)`（头 L52 参数名叫 mul，实现叫 coeff）等命名与实际语义有偏差，仅为兼容 UI 保留。


---

## 模块：`t3sdk`

我已完整读取 `t3sdk.h`（250 行）与 `t3sdk.cpp`（1248 行）全部内容，并核对了它与项目其它模块的引用关系。下面是通读记录。

---

# t3sdk 模块通读技术笔记

## 0. 重要结论（先纠正任务前提）

**本组 `t3sdk` 与 il2cpp / 游戏引擎访问毫无关系。** 它不是"读内存 / 找类找方法找字段 / GameObject·Transform 遍历 / 反射"的运行时 SDK，里面**没有任何** `il2cpp_*`、`Dobby`/hook、`ptrace`/`/proc/pid/mem`、Unity 结构体或指针偏移的代码。

`t3sdk` 实际上是一个 **网络授权 / 卡密（License Key）验证 SDK**，对接第三方发卡验证平台 **"T3验证"（`https://w.t3yanzheng.com/`，官网 t3yanzheng.com）**。它的职责是：卡密登录、心跳保活、公告/版本/更新检查、用户体系、设备绑定、远程变量、在线人数统计等。在本项目里它扮演的是 **菜单启动前的付费/授权门禁**——验证通过才允许使用 ImGui 作弊菜单。真正的 il2cpp / 游戏函数调用逻辑在别的目录（`jni/App`、`jni/xdl`、`jni/Dobby` 等），不在本组。

以下按要求逐项给出准确记录。

---

## 1. 每个文件的作用与代码规模

| 文件 | 行数 | 作用 |
|---|---|---|
| `jni/t3sdk/t3sdk.h` | 250 | 公开头文件。声明 10 个结果结构体（`T3Result`/`T3LoginResult`/…）、`CustomBase64`、`RSACrypto`、主类 `T3Verify` 的完整 API，以及工具函数 `getMachineCode()`。定义服务器地址宏 `T3_SERVER_URL`。 |
| `jni/t3sdk/t3sdk.cpp` | 1248 | 全部实现。自带 MD5、标准/自定义 Base64、纯 C++ 大数 RSA、极简 JSON 解析、基于 libcurl 的 HTTPS 客户端、机器码采集，以及 `T3Verify` 全部 30+ 个业务方法。声明"纯 C++ 实现，不依赖 OpenSSL"。 |

代码分区（cpp）：MD5（46–177）→ Hex 工具（181–198）→ 标准 Base64 解码（204–216）→ 大数 `BigNum`（224–348）→ `RSAKey`（352–355）→ `RSACrypto`（359–490）→ `CustomBase64`（496–555）→ JSON 辅助（561–635）→ HTTP 客户端（643–680）→ `getMachineCode`（686–742）→ `T3Verify`（744–1247）。

---

## 2. 关键类 / 函数：签名 + 实现要点

### 2.1 MD5（匿名命名空间，46–177）
- `struct MD5Context{state[4],count[2],buffer[64]}`，配合 `MD5_F/G/H/I`、`MD5_ROL`、`MD5_FF/GG/HH/II` 宏，是标准 RFC1321 实现。
- `md5Transform / md5Init / md5Update / md5Final`：教科书 64 轮压缩。
- `std::string md5String(const std::string& str, bool upperCase=false)`（168）：算摘要并按 16 字节输出十六进制。**签名和 token 校验都用它**。

### 2.2 十六进制 / Base64 工具（181–216）
- `bytesToHex` / `stringToHex`（大写十六进制）、`hexCharToInt`。
- `stdBase64Decode`（204）：标准字母表 Base64 解码，专用于解析 RSA 公钥 PEM 的 DER 内容，容错跳过 `=`、换行、空格与非法字符。

### 2.3 大数运算 `RSACrypto::BigNum`（224–348）
- **固定 72 个 32-bit 字**（`#define BN_MAX 72`，222 行）→ 最大约 2304-bit 模数；示例密钥为 1024-bit。
- 提供 `fromBE/toBE`（大端字节 ↔ 大数）、`cmp`、`add/sub`、`shl/shr`、`mul`（schoolbook 乘法）、`mod`（移位减法取模，329–335）、`modpow`（平方-乘模幂，338–347）。
- 所有运算在超过 `BN_MAX` 时**静默截断 `len`**（如 265、293、313 行），属潜在溢出隐患，但对 1024-bit 密钥够用。

### 2.4 `RSACrypto`（359–490）
- 构造：`RSACrypto(const std::string& publicKeyPem)` → `parsePEM` 后 `encryptBlockSize_ = keySize_-11`（PKCS#1 v1.5 预留 11 字节），`decryptBlockSize_ = keySize_`。
- `parsePEM`（371–424）：**手写 ASN.1 DER 解析器**。定位 `-----BEGIN/END PUBLIC KEY-----`，Base64→DER，然后逐层跳过 外层 SEQUENCE → AlgorithmIdentifier → BIT STRING → 内层 SEQUENCE → INTEGER n（去掉前导 0x00）→ INTEGER e。带截断/越界检查并抛 `std::runtime_error`。只取 `n` 和 `e`（公钥）。
- `encrypt`（426–457）：分块，每块做 **PKCS#1 v1.5 type-2 填充**（`00 02` + 随机非零填充 + `00` + data，随机数用 `std::mt19937`+`random_device`），再 `m^e mod n`，输出定长块拼接。
- `decrypt`（459–481）：`c^e mod n`——**注意用的是公钥指数 `e`（465 行）**，即它其实是"公钥验签/解签"操作，只对服务器用私钥签出的数据有效；随后剥离 PKCS#1 填充（找第一个 `0x00` 分隔）。命名叫 decrypt 但语义是 RSA 公钥运算，属该 SDK 的既定设计。
- `encryptToHex` / `decryptFromBase64`：便捷封装。

### 2.5 `CustomBase64`（496–555）
- 构造要求自定义字符集长度必须 64（否则抛异常）。
- `encode`（501）：先按标准 Base64 编码，再把标准字母表字符逐个映射到自定义字母表（字母表置换式混淆）。
- `decode`（524）：先映射回标准字母表，补 `=` 到 4 的倍数，再标准解码。
- `encodeToHex`：encode 结果再转 Hex。这是 `encodeType_==0`（Base64 模式）时的加密载体。

### 2.6 极简 JSON 解析（561–635，匿名命名空间）
- `unicodeToUtf8`、`decodeJsonString`（处理 `\uXXXX`、转义）。
- `jsonGetString`（597）/`jsonGetInt`（627）：**基于 `find("\"key\"")` 的手写取值器**，支持字符串、数字、bool、null。非严格 JSON 解析器，靠约定键名工作。响应字段都用它抽取。

### 2.7 HTTP 客户端（643–680，匿名命名空间）
- `httpPostRaw(url, postData)`（651）：`std::call_once` 全局 `curl_global_init`；`curl_easy` 发 POST；`CURLOPT_TIMEOUT=15`、`CONNECTTIMEOUT=8`、UA=`"T3VerifyCpp/1.0"`、`FOLLOWLOCATION`；**开启 `SSL_VERIFYPEER/VERIFYHOST`**，并通过 `CURLOPT_CAINFO_BLOB` 用**内嵌 CA 证书 blob**（`cacert_data`，来自 `curl/证书.h`，`CURL_BLOB_NOCOPY`）——即 CA 级 TLS 校验。非 2xx 或非 `CURLE_OK` 返回空串。

### 2.8 `getMachineCode()`（686–742）
- 采集网卡 MAC 后 `md5String(..., true)`（大写）。
- 平台分支：Windows `GetAdaptersInfo`；Apple `sysctl`(en0)；**Linux/Android** 走 `ioctl(SIOCGIFHWADDR)` 尝试 `eth0/ens33/enp0s3`。
- **坑**：Android 上这些接口名基本不存在且无权限，几乎必然失败 → 回落到 `"00:00:00:00:00:00"` 的 MD5（所有设备相同）。并且项目**根本没用它**（见第 5 节）。

### 2.9 `T3Verify` 主类（744–1247）
- `init`（Base64 模式）/ `initRSA`（RSA 模式）：保存四个基础调用码 + appkey，创建 `CustomBase64` 或 `RSACrypto`，置 `encodeType_`、`initialized_`。
- `checkInit`（784）：未初始化抛异常。
- `buildUrl`（788）：服务器地址 + 调用码拼 URL。
- `encodeValue`（793）：按模式选 `encoder_->encodeToHex` 或 `rsaCrypto_->encryptToHex`。
- `decodeResponse`（798）：**清洗响应**——去掉第一行（首个 `\n` 前）、删 `\r\n`、**删末尾一个 `'0'`**（807，硬编码 hack），再按模式 `decode` / `decryptFromBase64`。
- `encodeParams`（813–832）——**核心防篡改/签名方案**：
  1. 每个参数值只加密一次，保持原顺序；
  2. 用已加密值拼成 `k1=v1&k2=v2...` 再追加 `"&"+appkey_`；
  3. 对该串 `md5` 得 `sValue`，加密后作为参数 `s` 附加到末尾。
- `httpPost`（834）：把参数拼成 `k=v&...` 表单 POST。
- `login`（844–911）——最复杂：
  - 参数 `kami/imei/t(now)`；发送后 `decodeResponse`；`jsonGetInt("code")` 必须 200。
  - **时间戳防重放**：`|now - resp.time| > 5` 秒直接失败（879–882）。
  - **响应验签**：本地按 `md5(id + appkey + sOriginal + end_time + localtime("%Y%m%d%H%M"))` 生成 `expectedToken`，与服务器 `token` 忽略大小写比较（884–895）。
  - 通过后写入 `statecode_/endTime_` 及各业务字段。
- `simpleRequest`（942–962）：通用请求模板（附 `t` 时间戳→`encodeParams`→POST→解码→查 `code==200`→取 `msg`），被绝大多数业务方法复用。
- `setCode`（922）：用一张 `map<string,string*>` 把 22 个扩展调用码（query/register/user_login/…/app_sign）绑定到成员字段，运行时按 field 名设置。
- 其余业务方法（均较薄，多数一行转发 `simpleRequest`）：
  - 卡密：`queryKami`(966)、`heartbeat`(916)；
  - 内容：`getNotice`/`getLatestVersion`/`checkUpdate`(区分 code 200 有更新 / 201 无更新)/`getCloudDoc`/`appSign`；
  - 用户体系：`userRegister/userLogin/userHeartbeat/qqLogin/bindQQ/changePassword/userCancel/recharge`；
  - 设备安全：`unbindKami/unbindUser/ipUnbindKami/ipUnbindUser/disableKami/disableUser`；
  - 远程变量：`getVariableByKami/ByUser`、`modifyVariableByKami/ByUser`；
  - 核心数据：`modifyCoreByKami/ByUser`、`getCoreByKami/ByUser`；
  - 在线人数：`getOnlineKamiCount/getOnlineUserCount`（`atoi(msg)`）。

---

## 3. 关键数据结构 / 全局变量 / 枚举 / 常量 / 偏移

- **宏/常量**：`T3_SERVER_URL "https://w.t3yanzheng.com/"`（h:17）、`BN_MAX 72`（cpp:222）、MD5 移位常量、标准字母表 `STD_B64`/`STANDARD_CHARSET`。
- **模式标志**：`encodeType_`（0=CustomBase64，1=RSA）——本项目实际用 RSA 模式。
- **结果结构体**（h:21–109）：`T3Result / T3LoginResult / T3NoticeResult / T3VersionResult / T3QueryResult / T3UpdateResult / T3VariableResult / T3CloudDocResult / T3CoreResult / T3OnlineResult / T3AppSignResult`，统一 `success/error` + 各业务字段。
- **`T3Verify` 私有状态**：`serverUrl_`、26 个 `*Code_` 调用码、`appkey_`、`statecode_`、`endTime_`、`encoder_`、`rsaCrypto_`。
- **本组内无任何内存偏移/绝对地址**（这与 il2cpp 无关，进一步佐证前提有误）。
- **外部硬编码常量**（在 `jni/res/全局状态.cpp` 中，供本 SDK 使用，见第 6 节）。

---

## 4. 用到的关键技术与调用链

- **libcurl + TLS**：HTTPS POST，内嵌 CA 证书 blob（`<curl/证书.h>` 的 `cacert_data`/`cacert_data_len`），`SSL_VERIFYPEER/HOST` 开启。libcurl/ssl/crypto 来自 `res/music/curl` 的预编译静态库（Android.mk 里 `musiccurl/musicssl/musiccrypto`），与音乐播放模块共用。
- **纯 C++ 密码学（不用 OpenSSL API）**：自实现 MD5、大数 RSA（`n^e mod n` 模幂）、PKCS#1 v1.5 填充、手写 DER/PEM 解析。注意：虽然链接了 OpenSSL 静态库（给音乐 curl 用），t3sdk 自身**刻意不调 OpenSSL**。
- **字母表置换式 Base64 混淆** + **MD5 签名（s 参数）** + **时间戳防重放（±5s）** + **响应 token 验签**，组成一套轻量防篡改协议。
- **手写极简 JSON 解析**（基于字符串查找）。
- **并发**：`std::once_flag`（curl init、`T3_InitOnce`）、`std::thread` + `detach`（登录/心跳/公告异步）、`std::atomic`/`std::mutex`（状态同步，在调用方模块）。
- **未使用**：hook / Dobby / JNI / il2cpp / OpenGL / miniaudio —— 这些都在其它模块，t3sdk 内部完全没有。

典型调用链（授权门禁）：
`DrawT3AuthUI()`（ImGui 界面）→ `T3_InitOnce()`→`T3Verify::initRSA()` → 用户点"验证"→ `T3_LoginAsync()`（子线程）→ `T3Verify::login(kami, machineCode)` →`encodeParams`(RSA+MD5签名)→`httpPost`/`httpPostRaw`(libcurl)→`decodeResponse`→ 时间戳/token 校验 → 成功后 `T3_StartHeartbeat()`（60s 循环 `heartbeat`）+ 保存卡密。

---

## 5. 与项目其它模块的依赖关系

**t3sdk 引用了谁**
- `<curl/curl.h>` 与 `<curl/证书.h>`（内嵌 CA 证书，位于 `jni/res/music/curl/include/curl/证书.h`，与音乐模块共用同一 curl）。
- 无项目内其它头依赖，自给自足。

**谁引用 t3sdk**（`grep` 命中 6 处）
- `jni/res/全局状态.h`（15 行 `#include "t3sdk.h"`）：声明全局对象 `T3Verify* g_t3_verify` 及一整套 `g_t3_*` 原子状态、`g_t3_machine_code`，以及 6 个基础常量 `T3_LOGIN_CODE / T3_NOTICE_CODE / T3_VERSION_CODE / T3_HEARTBEAT_CODE / T3_APPKEY / T3_RSA_PUBLIC_KEY`。
- `jni/res/全局状态.cpp`：**定义这些硬编码常量**并初始化全局状态；注释明说"只复用旧项目 t3sdk 底层网络验证"。
- `jni/res/卡密验证.cpp`：**唯一业务封装层**。`T3_InitOnce/T3_SetStatus/T3_LoginAsync/T3_StartHeartbeat/T3_TryAutoLoginFromConfig/DrawT3AuthUI`，把 t3sdk 接入 ImGui 登录界面、自动登录（读 AppConfig 保存的卡密）、后台心跳。
- `jni/Android.mk`：把 `t3sdk/t3sdk.cpp` 加入源文件、`t3sdk` 加入 include 路径，产物为共享库 `libhook.so`。

**关键点**：`login` 传入的设备码不是 `getMachineCode()` 的返回值，而是全局硬编码 `g_t3_machine_code = "android_inject_device"`（全局状态.cpp:48），即**设备绑定被写死成同一个假 IMEI**，实际等于关闭了单机绑定。

---

## 6. 值得注意 / 风险点

1. **非 ASCII 头文件名**：`#include <curl/证书.h>`（cpp:19，"证书"=证书）。用中文文件名 include，在部分工具链/编码环境下是移植性与编码地雷。
2. **硬编码机密全部可提取**（全局状态.cpp:18–29）：
   - `T3_APPKEY = "10d0b15afbcf1091ec4b846601f7bd8e"`
   - `T3_LOGIN_CODE=555F6E97AA87EACA`、`T3_NOTICE_CODE=0E3C22A329179374`、`T3_VERSION_CODE=CF78A3C231FBC7E2`、`T3_HEARTBEAT_CODE=87A6E643DAED8CFF`
   - 完整 RSA 公钥（1024-bit）。
   由于请求侧用的是**公钥**加密 + 公开 appkey + 公开调用码，任何拿到 `.so` 的人都能重放/伪造请求或**自建假服务器**绕过验证——整套防护本质是混淆，不是真正的机密性。
3. **`decrypt` 用公钥指数 `e`（cpp:465）**：语义是 RSA 公钥验签，只能还原服务器用私钥签的数据。命名为 decrypt 容易误导；同时意味着服务端下发数据的"加密"实为签名，机密性依赖字母表混淆。
4. **`decodeResponse` 删末尾一个 `'0'`（cpp:807）**：脆弱的硬编码约定，若服务器格式微调即失效。
5. **设备绑定形同虚设**：`g_t3_machine_code` 写死；且 `getMachineCode()` 在 Android 上基本拿不到真实 MAC（回落常量），二者叠加使 IMEI 维度防护失效。
6. **登录 token 依赖本地时钟与时区**：token 含 `localtime("%Y%m%d%H%M")`（分钟粒度）+ ±5s 时间戳窗口。**跨分钟边界**或设备时区/时间不准会误判 token 校验失败，健壮性差。
7. **BigNum 溢出静默截断**：超过 `BN_MAX=72` 字时 `len` 被夹紧（cpp:265/293/313 等），对超长模数会得错误结果，无告警；当前 1024-bit 密钥安全。
8. **TLS 为 CA 级校验**（`CAINFO_BLOB` 内嵌 CA bundle），非证书/公钥 pinning；能防普通 MITM，但不锁定具体服务器证书。
9. **线程与生命周期**：登录/心跳/公告都是 `detach` 的裸线程，读写 `g_t3_*` 靠 `atomic`+`g_t3_mutex`；`g_t3_verify` 为 `new` 出的裸指针、进程存续期不释放（可接受，但属资源不回收）。

---

### 相关文件路径（绝对）
- `/home/user/-/jni/t3sdk/t3sdk.h`
- `/home/user/-/jni/t3sdk/t3sdk.cpp`
- 调用方：`/home/user/-/jni/res/卡密验证.cpp`、`/home/user/-/jni/res/全局状态.h`、`/home/user/-/jni/res/全局状态.cpp`
- 内嵌 CA 证书：`/home/user/-/jni/res/music/curl/include/curl/证书.h`
- 构建：`/home/user/-/jni/Android.mk`

**一句话总结给负责人**：本组是"T3验证"发卡平台的 C++ 授权/卡密验证客户端（自带 MD5/RSA/Base64/JSON/libcurl，走 HTTPS + 签名 + token 验签 + 心跳），作为 ImGui 菜单的付费门禁——**与 il2cpp/内存读写/反射无关**，任务描述中"疑似 il2cpp/游戏引擎访问 SDK"的判断不成立；真正的引擎访问代码应在 `jni/App`、`jni/xdl`、`jni/Dobby` 等其它模块。


---

## 模块：`imgenie-widgets`

我已完整读取全部 4 个文件（ImGenie.cpp 1748 行分三次读取覆盖 1-1748；ImGenie.h 343 行；ImGenie捕获.cpp 102 行；ImGenie捕获.h 8 行），并核对了它们在项目中的引用关系。以下是通读记录。

---

# ImGenie 模块组 通读技术笔记

## 0. 一句话结论（先纠正一个前置误解）

**ImGenie 不是"自定义控件库"，它没有提供任何按钮/开关/滑条/卡片/Tab 等控件。** 它是一个第三方 MIT 开源库（作者 Stephane Cuillerdier / Aiekick，版本 0.1.0），功能是给 Dear ImGui 的**窗口**做 **macOS 风格 Genie（神灯吸入）开关动画 + 拖拽果冻抖动（Wobbly）效果**。本项目只写了其中的"捕获"胶水模块（中文文件名那两个），负责在 Android OpenGL ES 3 上把窗口内容渲染进 FBO 纹理供动画使用。

从"CTF/游戏逆向"视角看，**这组文件是纯 UI 视觉特效，与 hook / il2cpp / 内存读写 / 游戏截屏都无关**，没有可疑或攻击性代码。唯一叫"捕获"的模块只捕获 ImGui 菜单窗口自己的矢量绘制，**不捕获游戏画面**。

---

## 1. 每个文件的作用与规模

| 文件 | 行数 | 性质 | 作用 |
|---|---|---|---|
| `jni/res/ImGenie.h` | 343 | 第三方库头（MIT，Aiekick） | 公开 API：枚举、参数结构体、`ImGenieEffect`/`ImGenieContext`、C++ 命名空间 `ImGenie::` 和一套 `extern "C"` 的 C API |
| `jni/res/ImGenie.cpp` | 1748 | 第三方库实现 | 全部动画/物理/状态机/网格绘制/Demo 窗口/上下文管理的实现 |
| `jni/res/ImGenie捕获.h` | 8 | **本项目自写** | 声明两个捕获回调 `捕获_创建纹理` / `捕获_销毁纹理` |
| `jni/res/ImGenie捕获.cpp` | 102 | **本项目自写** | 用 GLES3 FBO 把 `ImDrawData` 离屏渲染成一张纹理（供 Genie 动画采样），及销毁纹理 |

`ImGenie.cpp` 里几乎所有内部函数都是 `static` 文件私有、以 `s_` 前缀命名；公开实现放在 `ImGenie::` 命名空间与文件末尾的 `IMGENIE_C_API` C 封装里。

---

## 2. 关键函数：签名 + 实现要点

### 2.1 `ImGenie捕获.cpp`（项目自写，最该关注）

**`ImTextureRef 捕获_创建纹理(int32_t width, int32_t height, ImDrawData* drawData)`**（第 12-95 行）
- 用途：作为 `CreateCaptureFunctor` 回调注入 ImGenie。把某个窗口的 `ImDrawData` 渲染进一张 RGBA8 纹理并返回 `ImTextureRef`。
- 实现步骤：
  1. 先用 `glGetIntegerv/glGetFloatv` **保存现场**：`GL_FRAMEBUFFER_BINDING`、`GL_TEXTURE_BINDING_2D`、`GL_COLOR_CLEAR_VALUE`、`GL_VIEWPORT`（第 15-20、68-69 行）。
  2. `glGenTextures` + `glTexImage2D(GL_RGBA, width, height, nullptr)`，线性过滤、`CLAMP_TO_EDGE`（第 22-29 行）。
  3. `glGenFramebuffers` + `glFramebufferTexture2D(GL_COLOR_ATTACHMENT0, texId)`，`glCheckFramebufferStatus` 校验；不完整时打 `__android_log_print(ANDROID_LOG_ERROR,"ImGenie",...)`、回滚现场、删除资源并返回空纹理（第 31-45 行）。
  4. **裁剪矩形扩张技巧**（第 47-66 行）：遍历所有 `ImDrawCmd`，先把原始 `ClipRect` 存进 `ImVector<ImVec4> original_clips`，再把贴到显示边缘（`<=0.5` 或 `>=DisplaySize-0.5`）的裁剪边界改写成 `±65536`。目的：离屏 FBO 的视口不等于主显示尺寸，若沿用原裁剪会把窗口边缘/阴影裁掉，扩张后保证整窗完整绘入纹理。
  5. `glViewport(0,0,w,h)` → 透明清屏 → `ImGui_ImplOpenGL3_RenderDrawData(drawData)`（第 71-74 行）——**复用 ImGui 官方 OpenGL3 后端**来画。
  6. 用 `original_clips` **原样恢复**每条 cmd 的 `ClipRect`（第 76-83 行）。
  7. 恢复 FBO/纹理/清屏色/视口，删 FBO（保留纹理，纹理 ID 通过 `ref._TexID = (ImTextureID)(intptr_t)texId` 返回）（第 85-94 行）。

**`void 捕获_销毁纹理(const ImTextureRef& tex)`**（第 98-102 行）
- `glDeleteTextures` 释放 `tex._TexID` 对应的纹理，作为 `DestroyCaptureFunctor` 回调。

> 关键点：该模块渲染的是 ImGenie 传进来的、**仅包含目标窗口及其子窗口 DrawList 的离屏 `ImDrawData`**，因此纹理内容是"这一个 ImGui 窗口"的截图，绝非游戏帧缓冲。

### 2.2 `ImGenie.cpp` — 网格/贝塞尔绘制辅助

- **`s_tg(v0,v1,percent)`**（第 20 行）：切线控制点，`(v1-v0)*(0,percent)+v0`，即 x 取 v0.x、y 沿 delta 插值。用于构造竖向 Coons S 形曲线的贝塞尔控制点。
- **`s_bezierCubicCalc(p1,p2,p3,p4,t)`**（第 25-32 行）：标量三次贝塞尔，内部用 `double` 计算避免大坐标精度损失。
- **`s_drawTexturedCoonsMeshPrimAnim(...)`**（第 53-187 行）：**整个库的核心网格绘制器**。以 4 角点 `p00/p10/p01/p11` 定义 Coons patch，四条边用三次贝塞尔 S 曲线，内部双线性混合。
  - `aStartAnimT/aEndAnimT` 指定主轴上可见网格范围（Genie 尾巴随动画收缩就是靠只生成这段行/列）。
  - `aHorizontal` 切换 Left/Right（主轴=列）与 Top/Bottom（主轴=行）两套代码路径。
  - `aAnimMode`：`Compress` 把 UV 重映射到可见范围 `[0..1]`（纹理跟着压缩），`Sliding` 让 UV 从纹理里滑过（纹理滚出去）。
  - `aFlipPrimaryUv`（Bottom/Right 用）、`aFlipV`（OpenGL Y 翻转）。
  - 用 ImGui 底层 `PushTexture/PrimReserve/PrimWriteVtx/PrimWriteIdx/PopTexture` 逐顶点、逐三角形写入前景 DrawList；竖直模式贝塞尔切线系数硬编码 `bezierTangentPercent = 0.4f`（第 136 行）。
- **`s_latticeDraw(...)`**（第 191-203 行）：对 `[0..1]` 全纹理调用上面的 Coons 网格；供 Wobbly 拖拽和 Slide-wobbly 用。
- **`s_drawTexturedQuad(...)`**（第 214-231 行）：`PrimQuadUV` 画单个带纹理四边形（Fade/Scale 用）。注意形参 `arCapturedSize` **未被使用**（多处如此，见风险点）。

### 2.3 `ImGenie.cpp` — 5 种过渡动画

全部缓动统一用 **smoothstep**：`eased = t*t*(3-2t)`。

- **`s_fadeAnimate`**（第 234-247 行）：按 `eased*255` 设置 tint alpha，画一张原位四边形。
- **`s_scaleAnimate`**（第 250-268 行）：以窗口中心为锚，半宽半高 `*eased`，从中心缩放。
- **`s_resolveSlideDir(source, dir, cornerRatio)`**（第 271-316 行）：把窗口在视口里的位置按每轴 3 段（起/中/止，段宽=`DisplaySize*ratio`）分类，映射到 8 方向/边角；`(1,1)` 居中时 fallback 选最近边。默认 `cornerRatio` 参数 0.15。
- **`s_slideAnimate`**（第 321-379 行）：整窗平移出屏。非 wobbly 时四角一起移；wobbly 时只"钉住"前导角（`pinned[]`按方向选），后随角靠 Coons S 曲线拉出橡皮筋拉伸感，`cellsH/cellsV` 来自 `spring`。
- **`s_pageCurlAnimate`**（第 384-515 行）：卷轴/翻页。按 8 种 `origin` 决定起始角与对角扫掠方向 `sweepDir`、扫掠长度 `sweepLen`；折线随 `foldDist=eased*sweepLen` 推进；折线之前的顶点保持原位，之后的顶点投影到折线上并 clamp 到窗口内 → 模拟卷起。
- **`s_autoDetectSide(source, target)`**（第 519-523 行）：按两中心点差的主导轴决定 Genie 吸入方向。
- **`s_latticeAnimate(...)`**（第 537-631 行）：**真正的 Genie 神灯动画**。网格分"尾边（停在源窗口）"与"收敛边（收窄到目标按钮）"。收敛边在前 30% 收到按钮宽度（`ratio = animT/0.3`），尾边在 25% 之后开始收缩（`startRatio=(animT-0.25)/0.75`）；`sourceRatio=sourceDim/totalDist` 决定源窗口占参数空间比例，最后交给 `s_drawTexturedCoonsMeshPrimAnim`。Top/Bottom/Left/Right 各一套角点与收敛边设置。

### 2.4 `ImGenie.cpp` — 弹簧物理（Wobbly）

- **`s_cornersFromRect`**（第 718-723 行）：取矩形 4 角 `[TL,TR,BL,BR]`。
- **`s_initSprings`**（第 728-744 行）：把 4 弹簧置于窗口当前角，按抓取点归一化坐标算**双线性权重** `springWeights`（越近的角权重越大→更硬→跟手更紧）。
- **`s_updateSpringsUniform`**（第 750-760 行）：统一劲度的欧拉积分弹簧（胡克力 - 阻尼），`substeps` 子步。
- **`s_updateSprings`**（第 763-779 行）：Wobbly 专用，每角劲度按 `springWeights` 在 `spring.stiffness`↔`maxStiffness` 间插值。

### 2.5 `ImGenie.cpp` — 捕获/隐藏窗口辅助

- **`s_addChildDrawLists`**（第 647-655 行）：DFS 收集子窗口 DrawList（父先于子，匹配 ImGui 渲染序）。
- **`s_captureWindow`**（第 659-677 行）：手工组装一个离屏 `ImDrawData`（设 `DisplayPos/DisplaySize/OwnerViewport/Textures`），塞入窗口及子窗口 DrawList，调用 `s_ctx->createCaptureFunc(w,h,&data)`（即项目的 `捕获_创建纹理`）。
- **`s_deleteEffectTexture`**（第 680-687 行）：调 `destroyCaptureFunc` 释放纹理。
- **`s_removeDrawListFromDrawData` / `s_removeChildDrawListsFromDrawData`**（第 691-713 行）：从主 `ImDrawData` 里删掉真实窗口的 DrawList（同步扣减 `TotalVtxCount/TotalIdxCount/CmdListsCount`），使后端不画真窗，只显示变形网格。

### 2.6 `ImGenie.cpp` — 上下文与主 API

- **`CreateContext/DestroyContext/Get/SetCurrentContext`**（第 783-824 行）：`IM_NEW/IM_DELETE`；销毁时遍历 `effects` 释放纹理。全局单例 `static ImGenieContext* s_ctx`（第 13 行）。
- **`SetCreateCaptureFunc/SetDestroyCaptureFunc/SetCaptureFlipV`**（第 826-842 行）：注入回调、设置 Y 翻转（OpenGL 置 true）。
- **`HasActiveEffects/IsEffectActive`**（第 800-812 行）：查 `effects` map（键 `ImHashStr(name)`）。
- **`Close/Open`**（第 814-822 行）：给无 `bool* p_open` 的窗口设置内部开关状态。
- **`bool Allow(name, p_open, params)`**（第 855-1331 行）：**核心状态机**，每帧无条件调用。
  - 无外部 bool 时用 `internalOpenStates`（默认开）。
  - 有活动 effect 时：每帧更新 `params/destRect`；支持**动画中途反转**（开关状态变了就 `animT=1-animT` 切 Animating↔AppearingAnimating）；按 8 个 `State` 分支推进——`PendingCapture`（保留一帧供捕获）、`Captured→Animating`、`Animating`（`animT += dt/animDuration`，按 transitionMode 分派到 PageCurl/Fade/Scale/Slide/Genie，画在 `GetForegroundDrawList()`，完成后删纹理、擦 map、置 `*p_open=false`）、`AppearingCapture/AppearingAnimating`（反向）、`MovingCapture/MovingActive/MovingSettle`。
  - `MovingActive`：`s_updateSprings` 后做**抓取点钉定校正**——用 `factor[i]=w[i]/Σw²` 把"目标抓取点-当前抓取点"的偏差按权重分摊回 4 角，保证光标处精确跟手（第 1187-1207 行）。检测 `GImGui->MovingWindow` 判断拖拽是否结束→转 `MovingSettle`（EaseOutQuad `1-(1-t)²` 回弹）。
  - 无活动 effect 时：用 `openStates` 比对上一帧检测**开→关**（建 `PendingCapture`）、**关→开**（建 `AppearingCapture`，并置 `HiddenFramesCannotSkipItems=1`）、以及 **拖拽开始**（两帧检测：先记 `dragStartPos`，下一帧 `pWin->Pos` 变了才确认真拖拽，过滤纯点击→建 `MovingCapture`）。Genie 吸入方向 `resolvedSide` 创建时解析一次、之后不变。
- **`Begin/End`**（第 1336-1356 行）：`Allow` + `ImGui::Begin/End` 封装。注意**只有 Begin 返回 true 才能调 End**（与原生 ImGui 不同）。
- **`Capture()`**（第 1364-1489 行）：**必须在 `ImGui::Render()` 之后、后端 `RenderDrawData()` 之前调用**。遍历 effects，对 `AppearingCapture/MovingCapture/PendingCapture` 做离屏捕获（`s_captureWindow`）并把真窗从主 DrawData 移除。含关键注释：`MovingCapture` 分支末尾必须 `continue`，否则同帧真窗+网格同显会闪一帧。
- **`ShowDemoWindow`**（第 1491-1681 行）：调参 Demo 窗口，`ImGui::Combo/SliderInt/SliderFloat/Checkbox` 暴露所有参数；Slide-Auto 时在视口四周画调试分区高亮（红=角、蓝=边、黄=当前解析结果），并列出所有活动 effect 的 State/AnimT/弹簧状态。
- **`DebugCheckVersion`**（第 1683-1702 行）：比对版本串与 `sizeof(ImGenieParams/Effect/Context)`，防 ABI 不一致（`IM_ASSERT`）。
- **C API**（第 1708-1748 行）：`ImGenie_*` 全套转发。`ImGenie_SetCreateCaptureFunc` 用 lambda 把 `void*`↔`ImTextureRef` 互转。

---

## 3. 关键数据结构 / 全局 / 枚举 / 常量

- 全局：`static ImGenieContext* s_ctx`（`ImGenie.cpp:13`，单例，**非线程安全**）。项目侧全局 `ImGenieParams g_GenieParams`（`全局状态.cpp:76`，`全局状态.h:80` extern）。
- 枚举（`ImGenie.h`）：`ImGenieSide`(Auto/Top/Bottom/Left/Right)、`ImGenieAnimMode`(Compress/Sliding)、`ImGenieTransitionMode`(None/Genie/PageCurl/Fade/Scale/Slide)、`ImGeniePageCurlOrigin`(8 角/边)、`ImGenieEffectMode`(None/Wobbly)、`ImGenieSlideDir`(Auto+8 向)。
- 参数结构体默认值：`GenieParams{cellsV=20,cellsH=1,side=Auto,animMode=Compress}`、`PageCurlParams{cellsH=30,cellsV=30,origin=BottomLeft}`、`SpringParams{stiffness=120,damping=8,substeps=8,cellsH=20,cellsV=20}`、`SlideParams{dir=Auto,autoCornerRatio=0.15,wobbly=false}`、`WobblyParams{maxStiffness=200,settleDuration=0.15}`、`Transitions{mode=Genie,animDuration=0.4}`、`Effects{mode=Wobbly}`。
- `ImGenieEffect`：8 态枚举 `State`、`sourceRect/destRect`、`capturedTex/capturedSize`、`animT`、`springs[4]{current,velocity}`、`springWeights[4]`、`grabUV`、`settleStart[4]`、`resolvedSide`、`params`。
- `ImGenieContext`：`effects/openStates/effectNames/dragStartPos/internalOpenStates`（均 `unordered_map<ImGuiID,...>`）、两个捕获 functor、`captureFlipV`。
- 硬编码常量：贝塞尔切线 `0.4f`（`:136`）；裁剪扩张 `±65536` 与 `0.5f` 边缘 epsilon（捕获模块 `:61-64`）；Genie 收敛 30%/尾边 25% 阈值（`:616-618`）；调试分区厚度 `t=10.0f`（`:1558`）；宏 `IMGENIE_VERSION "0.1.0"`、`IMGENIE_VERSION_NUM 00100`。
- 无内存偏移/游戏地址——**本组文件完全不涉及**。

---

## 4. 用到的关键技术与调用链

- **OpenGL ES 3 离屏渲染（FBO render-to-texture）**：仅在 `ImGenie捕获.cpp`，含 GL 状态完整保存/恢复与裁剪矩形扩张技巧。
- **ImGui 内部私有 API（`imgui_internal.h`）**：`ImDrawList::Prim*`/`PushTexture`、`GetForegroundDrawList`、`FindWindowByName`、`GImGui->MovingWindow/NavWindow/RootWindow`、`HiddenFramesCannotSkipItems`、`DebugNodeDrawCmdShowMeshAndBoundingBox`、`ImHashStr`。强耦合特定 ImGui 版本，用 `IMGENIE_CHECKVERSION` 防错配。
- **图形数学**：Coons patch + 三次贝塞尔网格变形、双线性混合。
- **物理仿真**：胡克弹簧+阻尼欧拉子步积分，权重化劲度做果冻抖动。
- **缓动**：smoothstep 与 EaseOutQuad。
- **复用 ImGui 官方后端**：`ImGui_ImplOpenGL3_RenderDrawData`（`imgui_impl_opengl3.h`）。
- **Android**：`android/log.h` 日志、`GLES3/gl3.h`。
- 调用链（每帧）：`imguijni.cpp` 初始化时 `ImGenie::CreateContext()` → `SetCreateCaptureFunc(捕获_创建纹理)` / `SetDestroyCaptureFunc(捕获_销毁纹理)` / `SetCaptureFlipV(true)`（`:110-113`）→ 帧内 `主界面绘制.cpp` 用 `ImGenie::Begin("另一个窗口",&g_show_another_window,...,&g_GenieParams)` / `ImGenie::End()`（`:326/354`，内部走 `Allow` 状态机）→ `ImGui::Render()` 后 `ImGenie::Capture()`（`imguijni.cpp:192`）离屏捕获并移除真窗 → 后端 `RenderDrawData` 只画前景变形网格。

---

## 5. 与项目其它模块的依赖关系

**被谁引用（谁依赖 ImGenie）：**
- `res/全局状态.h:14` `#include "res/ImGenie.h"`，`:80` `extern ImGenieParams g_GenieParams`；实体在 `全局状态.cpp:76`。
- `res/主界面绘制.cpp:13` include，`:326/354` 用 `ImGenie::Begin/End` 包裹"另一个窗口"。
- `App/imguijni.cpp:110-113,129,192` 负责创建/销毁上下文、注入捕获回调、每帧 `Capture()`，并在初始化时把 `g_GenieParams` 配成 Genie/0.4s/cellsV=20/cellsH=1/Compress。

**ImGenie 引用谁（依赖）：**
- `ImGenie.h` → `imgui_internal.h`（或 `IMGUI_INCLUDE`）、`<functional>`、`<unordered_map>`。
- `ImGenie.cpp` → `res/ImGenie.h`、`<cfloat>/<cmath>/<cstring>`。
- `ImGenie捕获.h` → `imgui.h`、`res/ImGenie.h`。
- `ImGenie捕获.cpp` → `ImGenie捕获.h`、`imgui_internal.h`、`imgui_impl_opengl3.h`、`GLES3/gl3.h`、`android/log.h`。
- 捕获模块与 ImGenie 之间通过 `CreateCaptureFunctor/DestroyCaptureFunctor` 回调解耦（ImGenie 不直接依赖 GL）。

---

## 6. 值得注意的点（技巧 / 坑 / 硬编码 / 风险）

1. **性质澄清（最重要）**：这是第三方 MIT 库（Aiekick/ImGenie 0.1.0），非本项目原创；提示词假设的"自定义按钮/开关/滑条/卡片/Tab 控件"在此**完全不存在**。它只做窗口开关/移动的视觉特效。
2. **"捕获"不是游戏截屏**：`捕获_创建纹理` 只渲染 ImGenie 传入的单个 ImGui 窗口的 `ImDrawData`，输出是菜单窗口自身的贴图；不读游戏帧缓冲，无逆向/取证含义。（对"CTF/逆向"关注点而言，这组文件是良性 UI eye-candy。）
3. **裁剪扩张 `±65536` 技巧**（捕获 `:61-64`）：为离屏视口正确绘制整窗，临时改写再恢复 `ClipRect`；该块无提前 return，正常路径能恢复，但若未来在中间加异常/早退会残留脏裁剪。
4. **GL 状态全保存/恢复**做得完整（FBO、纹理、清屏色、视口），但**未保存/恢复混合、深度、scissor** 等状态——依赖 `ImGui_ImplOpenGL3_RenderDrawData` 自身设置；若外部改了这些状态可能有隐患。
5. **`captureFlipV=true`**（`imguijni.cpp:113`）：OpenGL Y 轴翻转，配置正确，与 `s_drawTexturedCoonsMeshPrimAnim` 的 `aFlipV` 一致。
6. **死代码/未用形参**：`s_toRect`（`ImGenie.cpp:1736`）定义但从未调用；多处动画函数的 `arCapturedSize` 形参未使用（`s_drawTexturedQuad/s_fadeAnimate/...`）。
7. **`ImGenie_DefaultParams`**（`:1737`）返回**函数内 static 单例指针**——共享可变全局，非线程安全，多处修改会互相影响。
8. **非线程安全**：整库靠全局 `s_ctx`，无锁；Android 上须保证全部调用在渲染线程。
9. **强 ImGui 版本耦合**：大量使用 `imgui_internal.h` 私有结构（`GImGui`、`HiddenFramesCannotSkipItems`、`Prim*`），升级 ImGui 需重编并靠 `IMGENIE_CHECKVERSION` 校验 struct 尺寸，否则 ABI 崩。
10. **状态机时序坑（作者已在注释标注）**：`MovingCapture` 分支必须 `continue` 防一帧闪烁；`PendingCapture/AppearingCapture` 靠"多留一帧"才能捕获——这类隐性时序依赖若被误改容易出现真窗与网格同显、闪屏或纹理泄漏（每条错误路径都记得 `s_deleteEffectTexture`，目前泄漏防护是齐的）。
11. **本项目实际只对一个窗口用 Genie**（`主界面绘制.cpp:326` 的"另一个窗口"），Genie 参数 `cellsH=1, cellsV=20` 即竖向细条，是经典神灯观感。

---

相关文件（绝对路径）：
- `/home/user/-/jni/res/ImGenie.cpp`、`/home/user/-/jni/res/ImGenie.h`
- `/home/user/-/jni/res/ImGenie捕获.cpp`、`/home/user/-/jni/res/ImGenie捕获.h`
- 引用方：`/home/user/-/jni/App/imguijni.cpp`(110-113,129,192)、`/home/user/-/jni/res/主界面绘制.cpp`(13,326,354)、`/home/user/-/jni/res/全局状态.h`(14,80)、`/home/user/-/jni/res/全局状态.cpp`(76)


---

## 模块：`app-entry-build`

我已完整读取全部 6 个文件（每一行），并做了必要的跨模块依赖核对。以下是通读技术笔记。

---

# 通读技术笔记 — 程序入口与构建组（jni/App + 构建脚本）

## 0. 结论速览（先看这几条）

- **注入入口是 SO 构造函数** `__attribute__((constructor)) _init`（`Main.cpp:879`），它只做一件事：起一个 detached pthread 跑 `native_thread_func`。真正的初始化全在子线程里，靠"轮询等待"而不是固定 sleep 来等 JavaVM / 前台 Activity 就绪。
- **强目标绑定**：全项目硬编码了目标包名 `com.ztgame.bob`（腾讯/中手游《球球大作战 BOB》类），出现在初始化标志路径与日志路径。这不是通用菜单，是针对特定 App 的。
- **`dobby.h` 是"挂而未用"的死头文件**：全项目没有任何 `.cpp/.cc` include 它，`DobbyHook/DobbyInstrument/...` 无一处调用（grep 只命中 dobby.h 自身）。所谓"Dobby hook 框架"在本组代码里**没有实际启用**，仅保留了 API 声明和寄存器上下文结构。真正用到的"动态符号解析"走的是 **xdl**（`xdl_open/xdl_sym/xdl_dsym`），不是 Dobby。
- **渲染后端是 OpenGL ES 3.0（`#version 300 es`）**，不是 Vulkan；`imgui_impl_android` + `imgui_impl_opengl3` 双后端。含一条"防录屏"的 secure EGL surface 渲染分支。
- **日志默认全开且落盘到世界可读路径** `/storage/emulated/0/Android/data/com.ztgame.bob/ndk.log`（`log.h:22`）——对一个自称隐蔽的外挂菜单是明显 opsec 风险点。

---

## 1. 每个文件的作用与规模

| 文件 | 行数 | 作用 |
|---|---|---|
| `/home/user/-/jni/App/Main.cpp` | 887 | **注入引导 / 入口**。SO 加载后拿 JavaVM、找 Activity、释放内置 dex、`DexClassLoader` 加载 Java 侧 `ImGui` 类、提取自身 so、回调 Java 建立 ImGui 视图。 |
| `/home/user/-/jni/App/imguijni.cpp` | 275 | **ImGui 的 Android/OpenGL 渲染后端桥接（JNI 层）**。对应 Java 类 `GLES3JNIView` 和 `ImGui` 的 native 方法：init/resize/step/shutdown/触摸/音量键/窗口矩形。业务逻辑已外移到 `res/` 中文命名模块。 |
| `/home/user/-/jni/App/log.h` | 227 | **日志宏 + 文件落盘**。`LOGD/I/W/E/F`，双写 logcat 和文件，含 10MB 轮转。 |
| `/home/user/-/jni/App/dobby.h` | 239 | **Dobby hook 框架 API 声明**（未被引用）。含各架构 `RegisterContext`、`DobbyHook/Instrument/SymbolResolver/GOTReplace` 等原型。 |
| `/home/user/-/jni/Android.mk` | 77 | ndk-build 脚本。模块 `hook`（→ libhook.so）、源文件通配、链接库、4 个预编译静态库导入。 |
| `/home/user/-/jni/Application.mk` | 5 | 全局构建参数：`arm64-v8a` / `android-21` / `c++_static` / `release`。 |

---

## 2. 关键函数逐个说明

### 2.1 Main.cpp（注入链）

**`__attribute__((constructor)) void _init()`** — `Main.cpp:879`
SO 被加载即执行。`pthread_create(native_thread_func)` + `pthread_detach`。注释明确指出该构造函数会被调用两次（一次是注入器 dlopen，一次是 Java `System.load`），所以真正的去重在线程函数里做。

**`void* native_thread_func(void*)`** — `Main.cpp:786`
主初始化流水线，顺序严格：
1. `check_and_create_init_flag()` 去重，已初始化直接返回。
2. **循环 50 次 ×200ms** 调 `getJavaVMViaReflection()` 拿 JavaVM（作者注释：有些机型 so 构造执行过早，轮询比 sleep 稳）。
3. **循环 50 次 ×200ms** 调 `getCurrentActivityInstance()` 等前台 Activity；拿到后 `NewGlobalRef` 存 `g_ActivityInstance`。
4. `initMainThreadHandler()` 建主线程 Handler。
5. `loadImGuiClassFromDex()` 释放并加载 dex 里的 `com.example.imgui.ImGui`。
6. `extract_current_so(cacheDir+"/libimgui.so")` 把自身 so 拷到 cache。
7. `callImGuiSetupView()` 调 Java `setupImGuiViewOnMainThread(Activity)`。

**`bool check_and_create_init_flag()`** — `Main.cpp:34`
去重逻辑。**只用进程内环境变量** `IMGUI_INITIALIZED==1` 判定跳过（`getenv/setenv`）。作者特意注释：旧逻辑把 `/cache/.imgui_initialized` 文件当硬判断，会导致 App 下次启动因旧文件存在而"永远跳过初始化"——现在文件只写作**诊断标记**（内容 `INITIALIZED_20250208 pid=xxx`，`INIT_MARKER` 见 `Main.cpp:23`），不再作为拦截条件。写文件用了完整的 `write` 循环处理短写。

**`JNIEnv* getJNIEnv()`** — `Main.cpp:83`
`GetEnv(JNI_VERSION_1_6)`；若 `JNI_EDETACHED` 则 `AttachCurrentThread`。子线程访问 JNI 的标准套路。

**`bool getJavaVMViaReflection()`** — `Main.cpp:107`
名字叫 reflection 实际是**符号解析**：`xdl_open("libandroid_runtime.so")` 后依次找符号
`_ZN7android14AndroidRuntime9getJavaVMEv` / `JNI_GetCreatedJavaVMs` / `_Z20JNI_GetCreatedJavaVMs`，先 `xdl_sym` 再回退 `xdl_dsym`（调试符号）。命中后按 `jint(JavaVM**,jsize,jsize*)` 原型调用，取 `jvm_array[0]` 存入 `g_JavaVM`。用 xdl 而非 dlopen 是为了绕过 Android 对 `libandroid_runtime.so` 的 namespace 限制。

**`jobject getCurrentActivityInstance()`** — `Main.cpp:158`
纯反射爬栈拿当前 Activity，链路很典型：
`ActivityThread.currentActivityThread()` → 反射字段 `mActivities`（先试 `ArrayMap` 再退 `HashMap`）→ `values().toArray()` → 遍历每个 `ActivityClientRecord.activity` 字段 → `getClass().getName()`。
**优先匹配类名含 `UnityPlayerActivity`**（`Main.cpp:268`），否则记第一个作 fallback。注意：目标虽是 Unity 游戏（找 UnityPlayerActivity），但注入的却是 ImGui + DexClassLoader 那套。全程严格 `DeleteLocalRef` 释放引用，异常路径也清理。

**`bool initMainThreadHandler()`** — `Main.cpp:311`
`Looper.getMainLooper()` → `new Handler(Looper)`，存 `g_MainLooperHandler`（GlobalRef）。用于后续把工作 post 回主线程。

**`std::string releaseDexFile()`** — `Main.cpp:376`
通过 `Context.getCacheDir().getAbsolutePath()` 得到缓存目录，把内置数组 `classes_dex`（长度 `classes_dex_len`，来自 `classes_dex.h`）用带短写处理的 `write` 循环落地为 `<cache>/injected.dex`，`stat` 校验大小后返回路径。

**`jclass loadImGuiClassFromDex()`** — `Main.cpp:462`
1. `releaseDexFile()` 得 dex 路径；
2. `new DexClassLoader(dexPath, optimizedDir, "", parentClassLoader)`，parent 用 Activity 的 ClassLoader；GlobalRef 存 `g_ImGuiDexClassLoader`；
3. `loadClass("com.example.imgui.ImGui")`，GlobalRef 存 `g_ImGuiClassGlobal` 并返回。
每一步都有失败清理分支，引用管理很细。

**`std::string get_app_cache_dir()`** — `Main.cpp:609`
反射 `Context.getCacheDir().getAbsolutePath()`，返回缓存目录字符串（供提取 so 用）。

**`bool extract_current_so(const std::string& targetPath)`** — `Main.cpp:645`
**自定位 so 路径**：读 `/proc/self/maps`，逐行找 `.so` 且 `r-xp` 段，用 `sscanf` 解析地址区间，判断函数自身地址 `(void*)extract_current_so` 是否落在该段内 → 得到当前 so 的磁盘路径；再用 `ifstream/ofstream` + `rdbuf()` 整文件拷贝到 `<cache>/libimgui.so`。作用是把（可能是内存注入或临时路径的）自身 so 落一份到 App 私有目录。

**`bool callImGuiSetupView(jclass)`** — `Main.cpp:700`
`GetStaticMethodID("setupImGuiViewOnMainThread","(Landroid/app/Activity;)V")` 后 `CallStaticVoidMethod(g_ActivityInstance)`，带 `ExceptionCheck/Describe/Clear`。这是 native→Java 的关键回调，Java 侧据此在主线程创建 GLSurfaceView 并挂到 Activity。

**`extern "C" bool ImGui_SetSecureMode_FromMainDex(bool enable)`** — `Main.cpp:754`
反向桥：native 调 Java `ImGui.setSecureMode(Z)Z`。给防录屏功能用（native 想开/关安全模式时回调 Java 层设置 `SurfaceView.setSecure`）。是本文件里唯一导出的 C 接口，供其它模块（`res/防录屏`）链接调用。

### 2.2 imguijni.cpp（渲染后端桥接）

对应两个 Java 类：`com.example.imgui.ImGui` 与 `com.example.imgui.GLES3JNIView`。

**`Java_..._ImGui_isImGuiComponentTouched(env, clazz, x, y) -> jboolean`** — `imguijni.cpp:37`
Java 触摸分发时问 native：该坐标是否命中 ImGui 窗口。逻辑：`HasRegisteredTouchWindows()` 为假直接 `JNI_FALSE`（本帧无可交互窗口就把触摸让给游戏），否则 `AnyRegisteredWindowContains(x,y)`。这是"菜单吃触摸 / 游戏收触摸"的仲裁点。

**`Java_..._ImGui_nativeGetImGuiWindowBounds(env, clazz) -> jfloatArray`** — `imguijni.cpp:50`
返回长度 4 的边界数组。无登记窗口返回全 0 空矩形（避免 Java 把整屏当 ImGui 区域）；有窗口时以全屏为基，再 `MergeRegisteredWindowBounds()` 合并各交互窗口 bounds（注释说只带 48px 容差，防透明外圈吞触摸）。

**`Java_..._GLES3JNIView_init(env, cls, surface)`** — `imguijni.cpp:78`
渲染初始化，分两种情况：
- **已初始化 + 有 context**（Surface 重建）：`ANativeWindow_fromSurface` 取新窗口，Shutdown 旧 GL/Android 后端 → 释放旧 native window → 重新 `ImGui_ImplAndroid_Init` + `ImGui_ImplOpenGL3_Init("#version 300 es")`，即"重绑后端"。
- **首次**：`XmlConfigCompileProbe()` → `InitAppConfigOnce()` → `IMGUI_CHECKVERSION` → `ImGui::CreateContext` → **`ImGenie::CreateContext`**（过渡动画库）并注册捕获纹理回调、设置 genie 过渡参数（`transitionMode=Genie`, `animDuration=0.4`, `cellsV=20/cellsH=1`, `animMode=Compress`, flipV=true）→ `io.IniFilename=NULL`（禁 ini）→ 主题 `ApplyOldProjectTheme`（钳制 `g_current_theme_index` 到 `Theme_Classic..Theme_MusicUI`，默认 `Theme_LiquidGlass`）→ 建后端 → `FontGlobalScale=g_ui_font_scale` → `LoadVolumeDex()` + `SetVolumeScaleEnabled` → **加载字体**：`io.Fonts->AddFontFromMemoryTTF(font_data, font_size, 45.0f, ..., GetGlyphRangesChineseFull())`（字体来自 `uploaded_font.h` 的 fz.ttf，`FontDataOwnedByAtlas=false`）→ `g_Initialized=true`。

**`Java_..._GLES3JNIView_resize(env, obj, w, h)`** — `imguijni.cpp:159`
存 `screenWidth/Height`，`glViewport`，`io.DisplaySize`；若安全 surface 尺寸变化则 `DestroySecureEglSurface()`。

**`Java_..._GLES3JNIView_step(env, obj)`** — `imguijni.cpp:172`
**每帧渲染循环核心**：
```
补 g_ime_jvm / CaptureCurrentClassLoader / InitAppConfigOnce(懒)
未初始化或无 context → return
ImGui_ImplOpenGL3_NewFrame(); ImGui_ImplAndroid_NewFrame();
触摸_更新ImGuiIO(io); ImGui::NewFrame(); 触摸_帧后处理(io);
DrawFloatingWindow();            // 主界面绘制
ImeUpdateByImGui();              // 输入法桥接
触摸_清除点击事件();
ImGui::Render(); ImGenie::Capture();
drawData = GetDrawData();
处理 g_pending_secure_switch（切安全模式，失败回退）
if (g_secure_skip_screenshot):
    普通层只 glClear 透明，UI 渲染到 secure EGL surface（RenderDrawDataToSecureSurface）
    失败 → 回退普通 ImGui_ImplOpenGL3_RenderDrawData
else:
    glViewport + glClearColor(0,0,0,0) 透明 + ImGui_ImplOpenGL3_RenderDrawData
```
透明清屏（alpha=0）是叠加式覆盖层的标准做法；防录屏路径把 UI 画到独立 secure surface 让截屏/录屏抓不到。

**`Java_..._GLES3JNIView_imgui_1Shutdown(env, obj)`** — `imguijni.cpp:227`
Surface detach：关键盘、`DestroySecureEglSurface`、Shutdown 两个后端、释放 native window，**但刻意保留 ImGui context 存活**（注释明说），以便下次快速重绑。

**`Java_..._GLES3JNIView_MotionEventClick(env, obj, pointerId, down, PosX, PosY)`** — `imguijni.cpp:245`
把 Java 触摸事件喂给 `TouchHandlePointer(pointerId, down, ImVec2)`。

**`Java_..._GLES3JNIView_VolumeKeyEvent(env, cls, direction)`** — `imguijni.cpp:253`
音量键 → `ApplyVolumeKeyUiScaleDelta(direction)`（用音量键缩放 UI）。

**`Java_..._GLES3JNIView_getWindowRect(env, thiz) -> jstring`** — `imguijni.cpp:259`
返回 `x|y|w|h` 字符串；窗口折叠时用 `TitleBarRect()`，否则用 `Pos/Size`。供 Java 层知道菜单窗口位置。

**`Java_..._GLES3JNIView_real(env, obj, w, h)`** — `imguijni.cpp:272`
只更新 `screenWidth/Height`（真实分辨率上报）。

> 注意 JNI 签名的 static/instance 混用：`ImGui.*` 和 `GLES3JNIView.init/VolumeKeyEvent` 是 static（第二参 `jclass`），`resize/step/shutdown/MotionEventClick/getWindowRect/real` 是 instance（`jobject`）。每个入口都反复 `env->GetJavaVM(&g_ime_jvm)` 补 JVM 和 `CaptureCurrentClassLoader`，是为输入法/后续反射兜底。

### 2.3 log.h

- 宏 `LOGD/LOGI/LOGW/LOGE/LOGF`（`log.h:134-177`）：每条都 `__android_log_print` + 若 `LOG_TO_FILE` 则 `snprintf` 到 1024 栈 buffer 再 `writeToLogFile`。
- `writeToLogFile()`（`log.h:80`）：函数内 `static` 的 `ofstream/mutex/initDone`，首次打开时 `createDirectory` 建目录、**>10MB 轮转**（rename 为 `.old`），追加模式打开，每行 `[时间][级别][TAG] msg` 且 `flush()`。`try/catch(...)` 吞异常。
- `createDirectory()`（`log.h:27`）逐级 `mkdir(0755)`；`getCurrentTime()` 用 `put_time`；`getLogLevelName()` 映射级别。
- `LogUtils` 命名空间（`log.h:188`）：`flushLog/getLogFileSize/cleanupLogFile` 等工具，`setLogFilePath` 是空壳（注释坦白只是示例）。

### 2.4 dobby.h（声明层，未启用）

- 顶部 `#if 0` 的"混淆接口"把 `DobbyHook` 等 `#define` 成 MD5 样的乱名——用于发布时改名混淆，当前关闭。
- 各架构 `RegisterContext`（ARM64 有 `x0..x28`、`fp/lr/sp`、`q0..q31` FPReg 联合；另有 arm32/x86/x64 版本）。
- API 原型：`DobbyHook(addr, replace, &origin)`、`DobbyInstrument(addr, dbi_call)`（DBI 插桩，注明 ARM64 默认访问不到 q8-q31）、`DobbyDestroy`、`DobbySymbolResolver`、`DobbyGlobalOffsetTableReplace`、`dobby_enable/disable_near_branch_trampoline`、`dobby_register_image_load_callback`。
- `MemoryOperationError`、`HookEntryInfo`、`RetStatus` 等枚举/结构。
- **再次强调：无人 include，无人调用。** 作为"预留 hook 能力"存在，本组不构成实际 hook 链。

---

## 3. 关键数据结构 / 全局变量 / 常量 / 偏移

**Main.cpp 全局（`Main.cpp:71-77`）**
- `JavaVM* g_JavaVM`、`jobject g_ActivityInstance`、`jobject g_MainLooperHandler`、`jobject g_ImGuiDexClassLoader`、`jclass g_ImGuiClassGlobal`（均 GlobalRef）。
- `pthread_mutex_t g_InitMutex`（声明了但本文件实际未 lock 使用——去重改用 env var）。

**常量/硬编码**
- `INIT_FLAG_FILE = /data/user/0/com.ztgame.bob/cache/.imgui_initialized`（`Main.cpp:22`）
- `INIT_MARKER = "INITIALIZED_20250208"`（`Main.cpp:23`）
- 环境变量键 `IMGUI_INITIALIZED`
- Java 类名 `com.example.imgui.ImGui` / 方法 `setupImGuiViewOnMainThread(Activity)V`、`setSecureMode(Z)Z`
- 目标 Activity 关键字 `UnityPlayerActivity`
- 提取目标 so 名 `libimgui.so`、释放 dex 名 `injected.dex`
- xdl 符号：`_ZN7android14AndroidRuntime9getJavaVMEv` / `JNI_GetCreatedJavaVMs`

**log.h 常量**：`NDK_LOG=true`、`LOG_TO_FILE=true`、`LOG_FILE_PATH=/storage/emulated/0/Android/data/com.ztgame.bob/ndk.log`、`TAG="NDK"`、轮转阈值 10MB。

**imguijni.cpp 引用的外部全局（定义在 `res/全局状态.h`，已核对）**：`screenWidth/screenHeight`、`g_Initialized`、`g_imgui_backend_active`、`ANativeWindow* g_native_window`、`ImGuiWindow* g_window`、`JavaVM* g_ime_jvm`、`EGLSurface g_secure_egl_surface`、`g_ui_font_scale`、`g_current_theme_index`、`g_app_config_ready`；另有 `g_pending_secure_switch/g_pending_secure_target/g_secure_skip_screenshot/g_rendering_secure_surface`、`g_secure_surface_width/height` 等安全模式状态。ImGenie 过渡参数结构 `g_GenieParams`。

**外部数组**：`classes_dex` / `classes_dex_len`（`App/classes_dex.h`，真实 dex `App/classes.dex` 10644 字节）；`font_data`/`font_size`（`App/uploaded_font.h`，约 6.5MB）。另有 `App/zt_ttf.h`(13MB)、`App/ime_dex_data.h`(78KB) 存在但本组未直接引用。

主题枚举取值区间：`Theme_Classic`（下界）..`Theme_MusicUI`（上界），默认 `Theme_LiquidGlass`（液态玻璃）。

---

## 4. 用到的关键技术与调用链

- **SO 注入自启动**：`constructor` → detached pthread → 轮询等 JVM/Activity。不依赖 `JNI_OnLoad`（因为是被注入，不是被 App 正常 `System.loadLibrary`）。
- **xdl（非标准 dlopen）动态符号解析**：绕过 `libandroid_runtime.so` 的 linker namespace 拿 `JNI_GetCreatedJavaVMs`。这是本组真正的"底层技术"，替代了 Dobby 的 `DobbySymbolResolver`。
- **JNI 反射全家桶**：ActivityThread 爬 Activity、Looper/Handler、Context.getCacheDir、DexClassLoader 动态加载、Class.getClassLoader/loadClass。
- **DexClassLoader 动态加载内置 dex**：native 里嵌 dex（`classes_dex.h`），运行时落盘 + 加载 `com.example.imgui.ImGui`，实现 native↔Java 双向调用（Java 建 GLSurfaceView，native 回调 setup/setSecureMode）。
- **ImGui + OpenGL ES 3.0 渲染**：`imgui_impl_android` 收触摸/生命周期，`imgui_impl_opengl3`（`#version 300 es`）出图，透明覆盖层叠在游戏之上。
- **ImGenie**：第三方"精灵/genie 过渡动画"库，为窗口开合做压缩变形动画，带纹理捕获回调。
- **防录屏（secure surface）**：独立 EGL secure surface 渲染 UI + 回调 Java `setSecureMode`，普通层只清透明，使系统截屏/录屏抓不到菜单。
- **curl + OpenSSL（音乐/网络）**：构建链接 `libcurl.a/libssl.a/libcrypto.a`，`res/music/` 模块 + `nlohmann/json`，用于在线音频（`res/音频标签页.h`）。本组 cpp 未直接调，但构建组已把它们编进 libhook.so。
- **卡密验证**（`res/卡密验证.h`）：典型外挂授权，本组仅经 imguijni include 引入。
- **调用链主干**：`_init` → `native_thread_func` →（xdl 拿 JVM）→（反射拿 Activity）→（DexClassLoader 加载 ImGui 类）→ Java `setupImGuiViewOnMainThread` → Java 创建 `GLES3JNIView` → 回调 native `GLES3JNIView_init/step/...` → ImGui 每帧绘制。

---

## 5. 与其它模块的依赖关系

**Main.cpp**
- 依赖：`log.h`、`xdl/include/xdl.h`（xdl 静态源在 `xdl/src/*.c`，由 Android.mk 编入）、`App/classes_dex.h`。
- 导出：`ImGui_SetSecureMode_FromMainDex(bool)`（C 接口），被 `res/防录屏` 一类模块回调 Java。
- 与 Java：调 dex 内 `com.example.imgui.ImGui` 的 `setupImGuiViewOnMainThread` / `setSecureMode`。

**imguijni.cpp**
- 大量依赖 `res/` 中文模块头（`全局状态/触摸窗口/配置初始化/主题样式/触摸输入/输入法桥接/防录屏/音量键缩放/卡密验证/音频标签页/主界面绘制/ImGenie/ImGenie捕获/Config`）——它只是 JNI 薄壳，真正业务在 res。
- 全局变量定义在 `res/全局状态.h`；secure surface 函数实现在 `res/防录屏.cpp`（已核对 `DestroySecureEglSurface/RenderDrawDataToSecureSurface/RebindNativeWindowForSecure` 均在此）。
- 依赖 ImGui 头与预编译 `libimgui.a`（`ImGui/imgui/arm64-v8a/`）。
- 被 Java `GLES3JNIView`/`ImGui` 类通过 JNI 反向调用。

**log.h**：被几乎所有 cpp include（`dobby.h` 也 `#include <log.h>`）。

**dobby.h**：孤立，无被引用者。

---

## 6. 构建配置（Android.mk / Application.mk）

**Application.mk**：`APP_ABI=arm64-v8a`（仅 64 位）、`APP_PLATFORM=android-21`、`APP_STL=c++_static`、`APP_OPTIM=release`。

**Android.mk（模块 `hook` → libhook.so，共享库）**
- 编译标志：C 用 `-Wno-error=format-security -fvisibility=hidden -fexceptions`；C++ 追加 `-std=c++17 -Wno-error=c++11-narrowing -fpermissive -Wall -fexceptions`。`-fvisibility=hidden` 隐藏符号（隐蔽），但注入用的构造函数不受影响。
- include 路径：`App`、`xdl/include`、`ImGui` 及 `ImGui/imgui`、`t3sdk`、`res`、`res/music`（含 `nlohmann`、`curl/include`）、`thirdparty/pugixml`。
- 源文件（wildcard 收集，`filter-out %.bak`）：`xdl/src/*.c`；`App/*.{cpp,cc,c}`；`res/*.cpp`；`res/music/*.cpp`；显式 `t3sdk/t3sdk.cpp`、`thirdparty/pugixml/pugixml.cpp`；ImGui 核心 `ImGui/*.cpp` + backends `imgui_impl_android.cpp` / `imgui_impl_opengl3.cpp`。
- 链接系统库：`-llog -landroid -lEGL -lGLESv1_CM -lGLESv2 -lGLESv3 -lm -ldl -lz`（EGL + GLES 全版本 + zlib）。
- 静态库：`LOCAL_STATIC_LIBRARIES := imgui musiccurl musicssl musiccrypto`，对应 4 个 `PREBUILT_STATIC_LIBRARY`：`ImGui/imgui/arm64-v8a/libimgui.a`、`res/music/curl/{libcurl,libssl,libcrypto}.a`。
- **注意 t3sdk**：`t3sdk/t3sdk.cpp` 被编入。t3(Thunder/T3) 常见于网易/腾讯手游 SDK 相关命名，配合目标包 `com.ztgame.bob`，指向对特定商业游戏的逆向对接（值得单独看 t3sdk 模块）。

---

## 7. 值得注意 / 风险 / 坑

1. **Dobby 名不副实**：声明齐全但零调用，"hook 框架"在本组未落地；实际底层能力靠 xdl 符号解析。评审时别被 dobby.h 误导。
2. **强目标绑定 + 硬编码路径**：`com.ztgame.bob` 出现在 init 标志、日志路径；`UnityPlayerActivity` 关键字。换目标需改多处。
3. **日志 opsec 风险**：`LOG_TO_FILE=true` 默认全开，落盘到共享存储 `/storage/emulated/0/Android/data/com.ztgame.bob/ndk.log`（世界可读/易取证），且每条 `flush`。对隐蔽外挂是明显泄露面；发布版应关掉。
4. **去重逻辑演进痕迹**：作者从"文件硬判断"改为"进程内 env var"，并保留文件仅作诊断——注释里明说旧逻辑会"永远跳过初始化"的坑。`g_InitMutex` 声明了却没在 Main.cpp 里 lock，属残留。
5. **`extract_current_so` 靠 `/proc/self/maps` 自定位**：依赖 so 段可读、路径可读；若 so 从匿名内存映射（某些注入方式）加载，`ifstream(soPath)` 会失败——代码有返回 false 但不阻断主流程。
6. **50×200ms=最多 10s 的忙等**：JVM 与 Activity 各等最多 10 秒，慢启动机型可能超时失败（返回即放弃，无重试守护）。
7. **`_init` 被调用两次**是设计已知项（注入 dlopen + Java load），务必保持去重，否则重复建 context/线程。
8. **JNI static/instance 签名不一致**（第 2.2 末尾）：`ImGui.*` 与部分 `GLES3JNIView.*` 是 static，其余是 instance；Java 侧方法声明必须严格对应，否则 `RegisterNatives`/名称匹配失配。
9. **字体固定 45px + 全中文字形集**：`GetGlyphRangesChineseFull()` 会显著增大字体图集显存；`FontDataOwnedByAtlas=false` 要求 `font_data` 生命周期长于 atlas（来自静态头，OK）。
10. **异常处理表面化**：多处 `try/catch(const std::exception&)` 包 JNI 调用，但 JNI 异常并不会抛 C++ 异常，真正靠的是 `ExceptionCheck/Clear`；catch 基本是心理安慰，可能给人"已防护"的错觉。

—— 以上可逐条追溯到上面标注的文件行号。若要继续，建议下一步通读 `res/防录屏.cpp`（secure surface 全链）、`res/触摸窗口/触摸输入`（触摸仲裁）、`t3sdk/t3sdk.cpp`（游戏对接）与 Java 侧 dex 反编译（`com.example.imgui.ImGui` / `GLES3JNIView`）。


---

## 模块：`ui-theme`

读取完成。以下是这一组文件的详细中文技术通读记录。

---

# 通读记录：主界面绘制 / 主题样式 / imguiSimpleToast

> 覆盖文件：`jni/res/主界面绘制.cpp`、`jni/res/主界面绘制.h`、`jni/res/主题样式.cpp`、`jni/res/主题样式.h`、`jni/ImGui/imguiSimpleToast.hpp`。全部逐行读取完毕，并交叉核对了 `全局状态.h`、`触摸输入.cpp`、`游戏函数调用.h`、`App/imguijni.cpp` 中的相关声明与调用点。

---

## 1. 文件作用与代码规模

| 文件 | 行数 | 作用 |
|---|---|---|
| `主界面绘制.cpp` | 416 | 菜单主 UI。构建标签页布局、每个功能开关的控件，并在悬浮窗口每帧驱动内存修改功能的 tick 逻辑 |
| `主界面绘制.h` | 3 | 仅声明两个入口函数 `DrawOldMainUI()`、`DrawFloatingWindow()` |
| `主题样式.cpp` | 241 | 配色/样式实现。5 套主题（经典/纯净白/芭比粉/液态玻璃/音乐UI），设置圆角、边框、间距、颜色表 |
| `主题样式.h` | 3 | `#include "全局状态.h"`（取 `ThemeType` 枚举），声明 `ApplyOldProjectTheme(int)` |
| `imguiSimpleToast.hpp` | 441 | 独立的 ImGui toast 通知库（前景层绘制）。**注意：全项目未被任何文件 include，属未接入的死代码** |

---

## 2. 关键函数逐个说明

### 2.1 `主界面绘制.cpp` — 匿名命名空间内的三个 helper（行 19–79）

**`SliderFloatWithInput(label, v, v_min, v_max, format, popupId)`（行 23–47）**
在标准 `ImGui::SliderFloat` 之上叠加"双击滑块弹输入框精确改值"。实现要点：
- 行 26：判定 `IsItemHovered(AllowWhenBlockedByActiveItem)` 且 `IsMouseDoubleClicked(0)` → `OpenPopup(popupId)`。
- 行 29–45：弹窗内用 `InputFloat("##input", ...)` 输入，行 36–37 手动把值 clamp 回 `[v_min, v_max]`（因为 InputFloat 不受滑块范围约束，这是关键补丁）。
- `popupId` 必须唯一（注释明确要求），调用处分别用 `"popup_view_scale"`、`"popup_merge_value"` 区分。

**`DrawMemoryMoveButton(label, x, y, z, size)`（行 49–58）**
方向摇杆按钮。要点在注释（行 53）：只在 `IsItemActivated()`（按下瞬间）调用一次 `GameCall_InvokeMove(x,y,z,msg)`，**松手不归零**，让游戏按当前方向自然继续。这是"拨一下摇杆"而非"持续推杆"的设计。

**`DrawMemoryMoveButtons()`（行 60–77）**
四方向十字布局。按钮尺寸 `180×58`，用 `ItemSpacing.x` 算出居中缩进 `indent = (btnSize.x+gap)*0.5f`，通过 `Indent/Unindent` 让"上""下"居中、"左""右"用 `SameLine` 同排。方向向量：上 `(0,1,0)`、左 `(-1,0,0)`、右 `(1,0,0)`、下 `(0,-1,0)`。

### 2.2 `DrawOldMainUI()`（行 81–266）— 菜单主体，标签页清单

开头（行 83–88）在 `g_t3_mutex` 保护下取出公告 `g_t3_notice` 与卡密到期 `g_t3_end_time` 的副本，避免持锁绘制。标签页用 `BeginMainTabItem(label, index)`（实现在 `触摸输入.cpp:248`，见 §5）。

**Tab「说明」index=0（行 92–101）**：`TextWrapped` 显示公告（空则"暂无公告"）；「访问网盘」按钮 → `OpenUrlByActivity("https://share.feijipan.com/s/uGQ1k9ij")`（**硬编码飞机盘链接**）。

**Tab「内存」index=1（行 104–182）** — 核心功能区：
| 控件 | 绑定变量/函数 | 说明 |
|---|---|---|
| 「调用分身」按钮 | `GameCall_InvokeDevide` | 触发游戏分身 |
| 「调用吐球」按钮 | `GameCall_InvokeSpitBall` | 触发单次吐球 |
| 吐球加速 开启 | `g_spitball_accelerate` | 复选框（实际每帧执行在 DrawFloatingWindow） |
| 每帧次数 滑块 | `g_spitball_speed` (1–20) | "%d 次/帧" |
| 三角合球按钮 | `GameCall_TriangleMerge` | 按 `g_triangle_running` 切换文案"停止三角/三角合球" |
| 视野修改 开启 | `g_view_enabled` | 复选框 |
| 视野倍率 | `g_view_scale` (1.0–5.0) | 双击可精确输入 |
| 粘合修改 开启 | `g_merge_enabled` | 复选框 |
| 粘合值 | `g_merge_value` (0.1–1.5) | 双击可精确输入 |
| 排名名字修改 开启 | `g_rename_enabled` | 关闭时立即 `GameCall_RestoreAllNames` 恢复原名并置 `g_rename_was_enabled=false`（行 147–154） |
| 详细排名 | `g_rename_detailed_rank` | true=完整数字，false=千/万粗略 |
| 显示自我 | `g_rename_show_self` | 勾选变化当下立即跑一轮 `GameCall_RenameToRank`（行 157–163） |
| 名字上色 | `g_rename_use_color` | 勾选后出现 `ColorEdit3(g_rename_name_color)`（NoInputs+DisplayHex） |
| 名字大小（独立） 开启 | `g_name_scale_enabled` | 勾选后出现系数滑块 `g_name_scale` (0.5–6.0) |
| 方向摇杆 | `DrawMemoryMoveButtons()` | 十字方向键 |

**Tab「设置」index=2（行 185–224）**：
- 主题下拉 `Combo("##th", &g_current_theme_index, {"经典色","纯净白","芭比粉","液态玻璃","音乐UI"}, 5)` → 变化即 `ApplyOldProjectTheme` + `SaveAppConfigNow`。行 187 有越界保护，非法值回落到 `Theme_LiquidGlass`。
- UI 大小 `SliderFloat(&g_ui_font_scale, 0.5–1.0)` → 直接写 `ImGui::GetIO().FontGlobalScale` 并保存。
- 「无权限防录屏」`g_secure_skip_screenshot`：调 `SetJavaSecureSurfaceMode`，成功则挂起 `g_pending_secure_switch/g_pending_secure_target`；失败回滚复选框状态（行 201–208）。
- 「音量键调UI大小」`g_volume_key_ui_scale` → `SetVolumeScaleEnabled` + 保存。
- 卡密到期时间：`g_t3_authed && !endTime.empty()` 时绿色显示到期时间，否则橙色"未激活"。

**Tab「音频播放」index=4（行 227–230）**：仅调 `DrawUploadedUIInsideAudioTab()`（音频标签页模块）。

**Tab「日志」index=3（行 233–262）**：弹出日志窗口开关 `g_show_log_window`；清空 `ClearLog()`；保存到 `g_config_base_dir + "/game_log.txt"`（`fopen/fputs(g_log_text)/fclose`，失败写错误日志）；6 类过滤复选框 `log_flags[6]`（系统/按键/用户/按钮/错误/其他，**注意这些 flag 仅 UI 存在，未真正用于过滤 `TextUnformatted(g_log_text)` 的输出**）；输入框 `g_logInput` + 「发送」按钮（**按钮无回调，是空壳**）；子窗口滚动显示全量 `g_log_text`。

> 注意标签页的 index 与显示顺序不一致：显示顺序是 说明(0)→内存(1)→设置(2)→音频播放(4)→日志(3)，即"音频播放"的 index=4 排在 index=3 的"日志"之前。index 用于 `g_main_tab_index` 持久化选中态，与排列顺序解耦。

### 2.3 `DrawFloatingWindow()`（行 268–416）— 窗口生命周期 + 每帧驱动

每帧调用（`imguijni.cpp:187`）。流程：
1. **触摸窗口重登记（行 269–276）**：先 `ClearTouchWindows()`，`g_window=nullptr`，再设 `WindowMinSize(58, FrameHeight)`。注释说明：折叠状态残留矩形会导致 Java 层继续拦截整块触摸，所以每帧清空、仅 Begin 后未折叠时重登记。
2. **卡密门禁（行 278）**：`if (!g_t3_authed)` → 画登录窗（标题 **"句号的辅助"**，788×420），内部 `DrawT3AuthUI()`；否则画主窗（标题 **"测试"**，800×600），内部 `DrawOldMainUI()`。两者都 `RegisterTouchWindow(g_window)`。
3. **详细日志窗口（行 305–319）**：`g_show_log_window` 时弹出，内容基本是空壳（"数量: %d" 恒为 0，空 child）。
4. **另一个窗口（行 321–356）**：`g_show_another_window` 时用 `ImGenie::Begin` 画，内嵌 `ImCoolBar` Dock 栏（6 个按钮"要天天开心呀"），演示/彩蛋性质。CoolBar 配置：normal 40 / hovered 60、embed 模式、平滑 80/100ms、effect_strength 0.5、frame_rounding 6。
5. **每帧 tick 执行内存功能**（关键：UI 只设 flag，真正每帧生效在这里）：
   - 吐球加速（行 359–362）：开启且 speed>0 → 每帧 `GameCall_InvokeSpitBallDirect(g_spitball_speed, msg)`。
   - 视野（行 365–372）：开启且 `scale>1.0` 每帧 `GameCall_SetViewScale`；关闭时借 `g_view_was_enabled` 只 `GameCall_ResetViewPullup()` 一次。
   - 粘合（行 375–382）：同上模式，`GameCall_SetMerge` / `GameCall_ResetMerge`。
   - 名字大小（行 386–397）：开启每帧 `GameCall_TickNameScale(g_name_scale)`；关闭时先传 `1.875f`（游戏公式默认系数）再传 `0.f`（约定为 restore 模式，强制 `NameText.localScale=1`）。**`1.875` 是硬编码的游戏公式尾值**。
   - 排名改名（行 400–414）：开启时用 `static last_rename_time` 每 **4.0 秒**周期性 `GameCall_RenameToRank`；关闭沿的兜底路径 `GameCall_RestoreAllNames`（覆盖非 checkbox 关闭路径）。

### 2.4 `主题样式.cpp` — 三个函数

**`ApplyLiquidGlassTheme()`（行 5–70）**：默认主题。基于 `StyleColorsDark`，大圆角（Window 22 / Frame 14 / Tab 15）、有边框（Window 1.4 / Child 1.2）、大量**低 alpha / 全透明背景**（`WindowBg.a=0`, `ChildBg.a=0`, `PopupBg.a=0.18`）营造"液态玻璃"透明感，青蓝色高亮配色。`WindowMinSize=(58,24)`（注释：刚好容纳左上角折叠按钮）。

**`ApplyMusicUITheme()`（行 73–138）**：移植自用户上传包"布局.cpp / YouXiStyle"。深蓝底不透明配色，蓝色强调，圆角较小（Window 10 / Frame 8）、`ScrollbarSize=18`。注释强调**只移植样式，不 CreateContext、不重复加载字体**，以免破坏现有输入/字体/生命周期。

**`ApplyOldProjectTheme(int themeIndex)`（行 140–240）**：主题分发器。行 142 越界保护回落 `Theme_LiquidGlass`。`switch(ThemeType)`：
- `Theme_Classic` → `StyleColorsDark()`
- `Theme_Light` → `StyleColorsLight()` + 手改近 30 个 `Colors[...]`（浅灰白 + 蓝色强调，行 150–187）
- `Theme_Pink` → 基于 `StyleColorsDark()` 但整套改成粉色系（行 191–228，实为亮色粉）
- `Theme_LiquidGlass` / `Theme_MusicUI` → 转调上面两个函数

> **字体加载：本组文件不做字体加载**。仅设置 `FontGlobalScale`（在 `主界面绘制.cpp:196`）。字体上传/加载逻辑在别处（`ApplyMusicUITheme` 注释明确表示刻意不碰字体）。

### 2.5 `imguiSimpleToast.hpp` — Toast 通知库（v2.1.0）

单头文件、单例、前景层绘制。三大部分：
- **配置宏（行 16–31）**：位置边距 50、间距 10、淡入/淡出各 200ms、默认时长 3000ms、透明度 0.9、宽 350、最小高 60、圆角 8、边框 2、`TOAST_MAX_DISPLAY 0`（0=无限）、图标 20、内边距 20。
- **`enum class ToastType`**{Info,Success,Warning,Error}、**`enum class ToastPosition`**{TopLeft,TopRight,BottomLeft,BottomRight}。
- **`class Toast`（行 47–127）**：存 type/title/message/duration/`std::chrono::steady_clock` 创建时间。
  - `GetAlpha()`（行 67–86）：三段式透明度——淡入线性到 0.9、显示恒 0.9、淡出线性到 0。
  - `IsExpired()`：`elapsed > duration + fadeOut`。
  - `GetColor()`：Success 绿 / Warning 黄 / Error 红 / Info 蓝。`GetIcon()`：✓ / ! / ✕ / i。`GetDefaultTitle()`：无标题时按类型返回"成功/警告/错误/信息"。
- **`class ToastManager`（行 129–409）**，单例 `Instance()`：
  - `Add/Info/Success/Warning/Error/Clear/SetPosition`。
  - 私有排版 helper：`CalculateTextHeight`、`DrawTextWrapped`、`DrawTextCentered`——**自己实现按 `wrapWidth` 逐字符测宽换行**（用 `font->CalcTextSizeA`），不依赖 ImGui 的自动换行。
  - `Render()`（行 278–408）：先 `remove_if` 清过期，取 `GetForegroundDrawList()` 直接画（背景圆角矩形 + 类型色边框 + 圆形图标底 + 图标字 + 居中标题 + 左对齐消息），按 `WorkPos/WorkSize` 依 position 堆叠，`yOffset` 累加实现多条纵向排列。
- **`namespace SimpleToast`（行 412–440）**：全局便捷 inline 包装（`SimpleToast::Success("...")` 等）。

---

## 3. 关键数据结构 / 全局变量 / 枚举 / 常量 / 偏移

- **枚举 `ThemeType`**（`全局状态.h:128`）：`Theme_Classic=0, Theme_Light=1, Theme_Pink=2, Theme_LiquidGlass=3, Theme_MusicUI=4`。默认/回落值均为 `Theme_LiquidGlass`。
- **本组涉及的全局状态变量**（均声明在 `全局状态.h`）：`g_current_theme_index`、`g_ui_font_scale`、`g_main_tab_index`、`g_spitball_accelerate/g_spitball_speed`、`g_triangle_running`、`g_view_enabled/g_view_scale/g_view_was_enabled`、`g_merge_enabled/g_merge_value/g_merge_was_enabled`、`g_rename_*`（enabled/was_enabled/use_color/name_color/detailed_rank/show_self）、`g_name_scale_enabled/g_name_scale`、`g_secure_skip_screenshot`、`g_volume_key_ui_scale`、`g_show_log_window/g_show_another_window`、`g_window`、`g_GenieParams`、`g_t3_authed/g_t3_notice/g_t3_end_time/g_t3_mutex`、`g_log_text[262144]`、`g_logInput[256]`、`g_config_base_dir`。
- **硬编码常量/魔数**：
  - 网盘 URL `https://share.feijipan.com/s/uGQ1k9ij`（行 97）。
  - 名字缩放游戏公式系数 `1.875f`（行 392），`0.f` 约定为 restore 模式。
  - 改名周期 `4.0` 秒（行 403）。
  - 窗口标题 `"句号的辅助"`（登录）、`"测试"`（主窗）。
  - `WindowMinSize (58,24)` / 折叠按钮容差。
  - Toast 全套配置宏。
- **本组文件没有直接的内存偏移/地址**；真正的 il2cpp 偏移封装在 `游戏函数调用` 模块（`GameCall_*` 背后），本组只是调用者。

---

## 4. 关键技术与调用链

- **Dear ImGui**：全部 UI 基于 ImGui 立即模式；TabBar/TabItem、Slider、Checkbox、ColorEdit3、Combo、Popup、Child、ForegroundDrawList。
- **第三方 ImGui 扩展**：`ImGenie`（自定义窗口）、`ImCoolBar`（macOS 风格 Dock 动画栏）。
- **JNI / Android 桥接**：`OpenUrlByActivity`（起 Activity 开浏览器）、`SetJavaSecureSurfaceMode`（防录屏 FLAG_SECURE）、`SetVolumeScaleEnabled`（音量键调 UI）、`SaveAppConfigNow`（配置持久化）。
- **il2cpp 游戏函数调用链**（UI → GameCall → il2cpp）：
  - 内存 tab / 每帧 tick → `GameCall_InvokeDevide / InvokeSpitBall / InvokeSpitBallDirect(count) / TriangleMerge / SetViewScale(f) / ResetViewPullup / SetMerge(f) / ResetMerge / RenameToRank / RestoreAllNames / TickNameScale(mul) / InvokeMove(x,y,z)`（签名见 `游戏函数调用.h`）。
  - 设计模式统一为"UI 只置 flag，`DrawFloatingWindow` 每帧 apply，关闭沿用 `*_was_enabled` 只 reset 一次"。
- **触摸输入**：`RegisterTouchWindow / ClearTouchWindows` 把 ImGui 窗口矩形登记给 Java 层做触摸命中；`BeginMainTabItem` 内用 `触摸_主指点击/触摸_副指点击` 支持多指切页。
- **卡密验证**：`g_t3_authed` 门禁整个主 UI，`DrawT3AuthUI` 为登录界面。
- **未使用的技术**：`imguiSimpleToast.hpp` 虽实现完整，但**未被任何 cpp include**（见 §5/§6），miniaudio/curl 等不在本组文件出现（音频在 `音频标签页` 模块）。

---

## 5. 与其它模块的依赖关系

**主界面绘制.cpp 引用（include）**：`全局状态.h`、`配置初始化.h`、`主题样式.h`、`触摸输入.h`、`触摸窗口.h`、`输入法桥接.h`、`防录屏.h`、`音量键缩放.h`、`卡密验证.h`、`音频标签页.h`、`游戏函数调用.h`、`res/ImGenie.h`、`res/ImCoolBar.h`、`imgui.h`。

**被谁引用**：
- `DrawFloatingWindow()` 被主渲染循环 `App/imguijni.cpp:187` 每帧调用。
- `DrawOldMainUI()` 仅被 `DrawFloatingWindow()` 内部调用（`主界面绘制.cpp:301`）。
- `ApplyOldProjectTheme()` 被 `App/imguijni.cpp:123`（启动时）和 `主界面绘制.cpp:190`（切主题时）调用；实现在 `主题样式.cpp`。
- `BeginMainTabItem` 定义在 `触摸输入.cpp:248`，声明在 `触摸输入.h:9`，被本文件 5 个 tab 使用。
- `ThemeType` 枚举定义在 `全局状态.h:128`，被 `主题样式.h`（间接）与两处越界保护使用。

**关键：`imguiSimpleToast.hpp` 是孤岛**——全项目搜索（含 .cpp/.h/.hpp）除其自身外无任何 `#include "imguiSimpleToast"` 或 `SimpleToast::` / `ToastManager::` 调用点。即该 toast 控件当前**未接入 UI，是预留/死代码**。

---

## 6. 值得注意的点（技巧 / 坑 / 硬编码 / 风险）

1. **Toast 库未接线**：`imguiSimpleToast.hpp` 完整但无人 include，不会被编译进任何 UI。若负责人以为菜单有 toast 提示——实际没有；要用需手动 include 并在渲染循环里加 `SimpleToast::Render()`。
2. **日志 tab 的过滤/发送是空壳**：`log_flags[6]` 复选框与「发送」按钮均无实际逻辑，`TextUnformatted(g_log_text)` 恒显示全量文本；"详细日志窗口"里"数量"恒为 0、child 为空。属未完成 UI。
3. **每帧调用游戏函数的性能/稳定性风险**：吐球加速每帧执行最多 20 次 `InvokeSpitBallDirect`、视野/粘合/名字缩放每帧调 il2cpp 方法。这些是热路径，若 `GameCall_*` 内部有查找/反射开销会掉帧；改名用 4 秒节流是刻意规避。
4. **透明背景主题的隐患**：`ApplyLiquidGlassTheme` 把 `WindowBg/ChildBg/PopupBg` alpha 设 0，视觉靠 Border 与内容。若在深色游戏画面上，弹窗内容可能与背景难分辨；PopupBg alpha 仅 0.18 也偏低。
5. **`WindowMinSize` 三处并存**：`DrawFloatingWindow` 每帧设 `(58, FrameHeight)`，而两个主题里写死 `(58,24)`；切主题会覆盖，但每帧又被 `DrawFloatingWindow` 覆写为 `FrameHeight`。以运行期每帧值为准，主题里的 24 实际不生效。
6. **硬编码外链与品牌**：飞机盘分享链接、窗口标题"句号的辅助"直接写死在源码，逆向/分发时易暴露作者信息。
7. **名字缩放 restore 的约定式魔数**：`GameCall_TickNameScale(0.f)` 用 0 表示 restore 模式、`1.875f` 表示默认系数，是隐式协议，调用方与实现方需严格对齐，否则会把玩家名字尺寸改乱。
8. **`SliderFloatWithInput` 的 clamp 时机**：clamp 在弹窗每帧执行（行 36–37），只要弹窗开着就持续钳制，输入超范围会被即时拉回，用户体验上"打不进去超范围值"是预期行为而非 bug。
9. **防录屏切换失败回滚**：行 201–208 对 `SetJavaSecureSurfaceMode` 失败做了复选框状态回滚（`g_secure_skip_screenshot = !...`），处理得当；成功路径改为挂起 `g_pending_secure_switch`，说明真正切换在别处异步执行。
10. **标签页 index 与顺序解耦**（音频 index=4 排在日志 index=3 前面），`g_main_tab_index` 存 index 用于持久化选中，调整显示顺序时不要改 index，否则记忆的选中页会错位。


---

## 模块：`touch-input`

I have everything needed. All six files read line-by-line, plus the cross-module call sites (JNI layer `imguijni.cpp`, UI `主界面绘制.cpp`), globals (`全局状态.h`), IME bridge helpers, and the embedded dex data. Here is the通读记录.

---

# 触摸/输入处理模块 通读技术笔记

范围：`jni/res/触摸输入.{cpp,h}`、`jni/res/触摸窗口.{cpp,h}`、`jni/res/音量键缩放.{cpp,h}`。这三组文件构成菜单的**触摸输入注入**、**悬浮窗触摸穿透判定**、**音量键交互**三块。下面所有结论均可追溯到具体函数名与行号。

---

## 0. 一句话总览与总调用链

- 游戏/宿主的 Java 层把 `MotionEvent` 拆成单指事件，通过 JNI `MotionEventClick` 逐指转发进来 → `TouchHandlePointer` 维护触摸槽位 → 每帧 `触摸_更新ImGuiIO` 把"主指"位置写成 ImGui 的鼠标位置+左键按下，实现**用触摸驱动 ImGui**；`触摸_帧后处理` 用"副指"实现多指拖窗；`触摸_主指点击/副指点击` 实现点按判定。
- `触摸窗口` 模块负责每帧登记当前可交互 ImGui 窗口的矩形，Java 层通过 `isImGuiComponentTouched` / `nativeGetImGuiWindowBounds` 查询"这块屏幕该不该被菜单拦截"，实现**触摸穿透**（菜单窗口外的区域把触摸放行给游戏）。
- `音量键缩放` 模块在运行时用 `InMemoryDexClassLoader` 动态加载一段**内嵌 dex**（`com.mxp.VolumeKeyHelper`），注册 native 回调，把音量键变成"调 UI 缩放"的快捷键；关闭该功能时回退到手动调系统音量避免按键被吃。

---

## 1. 各文件作用与代码规模

| 文件 | 行数 | 作用 |
|---|---|---|
| `触摸输入.cpp` | 261 | 触摸槽位管理、多指状态机、把触摸注入 ImGuiIO、点按/拖动判定、`BeginMainTabItem` 触摸友好的标签页封装 |
| `触摸输入.h` | 9 | 对外导出 7 个触摸接口函数声明 |
| `触摸窗口.cpp` | 83 | 登记可交互窗口、带 padding 的命中检测、合并所有窗口 bounds，供 Java 触摸穿透判断 |
| `触摸窗口.h` | 8 | 导出 5 个窗口登记/查询函数 |
| `音量键缩放.cpp` | 179 | 动态加载内嵌 dex、注册 native、音量键→UI 缩放、启停开关、系统音量回退 |
| `音量键缩放.h` | 5 | 导出 3 个音量键接口 |

配套数据：`jni/res/volume/volume_key_dex_data.h`（437 行，`volume_key_dex[]` 字节数组 + `volume_key_dex_len = 5160`，头部魔数 `64 65 78 0a 30 33 38 00` = `"dex\n038\0"`，即 DEX 版本 038）。

---

## 2. 关键数据结构、全局变量、常量

### 触摸输入.cpp 内部结构（`触摸输入.cpp:9-39`）
- `#define MAX_TOUCH_SLOTS 5`：最多同时跟踪 5 指。
- `#define TAP_MAX_DIST_PX 25.0f`：判定"点按"（而非拖动）的最大位移阈值（像素）。
- `struct TouchSlot { bool active; int pointer_id; ImVec2 pos; }`：一个触摸点。`pointer_id` 是 Java `MotionEvent` 的 pointerId。
- `struct TouchDragState { bool active; char window_name[64]; ImVec2 offset; }`：某槽位正在拖动的窗口名 + 手指相对窗口原点的偏移。用**窗口名字符串**而非指针来锁定被拖窗口。
- `struct SlotTapInfo { bool fired; ImVec2 pos; bool is_primary; ImVec2 start_pos; float max_dist_sq; bool tracking; }`：点按跟踪。`max_dist_sq` 记录本次按压期间的最大位移平方，用来区分 tap 与 drag。

### 全局变量
- `触摸输入.cpp:33-39`：静态数组 `g_touch_slots[5]`、`g_drag_states[5]`、`g_tap_infos[5]`（均 file-static）；`g_last_valid_pos`（static，记住松手前最后有效位置）。
- **对外（非 static）全局**：`g_primary_just_released`、`g_zero_mouse_delta_next`、`g_primary_slot_idx = -1`（`触摸输入.cpp:36-39`）。`g_primary_slot_idx` 是"主指"所在槽位。
- 项目共享全局（`全局状态.h:57-87`）：`g_ime_jvm`(JavaVM*)、`g_ime_activity`(jobject)、`g_ui_font_scale`(float)、`g_volume_key_ui_scale`(bool)、`g_volume_loader`(jobject，动态 dex 的 ClassLoader)、`g_volume_loaded`(bool)、`g_main_tab_index`(int，被 `BeginMainTabItem` 读写并持久化)。

### 触摸窗口.cpp（`触摸窗口.cpp:4-7`）
- `static std::vector<ImGuiWindow*> g_touch_windows`：本帧登记的可交互窗口列表。
- `static const float kTouchResizePadding = 48.0f`：窗口矩形外扩容差。注释明确记载：**旧值 160px 会把窗口外一圈透明区纳入拦截，导致宿主按钮/摇杆点不到**，故收窄到 48px。
- 折叠态额外常量 `collapsedPadding = 8.0f`（`触摸窗口.cpp:14`）：折叠后只保留标题栏 + 8px 给 ImGui，其余穿透。

### 音量键缩放.cpp 中的硬编码
- Java 类名硬编码 `"com.mxp.VolumeKeyHelper"`（`音量键缩放.cpp:93,141,170`）。
- 音量调节常量（`音量键缩放.cpp:42-45`）：`STREAM_MUSIC=3`、`ADJUST_RAISE=1`、`ADJUST_LOWER=-1`、`FLAG_SHOW_UI=1`。
- UI 缩放范围硬编码 `[0.50, 1.00]`，步进 `0.05`（`音量键缩放.cpp:77-79`）。

---

## 3. 关键函数逐个说明

### 3.1 触摸输入.cpp

**`TouchFindWindowAtPos(ImVec2 pos) -> ImGuiWindow*`（41-55）**
从 `GImGui->Windows` 逆序（顶层优先）找命中窗口。过滤掉未激活、`NoMove`、`ChildWindow`、`Tooltip` 窗口。折叠窗口只用 `TitleBarRect()` 判定，否则用 Pos+Size 全矩形。用于拖窗时确定拖哪个窗口。

**`TouchIsWindowDraggedByOther(int skip_slot, ImGuiWindow* win) -> bool`（57-65）**
遍历其它槽位的 `g_drag_states`，若有别的手指已在拖同名窗口则返回 true。防止两指同时拖同一窗口冲突。用**窗口名 strcmp**比较。

**`TouchIsWindowUsedByPrimary(ImGuiWindow* win) -> bool`（67-71）**
判断主指当前所压窗口是否就是 `win`（按 `ID` 比较）。副指起拖前会排除主指正在操作的窗口，避免主指点控件时副指误拖同一窗口。

**`TouchFindSlotByPointerId` / `TouchFindFreeSlot`（73-85）**
按 pointerId 找已有槽位 / 找空闲槽位，找不到返回 -1。

**`TouchStartTracking(i, pos, is_primary)`（87-95）** 与 **`TouchEndTracking(i)`（97-106）**
Start 初始化 tap 跟踪（`fired=false`、记 `start_pos`、`max_dist_sq=0`、`tracking=true`）。End 在松手时判定：若**期间最大位移平方 ≤ 25²** 且该槽位没进入拖动状态，则 `fired=true` 并把落点记为松手时的 `pos` —— 即一次"点按"成立。

**`TouchUpdateMoveDistance(i, pos)`（108-115）**
每次移动更新 `max_dist_sq = max(旧值, 距起点位移²)`。是 tap/drag 区分的核心累积量。

**`TouchHandlePointer(int pointer_id, bool down, ImVec2 pos)`（117-158）— 触摸注入总入口**
- down 且槽位已存在：只更新 pos + 累积位移（移动事件）。
- down 且是新指：占用空闲槽，`is_primary = (g_primary_slot_idx < 0)`（第一根按下的指即主指），设 `g_primary_slot_idx`，开始 tap 跟踪。槽满则丢弃。
- up：更新末位移 → `TouchEndTracking`（决定是否 fire tap）→ 释放槽位、清 drag。若松开的是主指：置 `g_primary_just_released=true`、`g_zero_mouse_delta_next=true`、`g_primary_slot_idx=-1`，并**把主指身份转交给下一个仍按住的槽位**（158 循环，继承 is_primary）。
- 由 JNI `Java_com_example_imgui_GLES3JNIView_MotionEventClick`（`imguijni.cpp:245-250`）逐指调用。

**`触摸_更新ImGuiIO(ImGuiIO& io)`（160-188）— 触摸→鼠标注入**
每帧在 `ImGui::NewFrame()` 前调用（`imguijni.cpp:182`）。取主指（无主指则退而取任意激活槽）：
- 刚松开主指这一帧：全部 `MouseDown=false`，`MousePos=g_last_valid_pos`（停在原地，不跳变），清标志。
- 有主指：`MouseDown[0]=true`（**关键：只要有指按住，ImGui 就一直认为左键按下**）、`MousePos=主指坐标`、同步 `g_last_valid_pos`。
- 无任何指：全 false，鼠标停在最后有效位置。
这就是"用触摸驱动 ImGui 悬停/点击/拖动"的机制。

**`触摸_帧后处理(ImGuiIO& io)`（190-221）— 副指多指拖窗**
在 `ImGui::NewFrame()` 后调用（`imguijni.cpp:184`）：
- 若 `g_zero_mouse_delta_next`：把 `io.MouseDelta` 清零（配合主指刚释放，防止 delta 突跳）。
- 遍历所有**非主指**激活槽：位移平方超过 `(TAP_MAX_DIST_PX*0.5)² = 12.5²` 才允许起拖（低于则可能是点按，先不拖）。命中窗口且不被主指占用、不被别的指拖 → 进入 drag，记窗口名和 offset。已在拖：`ImGui::SetWindowPos(window_name, slot->pos + offset, Always)`。
- **主指只负责点击/操控控件，副指负责搬窗口**——这是该菜单单/多指分工的核心设计。

**`触摸_清除点击事件()`（223-225）**：每帧末（`imguijni.cpp:190`）清所有 `fired`，使 tap 是"单帧脉冲"。

**`触摸_副指点击(rect_min, rect_max)`（227-236）/ `触摸_主指点击(...)`（238-246）**
在给定矩形内是否发生了一次副指/主指点按。副指版排除主指槽和 is_primary 的 tap；主指版只认 is_primary 的 tap。供控件做触摸命中。

**`BeginMainTabItem(const char* label, int index) -> bool`（248-259）**
封装 `ImGui::BeginTabItem`，当 `g_main_tab_index==index` 时用 `ImGuiTabItemFlags_SetSelected` 强制选中。命中判定同时接受**鼠标点击 ∪ 主指点按 ∪ 副指点按**（取标签的 ItemRect），命中即更新 `g_main_tab_index` 并 `SaveAppConfigNow()` 持久化。主界面 6 个 Tab（说明/内存/设置/音频播放/日志）都用它（`主界面绘制.cpp:92,104,185,227,233`）。

### 3.2 触摸窗口.cpp

**`GetTouchRectForWindow(window) -> ImRect`（9-25，static）**
折叠窗口：`TitleBarRect()` 四周外扩 8px。展开窗口：Pos/Size 四周外扩 `kTouchResizePadding=48px`（给拖动/缩放留连续手感）。

**`WindowContainsWithPadding(window, x, y) -> bool`（27-31）**：点是否落在上面的矩形内。

**`MergeWindowBounds(window, float* bounds[4], bool hasBounds) -> bool`（33-52）**
把窗口矩形并入 `bounds`（left/top/right/bottom = 索引 0/1/2/3）。`hasBounds=false` 时**直接覆盖**为该窗口矩形，之后逐窗取并集（min/min/max/max）。

**`ClearTouchWindows`（54-56）/ `RegisterTouchWindow`（58-64）**：每帧开头清空、`Begin` 后登记（去重）。由 `DrawFloatingWindow` 驱动：`主界面绘制.cpp:269` 清、`:286/299/311/328` 逐窗登记（"句号的辅助"、"测试"、"详细日志窗口"、"另一个窗口"）。

**`AnyRegisteredWindowContains(x, y)`（66-71）/ `MergeRegisteredWindowBounds(bounds)`（73-79）/ `HasRegisteredTouchWindows()`（81-83）**：供 JNI 查询。

**Java 触摸穿透调用链**：
- `Java_..._ImGui_isImGuiComponentTouched(x,y)`（`imguijni.cpp:36-44`）：无登记窗口→返回 `JNI_FALSE`（放行给游戏）；否则按命中返回。这样**主窗折叠/最小化/本帧无窗时游戏可正常触摸**。
- `Java_..._nativeGetImGuiWindowBounds()`（`imguijni.cpp:48-72`）：无窗返回空矩形；有窗则用 `MergeRegisteredWindowBounds` 返回并集 bounds 给 Java。

### 3.3 音量键缩放.cpp

**`VolumeGetClass(env, className) -> jclass`（10-21，static）**
用**动态 dex 的 ClassLoader `g_volume_loader`** 调 `ClassLoader.loadClass()` 加载类（因为 `VolumeKeyHelper` 不在宿主 APK 里，`FindClass` 找不到，必须走这个 loader）。

**`AdjustSystemVolumeByDirection(int direction) -> bool`（24-62，static）**
通过 `g_ime_activity.getSystemService("audio")` 拿 `AudioManager`，反射调 `adjustStreamVolume(STREAM_MUSIC, RAISE/LOWER, FLAG_SHOW_UI)`。全程细致 `DeleteLocalRef` + `ImeClearException` + 线程 Detach。用于"功能关闭时"手动补一次系统音量。

**`ApplyVolumeKeyUiScaleDelta(int direction)`（65-84）— 音量键主逻辑**
- `direction==0` 直接返回。
- **若 `g_volume_key_ui_scale` 关 或 无 ImGui 上下文**：调 `AdjustSystemVolumeByDirection` 走系统音量（避免 Java 已拦截却把按键"吃掉"）。
- 否则：`g_ui_font_scale ± 0.05`，clamp 到 `[0.50,1.00]`，变化则写 `io.FontGlobalScale` 并 `SaveAppConfigNow()`。**音量键 = 调菜单 UI 大小**（非切菜单/非缩放画面）。
- 两条 JNI 入口都会走到它：`Java_..._VolumeKeyEvent`（`imguijni.cpp:253-257`）和动态 dex 回调 `NativeVolumeKey`。

**`NativeVolumeKey(JNIEnv*, jclass, jint direction)`（86-89）**：`VolumeKeyHelper.nativeVolumeKey(I)` 的 native 实现，转调上面的函数。

**`RegisterVolumeNatives(env) -> bool`（91-102）**：对动态加载的 `com.mxp.VolumeKeyHelper` 调 `RegisterNatives` 绑 `{"nativeVolumeKey","(I)V", NativeVolumeKey}`。

**`LoadVolumeDex() -> bool`（104-161）— 内存 dex 注入，核心技术点**
幂等（`g_volume_loaded && g_volume_loader` 直接 true）。流程：
1. 取 `g_ime_activity.getClassLoader()` 作 parent；
2. `NewByteArray` 装入内嵌 `volume_key_dex`（5160 字节）→ `ByteBuffer.wrap`；
3. `new dalvik.system.InMemoryDexClassLoader(ByteBuffer, parent)`（**API 26+ 特性，无落地文件的内存 dex**）→ `NewGlobalRef` 存 `g_volume_loader`；
4. `RegisterVolumeNatives` 绑 native；
5. 反射调静态 `VolumeKeyHelper.init(Activity)` 完成 Java 侧音量键 hook；
6. 全程严格释放局部引用、Detach 线程；成功置 `g_volume_loaded`。
在 `GLES3JNIView_init` 里首次初始化时调用（`imguijni.cpp:141`）。

**`SetVolumeScaleEnabled(bool enable) -> bool`（163-179）**
确保 dex 已加载，反射调静态 `VolumeKeyHelper.setEnabled(Z)` 启停 Java 侧音量键拦截。初始化时按配置调一次（`imguijni.cpp:142`），设置页勾选框实时调用（`主界面绘制.cpp:210-212`）。

---

## 4. 用到的关键技术与调用链

- **JNI 双向桥接**：C→Java 全用反射（`FindClass`/`GetMethodID`/`Call*Method`），Java→C 用 `RegisterNatives` + 静态导出的 `Java_com_example_imgui_*`。
- **内存 dex 注入 / 运行时代码植入**：`InMemoryDexClassLoader` 加载 APK 之外的 `com.mxp.VolumeKeyHelper`（dex 以字节数组内嵌进 so）。对 CTF/逆向对抗而言，这是"辅助逻辑不落地、不进 APK 清单、难静态发现"的典型手法。
- **反射抓 Activity/ClassLoader**：`g_ime_activity` 来自输入法桥接（`输入法桥接.cpp` 通过 `ActivityThread.currentActivityThread().mActivities` 遍历取当前 Activity），音量模块直接复用。`g_ime_jvm` 由各 JNI 入口 `env->GetJavaVM` 惰性填充。
- **OpenGL/ImGui 后端**：触摸模块本身不碰 GL，但注入点严格卡在 `ImGui_ImplOpenGL3/Android_NewFrame` → `触摸_更新ImGuiIO` → `NewFrame` → `触摸_帧后处理` → `DrawFloatingWindow` → `Render`（`imguijni.cpp:179-192`）。
- **系统音量反射**：`AudioManager.adjustStreamVolume`。
- 未使用 miniaudio/curl/il2cpp/inline-hook（这三组文件里无相关代码；音频/网络在别的模块）。

**完整触摸帧调用链**：
`MotionEvent(Java)` → `GLES3JNIView.MotionEventClick(JNI)` → `TouchHandlePointer` 更新槽位 → 下一帧 `触摸_更新ImGuiIO`(注入鼠标) → `NewFrame` → `触摸_帧后处理`(副指拖窗) → `DrawFloatingWindow`(登记窗口 + `BeginMainTabItem` 消费 tap) → `触摸_清除点击事件` → `Render`。
穿透查询链：`Java 触摸预处理` → `isImGuiComponentTouched`/`nativeGetImGuiWindowBounds` → `触摸窗口` 模块。

---

## 5. 模块依赖关系

**触摸输入.cpp 引用**：`全局状态.h`(g_main_tab_index)、`配置初始化.h`(SaveAppConfigNow)、`imgui_internal.h`(GImGui/ImGuiWindow)。**被引用**：`imguijni.cpp`（4 个帧函数 + MotionEventClick）、`主界面绘制.cpp`（BeginMainTabItem）。

**触摸窗口.cpp 引用**：仅 `触摸窗口.h`(imgui_internal.h) + `<vector>`，**零外部耦合**。**被引用**：`imguijni.cpp`（穿透查询 3 函数）、`主界面绘制.cpp`（Clear/RegisterTouchWindow）。

**音量键缩放.cpp 引用**：`全局状态.h`、`配置初始化.h`、`输入法桥接.h`(ImeGetEnv/ImeEnsureActivity/ImeClearException/g_ime_jvm/g_ime_activity)、`res/volume/volume_key_dex_data.h`。**被引用**：`imguijni.cpp`（init 时 LoadVolumeDex/SetVolumeScaleEnabled、VolumeKeyEvent→ApplyVolumeKeyUiScaleDelta）、`主界面绘制.cpp`（勾选框 SetVolumeScaleEnabled）。**强依赖输入法桥接模块复用同一套 JNIEnv/Activity 获取逻辑**。

---

## 6. 值得注意的点（技巧、坑、硬编码、风险）

1. **`nativeGetImGuiWindowBounds` 里的全屏初始化是死代码**（`imguijni.cpp:60-63` + `触摸窗口.cpp:40-45`）：`windowBounds` 先填 `{0,0,screenW,screenH}`，但 `MergeWindowBounds` 首个窗口 `hasBounds=false` 会**直接覆盖**，且该分支仅在"有登记窗口"时执行，所以全屏值永远被第一个窗口矩形冲掉。无功能危害，但误导阅读，建议删除或改为真正并集。
2. **拖窗用窗口名字符串而非指针/ID**（`TouchDragState.window_name`）：`SetWindowPos(name,...)` 依赖窗口名全局唯一。项目窗口名硬编码为中文（"测试""句号的辅助""详细日志窗口""另一个窗口"），一旦有重名或运行时改名会拖错窗。`window_name[64]` + `strncpy` 截断安全，但超 63 字节的窗口名会静默截断导致匹配失败。
3. **单指=控件、多指=拖窗**的分工较隐蔽：主指恒定注入 `MouseDown[0]=true`，所以任何单指按住都会让 ImGui 持续处于"左键按下"，控件的 hover/held 逻辑要能容忍这一点；副指才触发窗口拖动。
4. **主指释放的 delta 清零**（`g_zero_mouse_delta_next`）是防止松手瞬间 `MousePos` 回到 `g_last_valid_pos` 造成 ImGui 拖拽跳变的补丁，属易踩坑点。
5. **`kTouchResizePadding` 从 160→48 的历史坑**已在注释中记录：外扩过大→窗口透明外圈吞掉宿主触摸；这类"穿透容差"是外挂菜单的通病，值得关注。
6. **音量键关闭态的手动补音量**（`ApplyVolumeKeyUiScaleDelta:71-74`）：因为 Java 侧 `VolumeKeyHelper` 一旦 hook 就可能拦下按键，关闭功能时必须主动 `adjustStreamVolume` 把音量还给系统，否则用户会觉得音量键"坏了"。设计上要注意 Java 的 `setEnabled(false)` 与这里的回退可能重复调音量。
7. **内嵌 dex + `InMemoryDexClassLoader`** 是本组最"敏感/可疑"的技术：辅助类 `com.mxp.VolumeKeyHelper` 以 5160 字节裸 dex 硬编码在 so 中、运行时无落地注入。逆向/查杀角度这是刻意规避静态分析的信号，也是 CTF 场景里最值得复现的点。`init(Activity)` 具体如何 hook 音量键（dispatchKeyEvent 或 view 层）需进一步反编译该 dex 才能确认。
8. **API 版本硬门槛**：`InMemoryDexClassLoader` 要求 Android 8.0(API 26)+，低版本会静默失败（返回 false），`LoadVolumeDex` 无降级路径。
9. **线程/引用管理**：音量模块每次都 `ImeGetEnv(&attached)` 并在末尾按 `attached` Detach，且逐个 `DeleteLocalRef` + `ImeClearException`，JNI 卫生做得较严谨，未见明显泄漏；唯一 `NewGlobalRef`（`g_volume_loader`）无对应 `DeleteGlobalRef`，属长生命周期常驻，符合预期但需知晓。


---

## 模块：`ime-bridge`

我已完整读取两个目标文件的每一行，并追踪了它们的全部依赖与调用方。以下是通读记录。

---

# 输入法桥接模块 通读技术笔记

## 0. 模块定位（一句话）

`输入法桥接` 是这个 Android ImGui 外挂菜单的 **Java 互操作核心层**。它解决一个根本矛盾：ImGui 是纯 C++ 自绘 UI，本身没有系统输入法能力；而承载渲染的 native 回调（`GLES3JNIView.step` / `MotionEventClick`）是 **static native**，JNI 第二个参数不是 View 对象，所以拿不到 `Context`。本模块用 **ActivityThread 反射兜底拿 Activity**，再 **内存加载一个内嵌 dex（com.mxp.Helper）** 来调起系统软键盘，并把用户键入的字符/按键 **回传给 ImGui 的 IO**。此外它还顺带承载了"用系统浏览器打开 URL""捕获宿主 ClassLoader"等 JNI 杂项。

---

## 1. 本组文件的作用与代码规模

| 文件 | 行数 | 作用 |
|---|---|---|
| `/home/user/-/jni/res/输入法桥接.h` | 12 | 对外声明 8 个函数（`ImeClearException`/`ImeGetEnv`/`ImeEnsureActivity`/`ImeLoadDex`/`ImeShowKeyboard`/`ImeUpdateByImGui`/`MainDexLoad`/`CaptureCurrentClassLoader`/`GetClassFromCurrentLoader`/`OpenUrlByActivity`）。注意：`ImeGetClass`、`ImeFindActivityByActivityThread`、`ImeRegisterNatives`、`MainDexGetClass`、两个 native 回调都**未进头文件**，是 .cpp 内部符号。 |
| `/home/user/-/jni/res/输入法桥接.cpp` | 434 | 全部实现。含 16 个函数。 |

强依赖的两个"数据文件"（不属于本组但被本组 `#include`）：

- `/home/user/-/jni/App/ime_dex_data.h`（1082 行，`imgui_dex[]`，**12948 字节** dex，magic `dex\n038`）→ 内含 Java 类 `com.mxp.Helper`（键盘桥）与 `com.mxp.GLES3JNIView`。
- `/home/user/-/jni/App/classes_dex.h`（893 行，`classes_dex[]`，**10644 字节** dex，magic `dex\n037`）→ 内含 `com.example.imgui.GLES3JNIView` / `ImGui`（宿主注入用的 View/生命周期 dex）。

---

## 2. 关键函数逐个说明（签名 + 实现要点，可追溯到行号）

### 2.1 JNI 环境与异常基础设施

**`bool ImeClearException(JNIEnv* env)`**（L9–16）
`ExceptionCheck()` 有挂起异常就 `ExceptionClear()` 并返回 true。全文件几乎每次 JNI 调用后都要点一次——**统一"吞异常"策略**：不打印、不上报，只保证 JNI 线程不带着挂起异常继续跑（否则下一个 JNI 调用会 abort）。

**`JNIEnv* ImeGetEnv(bool* attached = nullptr)`**（L18–29）
从全局 `g_ime_jvm` 取 `JNIEnv`。先 `GetEnv(JNI_VERSION_1_6)`；若返回 `JNI_EDETACHED`（当前线程没附着到 JVM，典型是渲染线程/工作线程）则 `AttachCurrentThread` 并把 `*attached=true` 回传，让调用方**自己负责 Detach**。这是本模块所有 JNI 函数的入口套路：`ImeGetEnv(&attached)` … 结尾 `if (attached) g_ime_jvm->DetachCurrentThread();`。

### 2.2 类加载

**`jclass ImeGetClass(JNIEnv*, const char* className)`**（L31–45）
**不用** `FindClass`（那会走系统 ClassLoader，找不到内嵌 dex 里的类），而是拿到 `g_ime_loader`（内存 dex 的 `InMemoryDexClassLoader` 全局引用），反射调用其 `ClassLoader.loadClass(String)`。注意传的是 **点号全名**（`"com.mxp.Helper"`），符合 `loadClass` 约定（`FindClass` 才要斜杠）。每个局部引用都 `DeleteLocalRef`。

**`jclass MainDexGetClass(JNIEnv*, const char*)`**（L175–189）与上者逐字节同构，只是改用 `g_main_dex_loader`。**未被任何地方调用，且未进头文件——死代码。**

**`bool CaptureCurrentClassLoader(JNIEnv* env, jclass cls)`**（L258–273）
一次性捕获**宿主 App 的 ClassLoader**：对传入的 `cls` 调 `Class.getClassLoader()`，存入全局 `g_current_imgui_class_loader`（`NewGlobalRef`）。有幂等守卫：已捕获则直接返回 true。被 `imguijni.cpp` 的每个 native 入口调用（见 §5）。

**`jclass GetClassFromCurrentLoader(JNIEnv* env, const char* className)`**（L275–286）
用 `g_current_imgui_class_loader.loadClass()` 加载宿主 dex 里的类（如 `com.example.imgui.ImGui`）。给 `防录屏.cpp` 用来拿宿主的 `ImGui` 类做防截屏（`FLAG_SECURE`）。

### 2.3 Activity 发现（本模块最核心的反射技巧）

**`jobject ImeFindActivityByActivityThread(JNIEnv* env)`**（L47–127）
因为 native 回调是 static、拿不到 View→Context，改用 **`ActivityThread` 私有反射**取当前前台 Activity：
1. `ActivityThread.currentActivityThread()`（静态）拿到主线程 AT 实例。
2. 反射字段 `mActivities`——**先按 `Landroid/util/ArrayMap;` 取，失败再退 `Ljava/util/HashMap;`**（兼容不同 Android 版本）。
3. `map.values().toArray()` 得到 `ActivityClientRecord[]`。
4. 遍历每条 record，读 `paused`(Z) 和 `activity`(Landroid/app/Activity;)。**优先选 `!paused` 的（前台可见的那个），选中即 `NewGlobalRef` 并 break**；否则把第一个非空 activity 作为兜底。
5. 全程海量 `DeleteLocalRef`，返回值是 **全局引用**（跨线程/跨帧存活）。

**`bool ImeEnsureActivity()`**（L129–139）
幂等地填充 `g_ime_activity`：已有则直接 true；否则 `ImeGetEnv`→`ImeFindActivityByActivityThread`→存全局→按需 Detach。被本模块及 `配置初始化.cpp`/`音量键缩放.cpp`/`防录屏.cpp` 大量复用。

### 2.4 内存 dex 加载（键盘桥的载入）

**`bool ImeLoadDex()`**（L288–361）—— 键盘功能的真正入口
幂等（`g_ime_loaded`）。流程：
1. 前置：`g_ime_jvm` 就绪、`ImeEnsureActivity()`、`imgui_dex_len>0`。
2. 从 `g_ime_activity.getClassLoader()` 拿宿主 ClassLoader 作为父加载器。
3. `NewByteArray(imgui_dex_len)` + `SetByteArrayRegion` 把内嵌 dex 拷进 JVM。
4. `ByteBuffer.wrap(byte[])` → `new dalvik/system/InMemoryDexClassLoader(ByteBuffer, parentLoader)`。
5. `g_ime_loader = NewGlobalRef(loader)`。
6. **关键联动**：`ImeGetClass("com.mxp.Helper")` → 调其静态 `init(Landroid/app/Activity;)V`，把 Activity 交给 Java 侧初始化键盘基础设施；随后 `ImeRegisterNatives(env)` 绑定回传函数。
7. 结尾清理 + Detach，置 `g_ime_loaded=true`。

**`bool MainDexLoad()`**（L191–256）与 `ImeLoadDex` 几乎逐行同构（复制粘贴），差别仅：用 `classes_dex`/`classes_dex_len`、写 `g_main_dex_loader`/`g_main_dex_loaded`，且**不调 Helper.init、不 RegisterNatives**。**同样是死代码——全项目无调用者**（`classes_dex` 的真正释放/注入在 `jni/App/Main.cpp` 里独立完成）。

### 2.5 Java↔Native 双向桥

**`void ImeNativeAddChar(JNIEnv*, jclass, jint codepoint)`**（L141–144）
Java 侧 TextWatcher 每收到一个字符就回调这里 → `ImGui::GetIO().AddInputCharacter((unsigned)codepoint)`。**这就是"文本回传"的主通道**——用户在系统软键盘上打的每个字符（Unicode 码点）灌进 ImGui 输入队列。

**`void ImeNativeKeyEvent(JNIEnv*, jclass, jint keyCode)`**（L146–158）
处理软键盘的**功能键**，把 Android keyCode 硬编码映射成 ImGui 按键，每键同帧发一次 down+up：
- `67` (KEYCODE_DEL) → `ImGuiKey_Backspace`
- `66` (KEYCODE_ENTER) → `ImGuiKey_Enter`
- `21` (DPAD_LEFT) → `ImGuiKey_LeftArrow`
- `22` (DPAD_RIGHT) → `ImGuiKey_RightArrow`
- `112` (FORWARD_DEL) → `ImGuiKey_Delete`
连续退格等"长按重复"由 Java 侧 `keyRepeatHandler`/`keyRepeatRunnable` 反复回调实现（见 §3）。

**`bool ImeRegisterNatives(JNIEnv* env)`**（L160–172）
对 `com.mxp.Helper` 注册上面两个 native：`nativeAddChar (I)V`、`nativeKeyEvent (I)V`。`RegisterNatives` 返回 0 为成功。这样 Java 的 `Helper.nativeAddChar/nativeKeyEvent` 就直连到 C++。

### 2.6 键盘显隐与每帧驱动

**`void ImeShowKeyboard(bool show)`**（L363–377）
确保 dex 已载入后，`ImeGetClass("com.mxp.Helper")` → 调静态 `showSoftKeyboard(Z)V`，把 true/false 传进去。这是 **C++→Java 显/隐软键盘** 的单一出口。

**`void ImeUpdateByImGui()`**（L379–388）—— 每帧调用
在 `step` 里每帧跑：确保 dex 载入；读 `ImGui::GetIO().WantTextInput`（有文本框获得焦点时 ImGui 置 true）；只在**边沿变化**时（`want != g_ime_last_want_text`）调 `ImeShowKeyboard(want)` 并记忆状态。避免每帧反复调起/收起键盘。

### 2.7 打开 URL

**`bool OpenUrlByActivity(const char* url)`**（L390–434）
纯 JNI 构造 `Intent(ACTION_VIEW, Uri.parse(url))` 并 `activity.startActivity(intent)`，用系统浏览器打开链接。被主界面"下载/更新"按钮用（`主界面绘制.cpp:97` 硬编码 `https://share.feijipan.com/s/uGQ1k9ij`）。同样全程精细 `DeleteLocalRef`，`ok` 取 `!ExceptionCheck()`。

---

## 3. 内嵌 Java 辅助类 `com.mxp.Helper`（从 dex 反解字符串还原）

`ime_dex_data.h` 里没有 Java 源码，我从 dex 常量池抽取符号还原了它的结构与流程：

**Helper 的字段/成员**：`keyboardEditText`(EditText)、`keyboardImm`(InputMethodManager)、`keyboardWatcher`(TextWatcher)、`keyboardActive`、`keyRepeatHandler`/`keyRepeatRunnable`(Handler+Runnable，按键重复)、5 个合成 lambda（`Helper$$ExternalSyntheticLambda0..4`）与内部类 `Helper$1..$5`。

**`init(Activity)`**（被 `ImeLoadDex` 调）：保存 Activity，创建一个隐藏 `EditText`，通过 `WindowManager` + `WindowManager$LayoutParams`（`addView`/`updateViewLayout`）挂到窗口上；`setInputType`/`setImeOptions`/`setPrivateImeOptions` 配置输入行为；给它装 `TextWatcher`（监听输入变化）和 `OnKeyListener`/`TextView$OnEditorActionListener`（监听功能键/回车）；取 `InputMethodManager`。

**`showSoftKeyboard(boolean)`**（被 `ImeShowKeyboard` 调）：通过 Handler 把操作 post 到 UI 线程（`lambda$showSoftKeyboard$1/2/3`）。true → 让隐藏 EditText 请求焦点并 `imm.showSoftInput(...)`；false → `imm.hideSoftInputFromWindow(...)`。

**文本回传闭环**：用户敲键 → 系统软键盘写入隐藏 EditText → `TextWatcher` 触发 → Java 逐字符调 `nativeAddChar(codepoint)`；功能键经 `onKey` → `nativeKeyEvent(keyCode)`；长按重复由 `keyRepeatHandler` 循环回调 → 最终都进 ImGui 的 IO。ImGui 的 `InputText` 于是"以为"自己在被真实键盘输入。

**顺带能力**（说明 Helper 是个杂物类，不止键盘）：`showToast`（`lambda$showToast$0`）、`vpnWatcherRunnable`/`secureWatcherRunnable`（网络/VPN 与安全窗监视）、Connectivity/NetworkCapabilities 相关引用。

（`classes_dex` 里的 `com.example.imgui.GLES3JNIView` 则是宿主注入用 dex：`ActivityLifecycleCallbacks`、`setupImGuiViewOnMainThread`、`VolumeKeyEvent`/`handleVolumeKeyEvent`、`dispatchKeyEvent` 等，与本键盘桥不同用途。）

---

## 4. 关键数据结构 / 全局变量 / 常量 / 偏移

全部定义在 `全局状态.cpp`（声明在 `全局状态.h`），本模块只读写这些跨模块全局：

| 全局 | 类型 | 语义 |
|---|---|---|
| `g_ime_jvm` | `JavaVM*` | 进程级 JVM 句柄；**惰性捕获**——在 `imguijni.cpp` 的 init/step/MotionEventClick/VolumeKeyEvent 里用 `env->GetJavaVM(&g_ime_jvm)` 填充，**没有 JNI_OnLoad 走这条路**。 |
| `g_ime_activity` | `jobject`(GlobalRef) | 反射拿到的前台 Activity。 |
| `g_ime_loader` / `g_ime_loaded` | `jobject`(GlobalRef)/`bool` | 键盘桥内存 dex 的 `InMemoryDexClassLoader` 与其加载标志。 |
| `g_main_dex_loader` / `g_main_dex_loaded` | 同上 | 仅被死代码 `MainDexLoad` 写，运行期永远为空。 |
| `g_current_imgui_class_loader` | `jobject`(GlobalRef) | 宿主 App ClassLoader，`防录屏` 用。 |
| `g_ime_last_want_text` | `bool` | 上一帧 `WantTextInput`，边沿检测用；`卡密验证.cpp` 也直接写它。 |
| `g_ime_last_input_active` | `bool` | 登录框焦点状态，`卡密验证.cpp` 用。 |

**常量/魔数**：内嵌 dex 长度 `imgui_dex_len=12948`、`classes_dex_len=10644`；`ImeNativeKeyEvent` 的 Android keyCode 硬编码 `67/66/21/22/112`；`JNI_VERSION_1_6`。本模块**不涉及 IL2CPP 内存偏移**（那些在 `游戏函数调用.cpp` 里）。

---

## 5. 关键技术与调用链

**用到的技术**：JNI（反射调用 Java、`RegisterNatives` 反向绑定）、Android 私有 API 反射（`ActivityThread.mActivities`）、**内存 dex 注入**（`InMemoryDexClassLoader`，Android 8.0/API 26+）、`WindowManager` 叠加隐藏 EditText 调起 `InputMethodManager`、`Intent.ACTION_VIEW`。**不涉及** hook/OpenGL/miniaudio/curl（那些分别在防录屏/EGL、音频模块、t3sdk 里）。

**键盘完整调用链**：
```
GLES3JNIView.step (imguijni.cpp:172, 每帧)
  → env->GetJavaVM(&g_ime_jvm)  // 首帧惰性捕获
  → CaptureCurrentClassLoader
  → DrawFloatingWindow()  // ImGui::InputText 置 io.WantTextInput
  → ImeUpdateByImGui (输入法桥接.cpp:379)
        → ImeLoadDex() [首次]
              → ImeEnsureActivity → ImeFindActivityByActivityThread(反射)
              → InMemoryDexClassLoader(imgui_dex)
              → Helper.init(activity) [Java: 建隐藏EditText+IMM+TextWatcher]
              → ImeRegisterNatives [绑定 nativeAddChar/nativeKeyEvent]
        → 边沿变化 → ImeShowKeyboard(want) → Helper.showSoftKeyboard(Z) [调起系统软键盘]

用户敲字 → 隐藏EditText.TextWatcher → Helper.nativeAddChar(cp)
        → ImeNativeAddChar → ImGui::GetIO().AddInputCharacter  // 回传闭环
功能键 → Helper.onKey → Helper.nativeKeyEvent(kc)
        → ImeNativeKeyEvent → io.AddKeyEvent(...)
```

**其它入口**：`imgui_Shutdown`(imguijni.cpp:230) 调 `ImeShowKeyboard(false)` 收键盘；`卡密验证.cpp:147` 在登录框获焦时**手动** `ImeShowKeyboard(true)`（绕过 `WantTextInput` 的边沿逻辑，直接顶开键盘）；`主界面绘制.cpp:97` 调 `OpenUrlByActivity`。

---

## 6. 依赖关系

**本模块 `#include`**：`全局状态.h`（全局）、`ime_dex_data.h`（`imgui_dex`）、`classes_dex.h`（`classes_dex`，仅死代码用）、`imgui.h`、`<android/log.h>`、`<jni.h>`。

**被谁引用**：
- `jni/App/imguijni.cpp` —— 主调用方：`ImeUpdateByImGui`(step 每帧)、`ImeShowKeyboard(false)`(shutdown)、`CaptureCurrentClassLoader`(init/step/MotionEventClick/VolumeKeyEvent)。
- `jni/res/卡密验证.cpp` —— `ImeShowKeyboard(true)` + 读写 `g_ime_last_want_text`/`g_ime_last_input_active`。
- `jni/res/主界面绘制.cpp` —— `OpenUrlByActivity`。
- `jni/res/防录屏.cpp` —— `GetClassFromCurrentLoader`（拿宿主 `com.example.imgui.ImGui`）。
- `jni/res/配置初始化.cpp`、`音量键缩放.cpp`、`防录屏.cpp` —— 复用 `ImeEnsureActivity`/`ImeGetEnv`/`ImeClearException` 及 `g_ime_activity`/`g_ime_jvm`（配置初始化用它拿 `getExternalFilesDir` 定位配置目录）。

即：本模块是 **JNI 基础设施的中枢**，被键盘、登录、防录屏、配置、音量键等模块共同依赖。

---

## 7. 值得注意 / 风险点

1. **两段死代码**：`MainDexLoad`（L191–256）与 `MainDexGetClass`（L175–189）无任何调用者，且 `MainDexGetClass` 连头文件都没有。它们是从 `ImeLoadDex`/`ImeGetClass` 复制来的，`classes_dex` 的实际处理另在 `Main.cpp`。属可删的冗余，维护时两份拷贝易不一致。

2. **`CaptureCurrentClassLoader` 的可疑类型强转**：在 `step`/`MotionEventClick` 里，native 签名是 `jobject obj`（非静态，obj 是 View 实例），却 `CaptureCurrentClassLoader(env, (jclass)obj)` 当成 `Class` 传入，函数内对它调 `Class.getClassLoader()`——**语义上是错的**（对 View 实例调 Class 的方法）。之所以没炸，是因为 `init` 是 static native、第二参是**真正的 jclass**、且函数有幂等守卫，首帧 `init` 已成功捕获，后续 step 的错误调用被早退挡住。属"能跑但不干净"的坑。

3. **API 版本硬依赖**：`InMemoryDexClassLoader` 要求 **Android 8.0 (API 26)+**，无 `DexClassLoader` 回退；低版本设备键盘功能整体失效。

4. **异常全静默**：`ImeClearException` 到处清异常但不记日志，任何 JNI 失败（找不到字段、反射失败）都被吞掉，只表现为"键盘不弹"，排障困难。反射字段名（`mActivities`/`paused`/`activity`）依赖 Android 内部实现，未来版本改名即静默失效。

5. **两条显示键盘的路径共用一个状态位** `g_ime_last_want_text`：`ImeUpdateByImGui` 的边沿逻辑与 `卡密验证.cpp` 的手动 `ImeShowKeyboard(true)` 都读写它，登录界面靠手动强开——说明作者遇到过 `WantTextInput` 在登录场景不可靠的问题，这是个"补丁式"处理。

6. **硬编码可追溯信息**：`主界面绘制.cpp:97` 的下载链接 `https://share.feijipan.com/s/uGQ1k9ij` 经本模块 `OpenUrlByActivity` 打开；配合 `全局状态.cpp` 里明文的 T3 验证码/APPKEY/RSA 公钥，整个项目的分发与授权链路是可指纹化的（CTF/溯源角度值得记一笔）。

7. **线程模型正确性**：`ImeGetEnv(&attached)` + 结尾按需 `DetachCurrentThread` 的配对在每个函数里都做对了；跨帧存活对象（activity、loader）一律 `NewGlobalRef`，局部引用逐一 `DeleteLocalRef`——这部分工程质量较高，没有明显引用泄漏。


---

## 模块：`state-config`

我已完整读取全部六个文件的每一行，并追踪了其跨模块引用关系。以下是通读记录。

---

# 通读记录：全局状态 / Config / 配置初始化 三件套

> 这三个文件构成整个 ImGui 游戏菜单的**中枢层**：`全局状态` 是所有功能开关与运行时变量的集中定义、`Config` 是基于 pugixml 的持久化引擎、`配置初始化` 是启动时的目录发现 + 注册 + 默认值生成。理解这一组是理解整个菜单功能集的钥匙。

---

## 0. 文件清单与规模总览

| 文件 | 行数 | 角色 |
|------|------|------|
| `jni/res/全局状态.cpp` | 198 | 全部全局变量的**实体定义** + 日志系统实现 |
| `jni/res/全局状态.h` | 134 | 全部全局变量的 `extern` 声明 + `sConfig`/`ThemeType` 类型 |
| `jni/res/Config.cpp` | 189 | `ConfigManager` 实现（pugixml XML 读写 + 键注册表） |
| `jni/res/Config.h` | 44 | `ConfigManager` 类声明（静态类，纯静态成员） |
| `jni/res/配置初始化.cpp` | 148 | 目录发现（JNI 反射）、递归建目录、字段注册、Load/Save 编排 |
| `jni/res/配置初始化.h` | 6 | 4 个自由函数原型 |

依赖方向：`配置初始化` → 依赖 `全局状态` + `Config` + `输入法桥接`；`Config` 独立（只依赖 pugixml）；`全局状态` 依赖 `t3sdk.h` + `ImGenie.h` + imgui。被引用方：`全局状态.h` 被 **13 个文件** include（几乎全项目），是最核心的头。

---

## 1. `全局状态.cpp` / `全局状态.h` — 功能开关与变量的集中地

### 1.1 基础运行时 / 渲染状态
```cpp
int screenWidth, screenHeight;
bool g_Initialized;              // ImGui 是否已初始化
bool g_imgui_backend_active;     // GLES3 后端是否活跃
ANativeWindow* g_native_window;
ImGuiWindow* g_window;
JavaVM* g_ime_jvm;               // 缓存的 JavaVM，供任意线程 attach
jobject g_ime_activity;          // 反射拿到的当前 Activity
sConfig Config{true};            // 结构体只有一个字段 IsWindowVisible，默认 true
```
`sConfig` 是个**极小结构体**（`struct sConfig { bool IsWindowVisible; };`），与后面重量级的 `ConfigManager` 完全无关，只用于控制菜单窗口是否可见，命名容易与配置系统混淆（见第 6 节坑点）。

### 1.2 T3 网络验证（卡密系统）—— 硬编码敏感常量
`全局状态.cpp:16-48` 内嵌了整套 T3（一个第三方卡密验证 SDK）接入参数，**全部明文硬编码**：

```cpp
const char* T3_LOGIN_CODE     = "555F6E97AA87EACA";
const char* T3_NOTICE_CODE    = "0E3C22A329179374";
const char* T3_VERSION_CODE   = "CF78A3C231FBC7E2";
const char* T3_HEARTBEAT_CODE = "87A6E643DAED8CFF";
const char* T3_APPKEY         = "10d0b15afbcf1091ec4b846601f7bd8e";
const char* T3_RSA_PUBLIC_KEY = "-----BEGIN PUBLIC KEY-----\n...";  // 1024-bit RSA 公钥
const char* LOCAL_VERSION     = "1000";
```

配套的运行时状态用 `std::atomic` + `std::mutex` + `std::once_flag` 做线程安全（验证在后台线程跑）：
- `T3Verify* g_t3_verify` — SDK 实例指针（类定义在 `t3sdk/t3sdk.h`，提供 `initRSA/login/queryKami/heartbeat/getNotice/checkUpdate` 等，见 `t3sdk.h:149`）
- `g_t3_ready / g_t3_authed / g_t3_verifying / g_t3_heartbeat_running`（atomic bool）— 各阶段标志
- `g_t3_status/notice/end_time/statecode/kami`（std::string）— 展示与心跳所需字段
- `g_t3_machine_code = "android_inject_device"` — **机器码是写死的常量字符串**，即所有设备上报同一个 IMEI，等于绕过了设备绑定（见第 6 节）
- 自动登录相关：`g_kami_saved_valid`（是否曾验证通过）、`g_saved_kami`（保存的卡密明文）、`g_t3_auto_login_tried/running`

> 调用链：`卡密验证.cpp` 消费这些全局；`g_kami_saved_valid` 与 `g_saved_kami` 被注册进配置（见 §3.3），因此**卡密明文会落盘到 XML**。

### 1.3 Android 输入法桥 / DEX 加载器 / 防录屏(Secure Surface) 状态
`全局状态.cpp:51-83` 一组指针与布尔，注释点明关键坑：当前 `GLES3JNIView.step`/`MotionEventClick` 是 **static native**，JNI 第二参数不是 View，所以拿 Activity 不能用 `obj.getContext()`，必须走 `ActivityThread` 反射。相关变量：
- DEX 加载：`g_main_dex_loader / g_main_dex_loaded / g_current_imgui_class_loader`、`g_ime_loader / g_ime_loaded`、`g_volume_loader / g_volume_loaded`、`g_secure_loader / g_secure_loaded` —— 说明项目把 IME、音量键、防录屏功能分别打进独立 DEX 动态加载。
- 防录屏(Secure)独立 EGL 上下文：`g_secure_egl_display/surface/context`（各自 `EGL_NO_*` 初值）、`g_secure_native_window`、`g_secure_surface_width/height`、`g_pending_secure_switch/target`、`g_rendering_secure_surface`、`g_secure_skip_screenshot` —— 用一套**独立 EGLDisplay/Surface/Context** 渲染到 SECURE flag 的 Surface 上以规避截屏/录屏。
- IME 状态：`g_ime_last_want_text / g_ime_last_input_active`。

### 1.4 UI / 主题 / 菜单状态
```cpp
int g_main_tab_index = 0;            // 当前标签页（持久化）
int g_current_theme_index = 3;       // 默认"液态玻璃"，见 ThemeType 枚举
bool g_show_another_window = false;
ImGenieParams g_GenieParams;         // ImGenie(可视化调试/捕获工具)参数
bool g_show_log_window = false;
ImVec4 g_clear_color = ImVec4(0.45,0.55,0.60,1.0);
float g_ui_font_scale = 1.0f;        // 界面缩放（持久化）
bool g_volume_key_ui_scale = false;  // 用音量键调 UI 大小（持久化）
char g_logInput[256] = "";
```
`ThemeType` 枚举（`全局状态.h:128`）：`Theme_Classic=0, Theme_Light=1, Theme_Pink=2, Theme_LiquidGlass=3, Theme_MusicUI=4` —— 与 `g_current_theme_index` 默认 3 对应。

配置基础设施三兄弟：`g_config_base_dir`（配置根目录）、`g_app_config_ready`（配置系统是否就绪）、`g_config_registered`（字段是否已注册，防重复注册）。

### 1.5 游戏作弊功能开关（菜单功能集的核心清单）
这是"这个菜单到底能干什么"的答案。全部为普通全局变量，被 `游戏函数调用.cpp` 消费并映射到 IL2CPP 字段偏移：

| 功能 | 变量 | 默认值 | 说明 |
|------|------|--------|------|
| 吐球加速 | `g_spitball_accelerate` / `g_spitball_speed` | false / 3 | 加速吐球 |
| 三角合球 | `g_triangle_running` | false | 运行中标志；执行时会临时关掉吐球加速避免并发 IL2CPP 调用崩溃（`游戏函数调用.cpp:1948`） |
| 视野修改 | `g_view_enabled` / `g_view_scale` / `g_view_was_enabled` | false / 1.5 / false | 写 `ViewScale` 相关字段；关闭时恢复为 0.5（`游戏函数调用.cpp:914`） |
| 粘合修改 | `g_merge_enabled` / `g_merge_value` / `g_merge_was_enabled` | false / **0.588** / false | 写 `ATimeSetting` 静态字段，实际写入 `value-0.588`（`游戏函数调用.cpp:1025`），0.588 是游戏原始基准值 |
| 排名改名 | `g_rename_enabled` / `g_rename_was_enabled` / `g_rename_use_color` / `g_rename_name_color`(金黄 1,0.85,0.2) / `g_rename_detailed_rank`(粗略) / `g_rename_show_self`(不改自己) | 见括号 | 遍历 NetPlayer 数组改名、可上色、可显示精确/粗略排名 |
| 球上名字大小 | `g_name_scale_enabled` / `g_name_scale` | false / **1.875** | 对应游戏 `TargetNameScale2.Update` 里 `SelfTF.localScale` 公式末尾系数，1.875 即原公式系数 |

> `was_enabled` 系列是"上一帧状态"，用于检测开关翻转以便在关闭时**恢复游戏原值**（典型的 hook 开/关幂等模式）。

### 1.6 日志系统（`全局状态.cpp:119-198`）
- 静态 262144 字节环形缓冲 `char g_log_text[262144]`（.h 中 `extern` 暴露给 UI 直接显示）；辅以静态 `g_log_len / g_log_line_count / MAX_LOG_LINES(2000) / g_log_file_path / g_log_save_failed`。
- **`AppendLog(const char* fmt, ...)`**：可变参 `vsnprintf` 到 512 栈缓冲。**硬编码黑名单**：含 `[名字大小]`/`[改名]`/`[IL2CPP]` 的日志直接丢弃（`全局状态.cpp:135`），避免刷屏。缓冲将满时从头**跳过 500 行**做 `memmove` 压缩。每次写完把**整块内存快照 `"w"` 覆盖写**到 `g_config_base_dir/game_log.txt`（注释说明：不用 `"a"` 追加以免旧的名字大小日志把磁盘堆满）。同时 `__android_log_print(ANDROID_LOG_INFO, "IMGUI_GAME", ...)` 打到 logcat。
  - 实现要点/坑：每条日志都全量重写整个文件（最坏 256KB/条），I/O 成本高；路径只在 `g_config_base_dir` 就绪后才生效，之前的日志不落盘。
- **`ClearLog()`**：清空内存缓冲并把文件截断为空。

---

## 2. `Config.cpp` / `Config.h` — pugixml 持久化引擎

`ConfigManager` 是**全静态类**（无实例），核心是一张"键→(Getter,Setter)"注册表加一个缓存的 XML 文档。

### 2.1 静态成员与并发
```cpp
std::string ConfigManager::s_configPath;
pugi::xml_document* ConfigManager::s_doc = nullptr;   // 惰性 new，缓存文档
std::unordered_map<std::string, std::pair<Getter,Setter>> s_registry;
static std::recursive_mutex g_config_mutex;           // 递归锁保护全部公有方法
```
用 **`std::recursive_mutex`** 是因为方法互相调用（如 `Save`→`Cleanup`），普通 mutex 会自锁。

### 2.2 关键函数逐一说明
- **`SetConfigPath(path)`**：路径变化时先 `Cleanup()` 丢缓存再赋值，保证换路径后重新加载。
- **`EnsureDocument()`**（private）：惰性构造文档；若文件存在则 `load_file`；**如果根节点 `天天开心` 不存在，`reset()` 后 `append_child("天天开心")`**。即 XML 根固定叫 `天天开心`（"天天开心"），文件名叫 `天天开心.xml`。
- **`Load()`**：遍历注册表，用 `SimpleTagForKey` 把内部 key 映射成 XML 标签名，取节点文本调用对应 Setter 回写到全局变量。**只加载 `SimpleTagForKey` 认识的键**（返回 nullptr 的跳过）。
- **`Save()`**：**不复用缓存文档**，新建一个 `pugi::xml_document`，按**固定 `saveOrder[]` 数组顺序**（8 个键）逐个从注册表取 Getter 写入。写法上先写 `s_configPath + ".tmp"` 再 `rename` 做**原子写入**（防写一半进程被杀导致 XML 损坏），rename 失败则 `remove` 临时文件。成功后 `Cleanup()` 使缓存失效，下次从新文件重读。注释明确："**颜色字段不写入配置**"——所以 `g_rename_name_color` 每次启动回默认金黄。
- **`Register / RegisterBool / RegisterInt / RegisterFloat / RegisterString`**：用 lambda 捕获变量指针生成 Getter/Setter。Bool 序列化为 `"true"/"false"`；Int/Float 用 `std::to_string`；反序列化经 `TryParseInt`/`TryParseFloat`（严格校验 errno、末尾字符、范围，**解析失败保持原值不覆盖**——一种健壮性设计）。
- **`GetString(key, default)` / `SetString(key, value)`**：绕过注册表的**手动读写**，直接操作缓存文档节点；找不到 `SimpleTagForKey` 映射时**回退用 key 本身作标签**。
- **`Cleanup()`**：`delete s_doc; s_doc=nullptr`。

### 2.3 键名映射表 `SimpleTagForKey`（`Config.cpp:42`）
内部 key 与落盘 XML 标签**不一致**，是一层刻意的别名（可能为混淆/兼容旧项目）：

| 内部 key | XML 标签 |
|----------|----------|
| 卡密验证通过 | 自动登录 |
| 保存的卡密 | 卡密 |
| 界面缩放 / 音量键调UI大小 / 当前标签页 / 主题索引 / 显示日志窗口 / 显示另一个窗口 | 同名 |

---

## 3. `配置初始化.cpp` / `配置初始化.h` — 启动编排

### 3.1 `XmlConfigCompileProbe()`
纯**编译/链接期探针**：`if(false)` 内调用一次 `ConfigManager::GetString`，只为确保 pugixml + ConfigManager 成功链接进来，不产生任何运行时行为。由 `imguijni.cpp:106` 在初始化时调用。

### 3.2 `MkdirRecursive(path)` / `ParseConfigFloat(...)`
- `MkdirRecursive`：标准的逐级 `mkdir(...,0755)` 递归建目录，用 `PATH_MAX` 栈缓冲，容忍 `EEXIST`，去尾部 `/`。
- `ParseConfigFloat`：与 `Config.cpp` 里 `TryParseFloat` 逻辑重复（一处代码冗余），本文件内实际未见调用点。

### 3.3 `InitAppConfigOldStyle(configDir)` — 字段注册中枢
拼出 `configDir + "/天天开心.xml"` 设为配置路径。用 `g_config_registered` 保证**只注册一次**，注册这 **8 个持久化字段**：
```cpp
RegisterBool ("显示另一个窗口", &g_show_another_window);
RegisterBool ("显示日志窗口",   &g_show_log_window);
RegisterFloat("界面缩放",       &g_ui_font_scale);
RegisterBool ("音量键调UI大小", &g_volume_key_ui_scale);
RegisterInt  ("当前标签页",     &g_main_tab_index);
RegisterInt  ("主题索引",       &g_current_theme_index);
RegisterBool ("卡密验证通过",   &g_kami_saved_valid);   // → XML <自动登录>
RegisterString("保存的卡密",    &g_saved_kami);         // → XML <卡密>，卡密明文落盘
```
随后按"旧项目逻辑"：先 `Load()` 再 `g_app_config_ready = Save()`（首次无文件时 Save 生成默认配置）。

> 注意：**游戏作弊开关（吐球/视野/粘合/改名/名字大小）一个都没注册**——这些功能状态**不持久化**，每次启动都是默认关闭。持久化的只有 UI 偏好 + 卡密自动登录。

### 3.4 `GetCurrentAppExternalFilesDir(outDir)` — JNI 反射拿外部目录
纯 JNI 反射，无硬编码包名：先 `ImeEnsureActivity()` 确保 `g_ime_activity`/`g_ime_jvm` 就绪 → `ImeGetEnv` 拿 JNIEnv（可能 attach 当前线程）→ 反射 `Activity.getExternalFilesDir(null)` 得 `File` → `File.getAbsolutePath()` 得路径字符串。全程严谨：每步 `ImeClearException` 清异常、`DeleteLocalRef` 释放局部引用、按需 `DetachCurrentThread`。依赖 `输入法桥接.h` 提供的 `ImeEnsureActivity/ImeGetEnv/ImeClearException`。

### 3.5 `InitAppConfigOnce()` — 幂等入口
`g_app_config_ready` 已就绪则直接返回；否则拿外部目录 → 拼 `externalDir + "/句号的辅助"`（"句号的辅助"，作者昵称）→ `MkdirRecursive` → `InitAppConfigOldStyle`。**最终配置目录**：`<app>/files/句号的辅助/`，里面有 `天天开心.xml` 与 `game_log.txt`。由 `imguijni.cpp:107` 与 `imguijni.cpp:175` 调用（后者是 surface 创建时的兜底重试，因为首次可能拿不到 Activity）。

### 3.6 `SaveAppConfigNow()`
未就绪先 `InitAppConfigOnce()`，就绪则 `ConfigManager::Save()`。被以下改动点调用，构成**"改一处即时存盘"**模式：`触摸输入.cpp:256`（切标签）、`音量键缩放.cpp:82`、`主界面绘制.cpp:191/197/212`（主题/缩放/音量键开关）、`卡密验证.cpp:103/110`（验证成功/失败清标志）。

---

## 4. 关键技术与调用链小结

- **JNI 反射链**：`ActivityThread`（在输入法桥接层）→ 当前 Activity → `getExternalFilesDir`/`getAbsolutePath`。所有跨线程 JNI 走 `g_ime_jvm` 缓存 + attach/detach。
- **IL2CPP**：本组文件本身不直接调 IL2CPP，但定义的作弊开关（`g_view_scale/g_merge_value/g_name_scale/...`）在 `游戏函数调用.cpp` 里通过 `il2cpp_field_get_offset` 动态解析字段偏移后 `*(float*)((char*)instance+offset)=value` 写内存。硬编码兜底偏移：`id@16, name@24, OriName@32, NameLables@72, NetPlayer@464, LastRank@64`（`游戏函数调用.cpp:1042-1061`），运行时若能解析到符号则覆盖这些默认值。
- **T3 卡密网络验证**：`t3sdk`（RSA 模式 `initRSA`，见 `t3sdk.h:149` 的 `T3Verify` 类）→ `login/queryKami/heartbeat`。心跳后台线程 + atomic 状态机。
- **pugixml**：唯一的持久化格式（XML，非 JSON），原子 tmp+rename 写入。
- **EGL/防录屏**：独立 EGLDisplay/Surface/Context 渲染到 SECURE surface。
- **DEX 动态加载**：IME/音量键/防录屏各自独立 ClassLoader。
- **持久化格式与路径**（重点结论）：
  - 路径：`<外部私有目录>/files/句号的辅助/天天开心.xml`
  - 格式：UTF-8 XML，根 `<天天开心>`，8 个子节点（标签名见 §2.3），两空格缩进。
  - 日志：同目录 `game_log.txt`。

---

## 5. 依赖关系

- **`全局状态.h` 是全项目最核心头**，被 13 个 .cpp include（音频/音量/防录屏/输入法/触摸/游戏函数/卡密/主界面/主题等）。改动这里牵动全局。
- `配置初始化.cpp` 依赖：`全局状态.h`（读写全局）+ `res/Config.h`（ConfigManager）+ `输入法桥接.h`（JNI 反射）。
- `Config.*` 只依赖 pugixml，**零业务耦合**，可独立复用。
- 入口编排在 `jni/App/imguijni.cpp`（`InitAppConfigOnce`、`XmlConfigCompileProbe` 调用点）。
- 存盘触发遍布 UI 交互点（触摸/音量/主界面/卡密）。

---

## 6. 值得注意 / 风险点（给负责人）

1. **敏感常量全明文硬编码**（`全局状态.cpp:18-30`）：T3 的 5 个调用码 + APPKEY + RSA 公钥 + 版本号写死在 .cpp。虽是 CTF 自研靶标，但这套值可被直接提取复用/伪造请求。
2. **机器码写死**：`g_t3_machine_code = "android_inject_device"`（`全局状态.cpp:48`），所有设备上报同一 IMEI，等同**关闭设备绑定**——卡密可跨设备共用，是验证体系的明显弱点/后门式设计。
3. **卡密明文落盘**：`g_saved_kami` 经 `RegisterString("保存的卡密")` 写入 XML `<卡密>` 标签（`配置初始化.cpp:69`），无加密。
4. **`sConfig Config` 与 `ConfigManager` 同名混淆**：一个是"窗口可见"布尔壳，一个是持久化引擎，命名极易误读。
5. **日志 I/O 放大**：`AppendLog` 每条日志全量覆盖写整个内存缓冲到磁盘（最坏 ~256KB/条）+ `fflush`，高频日志下磁盘/性能压力大。
6. **颜色与作弊开关不持久化**：`Save()` 明确不写颜色字段；游戏功能开关根本未注册。用户每次重启都要重设——是有意为之（安全/避免误开），但需向用户说明。
7. **代码冗余**：`ParseConfigFloat`(配置初始化) 与 `TryParseFloat`(Config) 重复实现；且前者疑似**无调用点**（死代码）。
8. **`Load()` 依赖 `SimpleTagForKey` 白名单**：任何注册了但没进 `SimpleTagForKey` 的键会被静默跳过加载，新增持久化字段时**必须同步改三处**（`SimpleTagForKey` + `saveOrder[]` + `Register`），易漏。
9. `AppendLog` 满缓冲压缩用 `linesToSkip=500` 固定值，与 `MAX_LOG_LINES=2000` 无联动，属经验值硬编码。

以上均可追溯到具体文件与行号。


---

## 模块：`auth-antirecord`

我已完整读取全部4个目标文件（每一行），并追溯了它们依赖的外部定义（`T3Verify` SDK、全局状态、`secure_dex_data.h`、调用点）以便让网络请求/机器码/secure_dex 部分可追溯。以下是通读技术笔记。

---

# 通读技术笔记：卡密验证 + 防录屏 模块

## 0. 本组文件概览与代码规模

| 文件 | 行数 | 作用 |
|---|---|---|
| `/home/user/-/jni/res/卡密验证.cpp` | 177 | T3 卡密授权的 App 层封装：初始化、异步登录、心跳、自动登录、ImGui 登录 UI |
| `/home/user/-/jni/res/卡密验证.h` | 9 | 上述 7 个函数的声明 |
| `/home/user/-/jni/res/防录屏.cpp` | 271 | 反截屏/反录屏：动态加载内存 dex → 创建 SECURE Surface → 把 ImGui 画面单独渲染到该安全层 |
| `/home/user/-/jni/res/防录屏.h` | 9 | 上述 7 个函数的声明（少 1 个内部函数） |

这两个模块本身**不含**加密算法、网络栈、dex 字节，它们只是 App 层胶水。真正的实现分布在：
- `jni/t3sdk/t3sdk.h` / `t3sdk.cpp`：T3Verify 类、RSA、CustomBase64、curl、`getMachineCode()`
- `jni/res/全局状态.h` / `全局状态.cpp`：所有 `g_t3_*`、`g_secure_*` 全局量与常量（RSA 公钥、appkey、调用码）
- `jni/res/secure/secure_dex_data.h`：内嵌的 SECURE Surface 辅助类 dex（4584 字节）
- 调用点：`jni/App/imguijni.cpp`（渲染循环）、`jni/res/主界面绘制.cpp`（UI 开关）

---

## 1. 卡密验证模块（license / 授权）

### 1.1 头文件 `卡密验证.h`
仅声明 7 个自由函数（无类）：`T3_SetStatus / T3_GetStatus / T3_InitOnce / T3_StartHeartbeat / T3_LoginAsync / T3_TryAutoLoginFromConfig / DrawT3AuthUI`。全部走 C++ std::string，属于 App 层门面。

### 1.2 关键函数逐个说明（`卡密验证.cpp`）

**`void T3_SetStatus(const std::string&)` / `std::string T3_GetStatus()`（L9-19）**
线程安全的状态字读写，`std::lock_guard<std::mutex> g_t3_mutex` 保护 `g_t3_status`。UI 线程和后台工作线程共享该字符串显示登录进度。

**`void T3_InitOnce()`（L21-47）**
`std::call_once(g_t3_init_once, ...)` 保证只初始化一次：
1. `g_t3_verify = new T3Verify()`；
2. 调用 `initRSA(T3_LOGIN_CODE, T3_NOTICE_CODE, T3_VERSION_CODE, T3_HEARTBEAT_CODE, T3_APPKEY, T3_RSA_PUBLIC_KEY)` — **采用 RSA 编码模式**（`encodeType_=1`），失败则置状态"验证系统初始化失败"、`g_t3_ready=false`；
3. 成功后 `g_t3_ready=true`，并**另起 detach 线程**异步拉取 `getLatestVersion()` 与 `getNotice()`，把版本号与本地 `LOCAL_VERSION`("1000") 字符串比较（注意是字符串 `>` 比较，非数值），过旧则提示"版本过旧"，公告写入 `g_t3_notice`。放线程里是为了避免首帧绘制卡顿。

**`void T3_StartHeartbeat()`（L49-73）**
`g_t3_heartbeat_running.exchange(true)` 防重入。detach 线程内 **每 60 秒**（拆成 60×1s sleep 以便快速响应停止）调用一次 `g_t3_verify->heartbeat(kami, statecode)`。任一次心跳失败 → `g_t3_authed=false`、停心跳、状态置"心跳失败，请重新验证"。`kami`/`statecode` 取自互斥保护的全局量。

**`void T3_LoginAsync(const char* kami)`（L75-117）** — 核心登录流程
- 先 `T3_InitOnce()`，校验 ready / 非空卡密；`g_t3_verifying.exchange(true)` 防并发登录；
- detach 线程内调用 **`g_t3_verify->login(kamiStr, g_t3_machine_code)`**（机器码作为 imei 参数）；
- 成功：锁内保存 `g_t3_kami / g_t3_statecode / g_t3_end_time`，置"验证成功"，并**持久化卡密**（`g_kami_saved_valid=true; g_saved_kami=kamiStr; SaveAppConfigNow()`）供下次自动登录；然后 `g_t3_authed=true` 并 `T3_StartHeartbeat()`；
- 失败：若当前是"自动登录"且卡密等于已保存卡密，则清除已保存标记并落盘（避免坏卡密反复自动登录）；状态置"验证失败：..."。

**`void T3_TryAutoLoginFromConfig()`（L119-130）**
守卫：未尝试过、未认证、未在验证中；确保 `InitAppConfigOnce()` 已就绪；仅当 `g_kami_saved_valid && !g_saved_kami.empty()` 时，置"发现已保存卡密，尝试自动登录..."并调用 `T3_LoginAsync(g_saved_kami)`。用 `g_t3_auto_login_tried` 保证一次会话只自动登录一次。

**`void DrawT3AuthUI()`（L133-177）** — ImGui 登录界面
每帧调用（幂等地触发 `T3_InitOnce` + 自动登录）。要点：
- `ImGui::InputText("##pwd", kami[128])`，并与**自研输入法桥**联动：输入框被点击/激活且当前未请求文字输入时，调用 `ImeShowKeyboard(true)` 弹出软键盘（`g_ime_last_input_active / g_ime_last_want_text` 状态机）；
- "验证"按钮：空卡密报红字"请输入卡密。"，否则 `T3_LoginAsync(kami)`；
- 读取锁保护的 `g_t3_status`，若以"验证失败"开头（`rfind(...,0)==0`）则显示红字错误；验证中显示黄字"验证中..."；已认证显示到期时间 `g_t3_end_time`。

### 1.3 卡密校验的网络请求与加密（追溯到 `t3sdk.cpp`）

- 服务器：`#define T3_SERVER_URL "https://w.t3yanzheng.com/"`（官网 t3yanzheng.com，第三方"T3验证"云授权 SaaS）。
- `T3Verify::login`（t3sdk.cpp L844）参数 `{kami, imei, t=当前时间戳}`，经 `encodeParams()` 处理：
  1. 每个值用 `encodeValue()` → RSA 加密后转 hex（`rsaCrypto_->encryptToHex`）；
  2. 拼 `k1=v1&k2=v2...&<appkey>` 作签名串，`md5String()` 得签名，再 RSA 编码为参数 `s` 追加；
  3. `httpPost` → `httpPostRaw()`（L651）用 **libcurl** POST：`CURLOPT_SSL_VERIFYPEER=1 / VERIFYHOST=2`，且证书来自**内嵌 CA blob**（`CURLOPT_CAINFO_BLOB`，`cacert_data`），UA `"T3VerifyCpp/1.0"`，超时 15s/连接 8s；
  4. 响应 `decodeResponse()`：跳过首行、去 `\r\n`、去末尾一个 `0`，再 RSA/Base64 解码，`jsonGetInt(decoded,"code")` 判 200。
- 心跳、公告、版本走相同 code（`heartbeatCode_` 等）和相同签名/加密管线。

### 1.4 机器码生成（重要发现）
- SDK 提供 `getMachineCode()`（t3sdk.cpp L686）：Win 用 `GetAdaptersInfo`，mac 用 sysctl，**Android/Linux 分支用 `ioctl(SIOCGIFHWADDR)` 读 eth0/ens33/enp0s3 的 MAC**，取不到则 `00:00:00:00:00:00`，最终 `md5String(mac, true)`。
- **但 App 从不调用它**：登录传入的是全局 `g_t3_machine_code`，其在 `全局状态.cpp` L48 被**硬编码为常量字符串 `"android_inject_device"`**，全程未被 `getMachineCode()` 覆盖。→ 即所有设备上报同一机器码，**设备绑定实际被绕过/形同虚设**（对 CTF/自研逆向练习是明显可利用点）。Android 上 `SIOCGIFHWADDR` 读 eth0 本也多半失败，作者干脆写死。

---

## 2. 防录屏模块（反截屏/反录屏）

核心思路：安卓上给普通 `GLSurfaceView` 加 `FLAG_SECURE` 会导致自身黑屏；本模块改为**另建一个带 SECURE 标志的独立 Surface**，把 ImGui 菜单单独渲染到该安全层上——截屏/录屏工具捕获不到 SECURE 层，而普通游戏画面照常显示。

### 2.1 关键函数逐个说明（`防录屏.cpp`）

**`bool SetJavaSecureSurfaceMode(bool enable)`（L11-31）**
通过 **当前 ImGui ClassLoader**（`g_current_imgui_class_loader`，即真正创建 display/activity 的那份 `com.example.imgui.ImGui`）反射调用静态方法 `setSecureMode(Z)Z`。特意用同一 ClassLoader（注释 L13-15）避免重新 DexClassLoader 导致 static 字段不共享。是 UI 勾选开关的第一步（成功才真正切换渲染目标）。

**`jclass SecureGetClass(JNIEnv*, const char* className)`（L34-45）**
用全局 `g_secure_loader`（内存 dex 的 ClassLoader）反射 `ClassLoader.loadClass(String)` 取类。所有对 `com.mxp.SecureSurfaceHelper` 的访问都经此。

**`bool SecureLoadDex()`（L47-102）** — secure_dex 的加载（确认使用 secure_dex）
1. 已加载则直接返回；需要 `g_ime_jvm`、`ImeEnsureActivity()`、`secure_dex`、`secure_dex_len!=0`；
2. 取 Activity 的 `getClassLoader()` 作 parent；
3. `secure_dex` 字节 → `NewByteArray`+`SetByteArrayRegion` → `ByteBuffer.wrap` → **`dalvik/system/InMemoryDexClassLoader.<init>(ByteBuffer, ClassLoader)`**（内存加载，不落地磁盘），`NewGlobalRef` 存入 `g_secure_loader`；
4. 反射 `SecureSurfaceHelper.init(Object)Z`，传入 activity 完成初始化；
5. 全程细致 `DeleteLocalRef` + `ImeClearException`，末尾按 attach 状态 `DetachCurrentThread`，`g_secure_loaded=ok`。

**`ANativeWindow* SecureGetNativeWindow(int w,int h)`（L104-124）**
`SecureLoadDex()` 后，反射 `setProtectionEnabled(true)` 再 `getSurface(II)Ljava/lang/Object;` 拿到 Java Surface，`ANativeWindow_fromSurface(env, surface)` 转成 native window 返回。这一步产出的就是**带 SECURE 标志的原生窗口**。

**`void SecureSetEnabledOnly(bool enable)`（L126-140）**
只反射调用 `setProtectionEnabled(Z)`，用于开/关保护而不重建窗口（关闭/回退时用）。

**`void DestroySecureEglSurface()`（L142-170）**
销毁安全 EGL 资源：若当前 current 的是安全 surface 先 `eglMakeCurrent(EGL_NO_*)`，`eglDestroySurface`，全局句柄清零，`ANativeWindow_release`，`SecureSetEnabledOnly(false)`，最后**恢复原来的 EGL current**（保存/还原 old display/draw/read/context），避免破坏主渲染上下文。

**`EGLConfig FindCurrentEglConfig(EGLDisplay, EGLContext)`（L172-188）**
`eglQueryContext(EGL_CONFIG_ID)` 拿当前 context 的 config id，再 `eglGetConfigs` 遍历所有 config 匹配同 id 返回——保证安全 surface 与主 context 配置兼容，可共享 context 渲染。

**`bool EnsureSecureEglSurface(int w,int h)`（L190-236）**
惰性创建/复用安全 surface：若已有且 display/context/宽高/window 全部匹配则复用；否则 `DestroySecureEglSurface()` 后 `SecureGetNativeWindow` → `FindCurrentEglConfig` → **`eglCreateWindowSurface`**（attrs 仅 `EGL_NONE`）。成功后缓存全部句柄并置 `g_rendering_secure_surface=true`。任一步失败都会 release window + 关保护。

**`bool RenderDrawDataToSecureSurface(ImDrawData*)`（L238-261）** — 每帧安全渲染
1. 取宽高（优先全局 `screenWidth/Height`）→ `EnsureSecureEglSurface`；
2. 保存当前 EGL 状态，`eglMakeCurrent(display, 安全surface, 安全surface, 原context)` 切到安全层；
3. `glViewport` + `glClearColor(0,0,0,0)` 透明清屏 + `ImGui_ImplOpenGL3_RenderDrawData(drawData)` 把菜单画到安全层，`eglSwapBuffers`；
4. **切回原 surface**并恢复 viewport。即"菜单只在 SECURE 层可见"。

**`bool RebindNativeWindowForSecure(bool enable)`（L263-271）**
开：`EnsureSecureEglSurface`；关：`DestroySecureEglSurface()` + `g_rendering_secure_surface=false`。是切换开关时的落地函数。

> `防录屏.h` 声明了 7 个，但漏声明了 `SecureGetClass` 与 `SecureGetNativeWindow`（它们仅在 .cpp 内部使用，属实现细节）。

### 2.2 用到的 Android / 图形 API
- **JNI 反射**：`FindClass / GetStaticMethodID / CallStaticBooleanMethod / CallObjectMethod / NewGlobalRef`，异常清理 `ImeClearException`。
- **内存 DEX 加载**：`dalvik/system/InMemoryDexClassLoader` + `java/nio/ByteBuffer.wrap`（不落地、隐蔽）。
- **NativeWindow**：`ANativeWindow_fromSurface / _release`（`android/native_window_jni.h`）。
- **EGL/GLES3**：`eglGetCurrentDisplay/Surface/Context`、`eglQueryContext`、`eglGetConfigs`、`eglGetConfigAttrib`、`eglCreateWindowSurface`、`eglMakeCurrent`、`eglSwapBuffers`、`eglDestroySurface`；`glViewport/glClear`。
- **Java 侧真正的 FLAG_SECURE**：C++ 不直接调 `WindowManager.LayoutParams.FLAG_SECURE`；由内存 dex 里的 `com.mxp.SecureSurfaceHelper`（`init/setProtectionEnabled/getSurface`）与 `com.example.imgui.ImGui.setSecureMode` 在 Java 层完成加 SECURE 标志。`secure_dex` 即这份辅助类的编译产物（DEX 039 版，`secure_dex_len = 4584`）。

---

## 3. 关键数据结构 / 全局量 / 常量 / 硬编码地址

**T3 结果结构体（t3sdk.h）**：`T3Result{success,error,msg}`、`T3LoginResult{...end_time,statecode,imei,core...}`、`T3NoticeResult`、`T3VersionResult` 等一系列。

**卡密全局量（全局状态.h/.cpp）**：`g_t3_verify(T3Verify*)`、`g_t3_init_once`、原子标志 `g_t3_ready/authed/verifying/heartbeat_running`、`g_t3_mutex`、字符串 `g_t3_status/notice/end_time/kami/statecode`、`g_t3_machine_code`、`g_kami_saved_valid/g_saved_kami`、`g_t3_auto_login_tried/running`。

**防录屏全局量**：`g_current_imgui_class_loader(jobject)`、`g_secure_loader(jobject)`、`g_secure_loaded`、`g_pending_secure_switch/target`、`g_rendering_secure_surface`、`g_secure_native_window(ANativeWindow*)`、`g_secure_egl_display/surface/context`、`g_secure_surface_width/height`、`g_secure_skip_screenshot`（UI 勾选状态）。

**硬编码常量（全局状态.cpp）**——CTF 逆向可直接提取：
- 调用码 `T3_LOGIN_CODE="555F6E97AA87EACA"`、`T3_NOTICE_CODE="0E3C22A329179374"`、`T3_VERSION_CODE="CF78A3C231FBC7E2"`、`T3_HEARTBEAT_CODE="87A6E643DAED8CFF"`；
- `T3_APPKEY="10d0b15afbcf1091ec4b846601f7bd8e"`（32 位，参与 MD5 签名）；
- `T3_RSA_PUBLIC_KEY`：1024 位 RSA PEM 公钥（明文内嵌）；
- `LOCAL_VERSION="1000"`；机器码 `g_t3_machine_code="android_inject_device"`（写死）。
- `secure_dex[]`：4584 字节内联 DEX（`res/secure/secure_dex_data.h`）。

---

## 4. 关键技术与调用链

**卡密调用链**：`DrawT3AuthUI`(每帧,主界面绘制.cpp:288) → `T3_InitOnce`/`T3_TryAutoLoginFromConfig` → `T3_LoginAsync`(detach线程) → `T3Verify::login` → `encodeParams`(RSA+MD5签名) → `httpPost`→`httpPostRaw`(**libcurl HTTPS + 内嵌CA**) → `decodeResponse`(RSA解密+JSON) → 成功后 `SaveAppConfigNow` 持久化 + `T3_StartHeartbeat`(60s 周期心跳)。

**防录屏调用链**：UI 勾选"无权限防录屏"(主界面绘制.cpp:201) → `SetJavaSecureSurfaceMode`(反射 `ImGui.setSecureMode`) → 置 `g_pending_secure_switch` → 渲染循环 `imguijni.cpp:195` 消费该标志 → `RebindNativeWindowForSecure` → `SecureLoadDex`(**InMemoryDexClassLoader 加载 secure_dex**) → `SecureGetNativeWindow`(`SecureSurfaceHelper.getSurface`+`ANativeWindow_fromSurface`) → `EnsureSecureEglSurface`(`eglCreateWindowSurface`)。此后每帧 `imguijni.cpp:205` 见 `g_secure_skip_screenshot` 为真时：主 GLSurfaceView 只清透明、**不画 UI**，改由 `RenderDrawDataToSecureSurface` 把菜单画到 SECURE 层；失败即回退普通渲染以防界面全黑。

**涉及的库/技术**：libcurl(HTTPS)、自实现无 OpenSSL 依赖的 RSA + CustomBase64 + MD5、第三方 T3 云验证、JNI 反射、InMemoryDexClassLoader、ANativeWindow、EGL/GLES3、ImGui + `imgui_impl_opengl3`。未见 il2cpp/miniaudio 直接参与本组两模块（属项目其它模块）。

---

## 5. 与其它模块的依赖关系

**卡密验证.cpp include**：`全局状态.h`(全局量/常量/T3Verify 声明)、`配置初始化.h`(`InitAppConfigOnce/SaveAppConfigNow/g_app_config_ready`)、`输入法桥接.h`(`ImeShowKeyboard`、`g_ime_last_*`)、`imgui.h`。间接依赖 `t3sdk`。**被引用**：`主界面绘制.cpp:288` 调 `DrawT3AuthUI`；`主界面绘制.cpp:217` 用 `g_t3_authed/end_time` 显示到期时间。

**防录屏.cpp include**：`全局状态.h`、`输入法桥接.h`(`ImeGetEnv/ImeEnsureActivity/ImeClearException/g_ime_jvm/g_ime_activity`)、`res/secure/secure_dex_data.h`、`imgui_impl_opengl3.h`、`native_window_jni.h`、`GLES3/gl3.h`。**被引用**：`imguijni.cpp`(渲染循环调 `RebindNativeWindowForSecure/RenderDrawDataToSecureSurface/DestroySecureEglSurface/SecureSetEnabledOnly`)、`主界面绘制.cpp`(勾选框调 `SetJavaSecureSurfaceMode`)。两模块通过 `全局状态` 与 `输入法桥接` 高度耦合（共享 JVM/Activity/ClassLoader 句柄）。

---

## 6. 值得注意 / 风险 / 可疑点

1. **机器码形同虚设（最大发现）**：`getMachineCode()` 存在但从不被调用，登录始终上报写死的 `"android_inject_device"`。设备绑定被完全绕过——一张卡在任意设备/模拟器都判为同一机器；对 CTF 逆向而言这是明显后门/弱点。
2. **敏感常量全部明文内嵌**：appkey、4 个调用码、RSA 公钥、服务器地址均硬编码在 `全局状态.cpp`，`strings` 即可提取；配合已知 T3 验证协议，可离线复现签名/请求。
3. **卡密明文落盘**：登录成功后 `g_saved_kami` 经 `SaveAppConfigNow()` 持久化以便自动登录（未见加密，取决于 `配置初始化` 实现，值得复核落盘格式）。
4. **版本比较用字符串 `>`**：`vr.version > LOCAL_VERSION`（"1000"）是字典序比较，`"999">"1000"` 为真，位数不同会误判，属逻辑坑。
5. **心跳失败即掉线**：任一次心跳非成功就 `g_t3_authed=false` 并停心跳，弱网下易误踢；但不阻断游戏渲染主循环。
6. **防录屏靠 SECURE 层 + 内存 dex 反射**：技术上规避了给主 GLSurfaceView 加 FLAG_SECURE 导致自身黑屏的问题，思路是"游戏画面留普通层、菜单进 SECURE 层"。依赖 `com.mxp.SecureSurfaceHelper`（内存 dex，不落地，隐蔽性强，也难被静态扫描）。失败路径有完整回退，健壮性不错。
7. **ClassLoader 一致性是关键坑**（作者注释 L13-15）：`setSecureMode` 必须用创建 display/activity 的同一份 `com.example.imgui.ImGui` ClassLoader，否则 static 字段不共享导致开关无效——这是该功能能否生效的隐藏前提。
8. **curl 用内嵌 CA blob 且开启完整证书校验**（`VERIFYPEER=1/VERIFYHOST=2`）——安全性上是好实践，但也意味着抓包需替换内嵌 `cacert_data` 或 hook curl。

（可追溯锚点：登录 `卡密验证.cpp:75-117`；机器码写死 `全局状态.cpp:48`；curl `t3sdk.cpp:651-680`；login+签名 `t3sdk.cpp:813-867`；secure dex 加载 `防录屏.cpp:47-102`；安全层渲染 `防录屏.cpp:238-261`；渲染循环接管 `imguijni.cpp:195-224`；UI 开关 `主界面绘制.cpp:201-208`。）


---

## 模块：`audio-music`

我已完整读取全部四个文件（音频标签页.cpp 95 行、音频标签页.h 2 行、MusicPlayer.cpp 931 行、MusicPlayer.h 25 行），并追溯了跨模块引用。以下是通读记录。

---

# 音乐播放器模块通读技术笔记

## 0. 模块定位（重要总体判断）

这一组文件是整个游戏菜单里**唯一一个与游戏本体完全无关**的自包含子系统：一个联网点播的网易云音乐播放器 + 音频可视化面板，嵌在 ImGui 主界面的「音频播放」标签页里。**本模块内没有任何 hook / JNI / il2cpp / OpenGL / 内存偏移 / 目标进程读写**——它纯粹是 miniaudio（解码播放）+ libcurl（联网）+ nlohmann/json（解析）+ ImGui（UI）+ 手写 FFT（频谱/鼓点）。所谓“逆向能力测试”跟这个模块无关，它更像作者塞进菜单里的一个娱乐/炫技功能。

调用入口链：
`主界面绘制.cpp:228` 在 `BeginMainTabItem("音频播放", 4)` 内调用 → `音频标签页.cpp:DrawUploadedUIInsideAudioTab()` → `音乐窗口()`（MusicPlayer.cpp:818）→ 首帧惰性 `InitMusicPlayer()` 拉起后台线程。

---

## 1. 各文件作用与规模

| 文件 | 行数 | 作用 |
|---|---|---|
| `jni/res/music/MusicPlayer.h` | 25 | 对外接口头。定义 `Song`、`PlayState` 枚举、`Playlist` 结构；`extern` 声明 `g_Playlist / g_SongList / g_BeatIntensity`；导出 `音乐窗口()`、`CleanupMusicPlayer()` |
| `jni/res/music/MusicPlayer.cpp` | 931 | 核心实现。全局状态、FFT、音频回调、miniaudio 设备/解码、curl 网络层、oiapi 搜索/取播放地址、播放控制状态机、播放/可视化/搜索三条线程、频谱与进度绘制、`音乐窗口()` 主 UI |
| `jni/res/音频标签页.cpp` | 95 | 标签页外壳 UI。节奏联动的彩色“雪花”粒子背景 + 左侧「音乐/设置」二级导航；「设置」页把播放模式映射到 shuffle/repeat；「音乐」页转调 `音乐窗口()` |
| `jni/res/音频标签页.h` | 2 | 只导出 `void DrawUploadedUIInsideAudioTab();` |

`MusicPlayer.cpp:1-2` 用 `#define MINIAUDIO_IMPLEMENTATION` 和 `#define CACERT_DATA_IMPLEMENTATION` 在本 TU 里实体化 miniaudio 与内嵌 CA 证书数据——所以这是 miniaudio 的唯一实现单元。

---

## 2. 关键数据结构 / 全局变量 / 枚举 / 常量

### 数据结构（MusicPlayer.h）
- `struct Song { std::string name, id, picurl, singers, jumpurl; }` — 一首歌的元数据。
- `enum class PlayState { Idle, Loading, Playing, Paused }` — 播放状态机四态。
- `struct Playlist { vector<Song> songs; int currentIndex=-1; PlayState state=Idle; float progress=0, duration=0; bool shuffle=false, repeat=false; }`

### 全局变量（MusicPlayer.cpp:26-75）
- 数据：`g_SongList`（搜索结果，UI 展示源）、`g_Playlist`（实际播放队列）、`g_CurrentSong`、`g_CurrentSongId`、`g_StatusMsg/g_ErrorMsg`。
- 三把锁：`g_StatusMutex`、`g_SongListMutex`、`g_AudioVisualMutex`（保护 `g_AudioBuf/g_Waveform/g_Spectrum`）。
- miniaudio 句柄：`static ma_decoder g_AudioDecoder`、`static ma_device g_AudioDevice`，配 `g_DecoderReady/g_DeviceReady` 两个就绪标志。
- **原子播放控制标志**（作者特意注释：这些标志被 UI/播放/音频回调三线程同时访问，`volatile` 无同步语义故改 `std::atomic`，见 42-51 行）：`g_ThreadRun / g_IsPlaying / g_DeviceRunning / g_PendingIndex(int) / g_RequestNext / g_RequestPrev / g_RequestPause / g_RequestResume / g_AutoNext / g_PlayNextFromNew`。
- 内存音频：`std::vector<uint8_t> g_AudioData`（**整首歌下载后驻留内存**）；`g_StartTime`、`g_PausedPos`、`g_PausedFrame`（暂停位置记录）。
- 可视化数据：`g_Spectrum(128)`、`g_Waveform(256)`、`g_AudioBuf`、`g_Window`（FFT 窗）。
- 线程对象：`g_PlayThread / g_VisualThread / g_SearchThread`；`g_Searching`、`g_CurlInit`、`g_CurlInitOnce`、`g_MusicPlayerInit`。

### 关键常量
- `FFT_SIZE = 8192`，`BANDS = 128`（60 行）；波形点数 256；`g_SpectrumGain = 2.0f`（频谱高度放大）。
- 采样参数**硬编码 44100 Hz / 2 声道 / f32**（贯穿 InitDevice、解码 config、鼓点计时、时长计算）。
- 鼓点历史窗 `bandHistory[3][43]`——3 频段各 43 帧（≈1 秒），`historyIdx % 43`。
- 雪花粒子固定 **250** 个（音频标签页.cpp）。
- **无任何内存偏移/绝对地址/il2cpp RVA**——本模块不涉及目标进程。

---

## 3. 关键函数逐个说明（签名 + 实现要点）

### 3.1 网络层（curl）
- `void InitCurl()` (376)：`std::call_once(g_CurlInitOnce, ...)` 保证 `curl_global_init(CURL_GLOBAL_DEFAULT)` 只跑一次。
- `void ApplyCurlCommonOptions(CURL*, long timeout)` (383)：统一设 `FOLLOWLOCATION`、超时、`CONNECTTIMEOUT=8s`、`UserAgent="Mozilla/5.0"`、开启 `SSL_VERIFYPEER=1 / VERIFYHOST=2`，并用**内嵌 CA 证书 blob**（`cacert_data / cacert_data_len`，`CURL_BLOB_NOCOPY`，来自 `curl/证书.h`）经 `CURLOPT_CAINFO_BLOB` 注入——即不依赖系统证书链，自带根证书。
- `WriteStr / WriteVec` (395/400)：curl 写回调，分别追加到 `std::string` 和 `std::vector<uint8_t>`。
- `std::string HttpGet(url)` (407)：GET 到 string，超时 10s，仅当 HTTP 200 返回正文否则空串。
- `std::vector<uint8_t> Download(url)` (424)：下载二进制到 vector，超时 30s，失败清空。
- `std::string UrlEncode(v)` (439)：`curl_easy_escape` 做 URL 编码。

### 3.2 API 层（第三方 oiapi.net，网易云 163）
- `void DoSearch(const std::string& kw)` (451)：URL = `https://oiapi.net/API/Music_163?name=<UrlEncode(kw)>&limit=50`。`json::parse(..., nullptr, false)` 容错解析，校验 `code==0` 且 `data` 为数组；逐条构造 `Song`（`id` 可能是数字或字符串两种情况都处理，`singers` 数组拼成逗号分隔字符串）。加 `g_SongListMutex` 锁把结果 move 进 `g_SongList` 并复制给 `g_Playlist.songs`；**若当前正在播放**则置 `g_PlayNextFromNew=true`、`currentIndex=-1`（当前歌放完后切到新列表第 0 首），否则直接 Idle。
- `void StartSearchAsync(kw)` (504)：`g_Searching.exchange(true)` 做去重（正在搜索则拒绝），join 旧搜索线程后新起 `g_SearchThread` 跑 `DoSearch`。
- `std::string GetPlayUrl(const std::string& id)` (518)：GET `https://oiapi.net/API/Music_163?id=<id>`，取 `data[0].url` 作为真实音频直链。

### 3.3 播放控制状态机
- `void SafeStop()` (530)：置 `g_IsPlaying/g_DeviceRunning=false`，**sleep 50ms** 等回调退出（粗糙同步），再 `ma_device_stop/uninit`、`ma_decoder_uninit`，清 `g_AudioData` 与暂停位。
- `int GetNextIdx()` (541) / `int GetPrevIdx()` (552)：算下一/上一索引；`shuffle` 时 `rand()%size` 随机；越界时按 `repeat` 决定回绕还是返回 -1（结束）；`g_PlayNextFromNew` 优先返回 0。
- `bool PlaySong(int idx)` (561)：核心加载流程——`SafeStop()` → 置 Loading → `GetPlayUrl` 取直链 → `Download` **整首下载进内存** → `ma_decoder_init_memory` 从内存解码（目标 f32/2/44100，miniaudio 内部重采样）→ `InitDevice()` → `ma_device_start` → 置 Playing、记 `g_StartTime`。任一步失败回退 Idle 并 `SetStatus(..., true)`。
- `void PlayThread()` (601)：**后台事件循环主体**。50ms 轮询各标志：处理 Pause（记 `g_PausedFrame` + 累加 `g_PausedPos`）、Resume（`ma_decoder_seek_to_pcm_frame`）、确定播放索引（AutoNext / Next / Prev / PendingIndex 四来源），调 `PlaySong`；成功后进入内层播放循环（100ms 轮询，支持暂停挂起、切歌打断、进度更新），失败则 `g_AutoNext=true` 跳下一首。
- `bool InitDevice()` (363)：`ma_device_config_init(playback)`，f32/2ch/44100，`dataCallback = AudioCallback`。

### 3.4 音频回调（实时线程，AudioCallback，167）
签名 `void AudioCallback(ma_device*, void* out, const void*, ma_uint32 n)`：
- 未就绪或 `n<=4` 直接输出静音。
- **零分配技巧**：用 `thread_local` 复用 `samplesBuffer/mono`，避免每帧 new/delete。
- `ma_decoder_read_pcm_frames` 读 PCM；`framesRead < n` → 判定播放到尾，置 `g_AutoNext`。
- 混单声道 `mono[i]=(L+R)*0.5`。
- **可视化不阻塞**：`std::try_to_lock` 抢 `g_AudioVisualMutex`，抢不到就跳过本帧波形/缓冲更新（优先保证不卡音频），作者有明确注释。
- **多频段鼓点检测（204-338，本文件最精巧处）**：滑窗（window=4）分离低频（长窗均值=底鼓）/中频（`|mono[i]-mono[i-2]|`=军鼓+人声）/高频（`|mono[i]-mono[i-1]|`=镲片）；各频段用 `x*0.99+cur*0.01` 慢速归一化基准做自适应归一；单独做“人声检测”启发式（中频能量高且变化率小 → 人声，幅度封顶 0.15）；`drumEnergy=low*0.5+mid*0.3+high*0.2`；用 43 帧历史最大值 × 0.4 作动态阈值；`timeSinceLastBeat>0.1s` 去抖，分低/中/高三档判 beat 并给不同强度权重；无 beat 时 `beatIntensityCache*=0.92` 衰减、叠加微量人声；结果 clamp 到 [0,1] 存入 `g_BeatIntensity`（供雪花与频谱联动）。
- 累积 `mono` 到 `g_AudioBuf`（超 `FFT_SIZE*4` 截断保留末尾 `FFT_SIZE*2`）。
- 用 `ma_decoder_get_cursor/length_in_pcm_frames` 更新 `g_Playlist.progress/duration`，`progress>=0.999` 也触发 `g_AutoNext`。

### 3.5 FFT / 可视化
- `void InitWindow()` (99)：预算 8192 点 **Hann 窗**。
- `void ComputeFFT(in, out)` (105)：**手写迭代基-2 FFT**——补零到 2 的幂、位反转重排、蝶形运算、取幅度并按最大值归一化、再映射到 128 个**对数频带**（`expf(b*bw)`）、最后 3 点邻域平滑。
- `void VisualThread()` (728)：16ms 循环，从 `g_AudioBuf` 尾取 `FFT_SIZE` 样本算频谱，`g_Spectrum[i]=旧*0.7+新*0.3` 时间平滑。
- `void DrawSpectrum(pos, size)` (748)：快照 `g_Spectrum` 后，**自适应满宽**画 128 条柱（早期是固定 128×5 只占左侧，已改），按频带索引 t 做蓝→青→绿→红渐变。
- `void DrawProgress(f, p, s, r, c)` (786)：底槽 + 前景进度条 + 边框。

### 3.6 生命周期
- `void InitMusicPlayer()` (794)：惰性初始化（`g_MusicPlayerInit` 守卫），`srand`，启动 `g_VisualThread` 与 `g_PlayThread`。由 `音乐窗口()` 首帧触发。
- `void CleanupMusicPlayer()` (804)：置各线程停止标志、join 三线程、`SafeStop()`。**故意不调用 `curl_global_cleanup`**（注释解释：避免二次进入音乐页时 `call_once` 不再重初始化）。

### 3.7 主 UI：`void 音乐窗口()` (818)
搜索框（`InputTextWithHint` + 「搜索」按钮 → `StartSearchAsync`）；状态行（Loading/错误红/正常绿）；三列表格（歌曲/歌手/操作，Loading 时禁用按钮，当前播放行高亮绿字），「播放##i」按钮 → `RequestPlay(i)`；底部 130px 控制区：曲名/`FormatTime` 计时、`|< / (||或>) / >|` 三按钮（PrevSong/PlayPause/NextSong）、`DrawSpectrum` 律动横条、`DrawProgress` 进度条。注意时长缺省用 **215** 兜底。

### 3.8 公共接口
`RequestPlay(idx)`(701) 置 `g_PendingIndex`；`PlayPause()`(707) 依状态发 Pause/Resume 或从当前索引起播；`NextSong/PrevSong`(713/714) 置请求标志；`StopPlayback()`(716) 全停并 Idle。

### 3.9 音频标签页.cpp（外壳 + 雪花）
- `AudioTabGetRandomColor / AudioTabInitSnow(250) / AudioTabDrawSnow`：250 个彩色雪花粒子，读 `g_BeatIntensity` 做节奏联动——`smoothBeat` 平滑（上升瞬时跟随、下降 0.92 衰减）、`basePulseScale=1+beat*0.9`、每粒子有随机 `individualFactor` 个性、按 y 深度 `depthFactor` 缩放，`snowState` 一阶低通逼近目标缩放，最终 clamp [0.8,2.2]。`AddCircleFilled` 绘制。若粒子数 ≠ 250 会重新初始化。
- `DrawUploadedUIInsideAudioTab()` (71)：外框 BeginChild + 裁剪 + 画雪花背景；左面板「音乐/设置」两个 80px 大按钮切 `page`；右面板 page==2 时是**播放模式设置**（4 个 RadioButton 映射到 `g_Playlist.shuffle/repeat`），否则调 `音乐窗口()`。

---

## 4. 用到的关键技术与调用链

- **miniaudio**：`ma_decoder_init_memory`（内存解码，非流式）+ `ma_device`（playback 回调驱动）+ `ma_decoder_seek/get_cursor/get_length_in_pcm_frames`（进度/暂停）。
- **libcurl**：easy 接口 + 内嵌 CA blob（`CURLOPT_CAINFO_BLOB`）+ `curl_easy_escape`。
- **nlohmann/json**：容错 `parse(...,false)` + `is_discarded/is_array/value()` 防御式取字段。
- **手写 FFT**（基-2 迭代 + Hann 窗 + 对数频带）驱动频谱；**自研多频段鼓点检测**驱动 `g_BeatIntensity`。
- **多线程 + 原子标志状态机**：UI 线程发请求 → `g_PlayThread` 消费执行 → `AudioCallback`（miniaudio 内部实时线程）产数据 → `g_VisualThread` 消费算频谱。三把 mutex + 一组 atomic 协调。
- 典型链路：UI 点「搜索」→ `StartSearchAsync` → `g_SearchThread:DoSearch` → oiapi → 填 `g_SongList`；UI 点「播放##i」→ `RequestPlay` 置 `g_PendingIndex` → `PlayThread` → `PlaySong` → `GetPlayUrl`+`Download`+解码+`ma_device_start` → `AudioCallback` 出声并产生 `g_BeatIntensity`/`g_AudioBuf` → 雪花 + 频谱联动。

---

## 5. 模块依赖关系

**引用（被本组包含）**：miniaudio、ImGui（含 imgui_internal）、curl + `curl/证书.h`（内嵌 CA）、nlohmann/json；`音频标签页.cpp` 额外 include `全局状态.h`（用于外壳，但 `g_BeatIntensity/g_Playlist` 实际来自 `MusicPlayer.h`，`全局状态.h` 内不含这些符号，已核对）。

**被引用**：
- `MusicPlayer.h`：由 `音频标签页.cpp` 和 `MusicPlayer.cpp` 引用。
- `DrawUploadedUIInsideAudioTab()`：唯一调用点 `主界面绘制.cpp:228`（第 4 号标签「音频播放」）。
- `g_Playlist / g_SongList / g_BeatIntensity`：`extern` 于 `MusicPlayer.h`，被 `音频标签页.cpp` 消费（播放模式、雪花律动）。
- 全项目匹配到 5 个相关文件，`MusicPlayer` 与音乐播放逻辑不被游戏 hook 层引用，确认为独立子系统。

---

## 6. 值得注意的点（技巧 / 坑 / 硬编码 / 风险）

1. **硬编码第三方 API `oiapi.net/API/Music_163`（版权/可用性风险）**：搜索与取直链都依赖这个公共第三方接口去拉网易云音乐资源。接口随时可能下线/限流/改协议导致整个播放器失效；且经第三方拉取受版权保护的音乐存在合规问题。这是本模块最大的外部脆弱点。
2. **`CleanupMusicPlayer()` 从未被调用**（全项目仅有定义与声明，无调用点）。意味着进程退出时播放/可视化/搜索三线程与 miniaudio 设备**不做优雅关闭**，属资源泄漏 / 潜在退出崩溃隐患。
3. **`g_Playlist` 的跨线程读写未完全加锁**：`DoSearch` 在 `g_SongListMutex` 下替换 `g_Playlist.songs`，但 `PlayThread/GetNextIdx/GetPrevIdx` 读 `g_Playlist.songs` 时**并未持同一把锁**——搜索与切歌并发时存在对 `vector` 的数据竞争。作者对线程问题有意识（把标志改成 atomic 并写了注释），但这条竞争仍在。
4. **整首下载进内存再解码**（`Download`→`g_AudioData`→`ma_decoder_init_memory`），非流式：大文件占内存、且下载阻塞在 `PlayThread` 上，切歌到出声有明显延迟（Loading 态期间 UI 禁用播放按钮以缓解）。
5. **采样率 44100 全程硬编码**：鼓点计时 `framesRead/44100`、`duration=len/44100` 都假定 44100；若解码目标改变会失真。设备/解码都强制重采样到 44100 以自洽。
6. **`SafeStop()` 用 sleep 50ms 等回调退出**——靠时间等待而非真正同步，理论上仍可能与回调竞态（虽有 `g_DeviceRunning/g_IsPlaying` 前置判断兜底）。
7. **播放模式标签与语义不符（逻辑坑）**：设置页 4 个单选把「循环单曲」映射为 `shuffle=false, repeat=true`、「循环列表」映射为 `shuffle=true, repeat=true`。但代码里 `repeat` 只是列表播完回绕，**并无“单曲循环”逻辑**，且「循环列表」被错配成了随机+回绕。UI 文案与实际行为存在偏差。
8. **`FormatTime` 返回 `char[16]` 局部数组**（隐式转 `std::string`，安全）；时长缺省兜底 `215` 秒是个魔法数。
9. 安全做法可取的部分：curl 开启了 `SSL_VERIFYPEER/VERIFYHOST` 且自带 CA blob（不弱化 TLS）；`json::parse` 全程容错解析防崩；音频回调用 `thread_local` 零分配 + `try_to_lock` 不阻塞实时线程——这几处工程质量较好。
10. **雪花粒子数 250 为魔法数**且散落在多处（`AudioTabDrawSnow` 里用 `size()!=250` 判重建、`individualFactor[250]/snowState[250]` 静态数组），改数量需多处同步，易漏。


---

## 模块：`imcoolbar`

我已完整读取两个文件，并确认了它们在项目中的唯一引用点。以下是通读记录。

---

# ImCoolBar 组 —— 通读技术笔记

## 0. 定位速览

ImCoolBar 是一个**纯 ImGui 即时模式 UI 插件**（macOS Dock 风格悬浮工具条），原作者 Stephane Cuillerdier (Aiekick)，本项目作者 NewYaroslav 在 2025 年做了二次修改。**整组代码不含任何 hook / JNI / il2cpp / OpenGL / miniaudio / curl / 硬编码游戏偏移**，只依赖 `imgui.h` 与 `imgui_internal.h`。它在本项目里只是"另一个窗口"里的一个装饰性 Demo 组件，并非真正的功能快捷菜单（详见 §5）。

---

## 1. 文件作用与规模

| 文件 | 行数 | 作用 |
|---|---|---|
| `/home/user/-/jni/res/ImCoolBar.h` | 108 行 | 公开 API 声明。定义 `ImCoolBarFlags` 位标志枚举、`ImCoolBarConfig` 配置结构体、以及 `ImGui::` 命名空间下 7 个导出函数原型。 |
| `/home/user/-/jni/res/ImCoolBar.cpp` | 568 行 | 全部实现。含匿名命名空间的 7 个静态辅助函数 + 6 个导出函数实现。 |

注意：头文件版权写 2024，cpp 写 2023，作者信息不一致（无实际影响）。

---

## 2. 关键函数 / 类：签名 + 实现要点

### 2.1 匿名命名空间辅助函数（cpp 63–107）

**`bubbleEffect(vValue, vStength)`**（65–67）—— 放大动画的核心曲线
```cpp
return pow(cos(vValue * IM_PI * vStength), 12.0f);
```
- 输入 `vValue` 是"鼠标到该按钮中心的归一化距离"，`vStength` 是 `effect_strength`。
- `cos(...)` 在鼠标正对按钮中心（vValue=0）时为 1，随距离增大衰减；`^12` 次幂把峰值削得很尖锐 → 只有鼠标正下方的按钮明显放大，两侧快速回落，形成 Dock 的"气泡凸起"效果。返回 [0,1]。

**`getHoverSize(vValue, vNormalSize, vHoveredSize, vStength, vScale)`**（69–71）
```cpp
return ImClamp(vNormalSize + (vHoveredSize - vNormalSize) * bubbleEffect(...) * vScale,
               vNormalSize, vHoveredSize);
```
- 由气泡系数在 `normal_size` 与 `hovered_size` 之间线性插值出单个按钮的实际尺寸，再乘 `vScale`（整条 bar 的 0→1 展开动画进度），最后 clamp 兜底。

**`isWindowHovered(vWindow)`**（73–75）：`IsMouseHoveringRect(window->Rect().Min, Max)`，判断鼠标是否在整条 bar 的矩形内 → 决定 bar 整体是否展开。

**`getBarSize(vNormalSize, vHoveredSize, vScale)`**（77–79）：`normal + (hovered-normal)*scale`，算 bar 在**交叉轴（厚度方向）**的当前尺寸，随展开动画 `anim_scale` 变化。

**`getChannel` / `getChannelInv`**（81–93）：方向解耦工具。`getChannel` 取**主轴**分量（Horizontal 取 `.x`，否则 `.y`），`getChannelInv` 取**交叉轴**分量。整套算法靠这两个函数实现水平/垂直两种布局共用一份代码。

**`emaReset(id, value)` / `emaUpdate(id, sample, alpha)`**（95–105）：基于 `ImGuiStorage`（按 `ImGuiID` 键）实现的指数移动平均滤波器。`emaUpdate` 做 `current += alpha*(sample-current)` 并写回。用于鼠标坐标平滑与动画平滑，**状态存在 ImGui storage 里而非静态全局变量**——这让插件天然支持多实例、可重入。

### 2.2 `ImGui::BeginCoolBar`（109–311）
```cpp
bool BeginCoolBar(const char* vLabel, ImCoolBarFlags vCBFlags,
                  const ImCoolBarConfig& vConfig, ImGuiWindowFlags vFlags);
```
实现要点，按顺序：
1. **两种模式**（113–135）：
   - `embed_mode=false`：自己 `ImGui::Begin` 一个无标题栏/无滚动条/自动 resize/不可移动/不保存设置/不抢焦点的窗口，非调试时加 `NoBackground`；失败即 `End` 返回 false。
   - `embed_mode=true`：不开新窗口，直接画在父窗口里，只检查 `SkipItems`。
2. `IM_ASSERT`（137–140）强制只能设 Horizontal 或 Vertical 之一。
3. **写一堆 StateStorage 键**（前缀 `"ICB"`，见 §3）：类型标记、ItemIdx 清零、Flags、Anchor、NormalSize、HoveredSize、EffectStrength 等。
4. embed 模式额外记录 `EmbedBaseX/Y`（当前光标）、`EmbedTotalWidth=0`、`EmbedRefSize`（首帧取内容区尺寸，≤0 退化为 200）（147–158）。
5. **局部抗锯齿**（160–194）：保存当前 `ImDrawList->Flags` 到 `PrevDLFlags`，强制打开 `AntiAliasedFill|AntiAliasedLines`；若 `frame_rounding_override>=0` 则 `PushStyleVar(FrameRounding)` 并记标志位以便 End 时 Pop。
6. **anim_scale 时间基 EMA 平滑**（207–234）：目标值 = 悬停则 1 否则 0。半衰期公式 `alpha = 1 - exp(-ln2 * dt_ms/anim_smoothing_ms)`（`ln2 = 0.69314718056`），使动画帧率无关；若 `anim_smoothing_ms<=0` 退回固定步进 `anim_step`。
7. **鼠标平滑 EMA 设置 + 更新**（236–285）：`dt_ms` 截断到 100ms 上限防跳变；`AppFocusLost` 或鼠标坐标无效时置 reseed，下一帧用 `emaReset` 重新播种，避免焦点切回时的跳变。滤波后主轴坐标存 `LastMousePos`。
8. **窗口定位**（287–305，仅非 embed）：`bar_size = ContentSize + WindowPadding*2`，交叉轴用 `getBarSize` 替换成动画厚度；按 `anchor` 定位 `new_pos = vp->Pos + (vp->Size - bar_size) * vConfig.anchor`；可选 `ImFloor` 对齐整数像素。

### 2.3 `ImGui::EndCoolBar`（313–359）
- 恢复之前保存的 DrawList Flags；若之前 push 过 FrameRounding 则 Pop。
- **embed 模式**（332–353）：把累计的 `EmbedTotalWidth` 回写成下一帧的 `EmbedRefSize`；按 `bar_thickness` 推进父窗口光标（水平推 Y、垂直推 X）；末尾 `ImGui::Dummy(ImVec2(0,0))`——这是**本项目作者加的本地补丁**（带中文注释 `// >>> FIX: 提交一个零尺寸 Dummy 项目，消除 ImGui 的 SetCursorPos 警告`），用来吞掉 SetCursorPos 触发的告警。
- 非 embed 模式调用 `ImGui::End()`。

### 2.4 `ImGui::CoolBarItem`（361–449）—— 每个按钮前调用
```cpp
bool CoolBarItem();
```
- 取当前 item 序号 `idx`，用 `GetID(window->ID + idx + 1)` 作为该 item 的 storage 键（**用 ImGuiID 算术拼 key**，稍 hacky 但有效）。
- 水平布局且非首项 → `SameLine()`。
- 若 `anim_scale>0`（bar 已展开）：
  - 取按钮当前屏幕坐标主轴分量 `csp`，算中心 `btn_center = csp + size*0.5`；
  - `diff_pos = (last_mouse_pos - btn_center) / ref_size`（`ref_size` embed 用 `EmbedRefSize`，否则用窗口主轴尺寸）；
  - `current_size = getHoverSize(diff_pos, ...)` → 得到该按钮放大后的尺寸；
  - **交叉轴居中**：`btn_offset = (bar_height - current_size) * anchor + (padding 或 embed_base)`，可选 `ImFloor`，然后水平设 `SetCursorPosY`、垂直设 `SetCursorPosX`，让放大的按钮在厚度方向按 anchor 对齐（0.5 即居中）。
- `BREAK_ON_KEY(ImGuiKey_D)`：调试断点宏（非 MSVC 下 `ICB_DEBUG_BREAK` 为空，等于空操作，但仍会每项每帧调一次 `IsKeyPressed(D)`，属遗留调试代码）。
- 写回 item 尺寸、`ItemCurrentSize`、`ItemCurrentScale = current_size/normal_size`；embed 模式累加 `EmbedTotalWidth`。

### 2.5 `GetCoolBarItemWidth` / `GetCoolBarItemScale`（451–468）
分别读 `ItemCurrentSize` / `ItemCurrentScale`。调用方在 `CoolBarItem()` 为真的分支里用返回的宽度画等宽等高按钮（本项目就是 `ImGui::Button(char, ImVec2(w,w))`）。

### 2.6 `ShowCoolBarMetrics(bool* vOpened)`（470–568）—— 调试面板
遍历 `g.Windows`，凭 StateStorage 里的 `"ICB""Type" == "ImCoolBar"` 标记筛出所有 CoolBar 实例，用 `TreeNode` + `BeginTable` 打印 MaxIdx、Anchor、AnimScale、NormalSize、HoveredSize、EffectStrength、当前 item 尺寸/缩放、两组 EMA 半衰期与 alpha、Flags、EmbedMode，以及逐 item 的尺寸。内部用局部宏 `SetColumnLabel` 简化两列表格填充。

---

## 3. 关键数据结构 / 全局量 / 常量

**`ImCoolBarConfig`（h 44–86）** —— 全部配置项及默认值：
`anchor(-1,-1)`、`normal_size(40)`、`hovered_size(60)`、`anim_step(0.15)`、`effect_strength(0.5)`、`mouse_smoothing_ms(50)`、`anim_smoothing_ms(50)`、`snap_window_to_pixels(true)`、`snap_items_to_pixels(true)`、`local_antialiasing(true)`、`frame_rounding_override(-1)`、`embed_mode(false)`。

**枚举 `ImCoolBarFlags_`（h 37–41）**：`None=0`、`Vertical=1<<0`、`Horizontal=1<<1`。

**无传统全局变量**——所有运行时状态都挂在 `window->StateStorage`（ImGuiStorage），键统一用 `GetID("ICB"+后缀)`。全部键名：`EmbedMode / EmbedBaseX / EmbedBaseY / EmbedTotalWidth / EmbedRefSize / PrevDLFlags / PrevDLFlagsSet / PushedRounding / SnapItemsToPixels / SnapWindowToPixels / Type / ItemIdx / Flags / Anchor / NormalSize / HoveredSize / EffectStrength / AnimScale / AnimSmoothingMs / AnimSmoothingAlpha / MouseSmoothingMs / MouseSmoothingAlpha / MouseSmoothingInit / MouseReseedPending / MouseSmoothing / LastMousePos / ItemCurrentSize / ItemCurrentScale`，加每 item 的 `GetID(window->ID+idx+1)`。

**常量 / 魔数**：`ICB_PREFIX="ICB"`（47）；`ln2=0.69314718056f`（219、250）；气泡幂次 `12.0f`（66）；鼠标 dt 上限 `100.0f` ms（249）；`ref_size` 兜底 `200.0f`（155）与 `normal_size*5.0f`（400）。

**编译期宏**：`IMCOOLBAR_HAS_DOCKING`（跟随 `IMGUI_HAS_DOCK`）、`ICB_DOCKING_HOST_FLAGS`、`ENABLE_IMCOOLBAR_DEBUG`（**已注释关闭**，开启会显示 bar 背景）、MSVC 专用 `ICB_DEBUG_BREAK`/`BREAK_ON_KEY`。

**内存偏移 / 地址**：本组无任何硬编码地址或结构偏移——纯 UI，与逆向目标进程无直接耦合。

---

## 4. 用到的关键技术与调用链

- **技术栈仅为 ImGui**：`imgui.h` + `imgui_internal.h`（用到 `GImGui`、`ImGuiWindow`、`StateStorage`、`GetCurrentWindow`、`ImDrawList` 内部结构），`<cmath>`/`<vector>`/`<array>`。**不涉及 hook / JNI / il2cpp / OpenGL / miniaudio / curl**。
- **状态管理技巧**：完全用 ImGuiStorage 存帧间状态，实现无全局变量、可多实例、可重入。
- **帧率无关平滑**：两处半衰期 EMA（鼠标、动画）用 `1 - exp(-ln2·dt/halflife)` 换算 alpha，配合 dt 截断与 reseed 防抖。
- **方向抽象**：`getChannel/getChannelInv` 让水平/垂直共用同一套布局与放大算法。
- **调用链**：`BeginCoolBar`（建窗/写状态/算位置）→ 循环 `CoolBarItem`（算每项放大与居中偏移）→ 调用方画控件（Button）→ `EndCoolBar`（还原状态/推进光标或 End）。可选 `ShowCoolBarMetrics` 旁路调试。

---

## 5. 与项目其它模块的依赖关系

- **引用者（唯一）**：`/home/user/-/jni/res/主界面绘制.cpp`，仅在 `g_show_another_window`（"另一个窗口"）分支内使用（333–353）：
  - 用 `embed_mode=true`、`Horizontal`、`anchor(0.5,0.5)`、`normal=40/hovered=60`、`mouse_smoothing=80ms`、`anim_smoothing=100ms`、`frame_rounding_override=6` 配置；
  - 循环 6 次画按钮，标签为 `static const char* happy_chars[] = {"要","天","天","开","心","呀"}`（即"要天天开心呀"），每个按钮 `ImGui::Button(char, ImVec2(w,w))`。
  - **结论**：在本项目里它是**装饰性 / 彩蛋性质的 Dock 演示条**，并未绑定任何游戏功能或菜单跳转——不是功能快捷入口。周围真正的功能开关（吐球加速、视野修改等）走的是普通 ImGui 控件 + `GameCall_*` 调用，与 CoolBar 无关。
- **被依赖**：只依赖 ImGui；不被除主界面绘制外的任何模块引用。所在窗口用的是项目自研的 `ImGenie::Begin/End` 包装（另一模块），CoolBar 本身不感知它。

---

## 6. 值得注意的点（技巧 / 坑 / 风险）

1. **本地补丁**：`EndCoolBar` 尾部的 `Dummy(0,0)` 是项目方加的（带中文 `// FIX` 注释），用于压制 embed 模式下 SetCursorPos 的 ImGui 告警——上游没有，升级插件时需保留。
2. **遗留调试代码**：`CoolBarItem` 里的 `BREAK_ON_KEY(ImGuiKey_D)`。非 MSVC 下 `ICB_DEBUG_BREAK` 展开为空，语句变成 `if (IsKeyPressed(D)) ;`——无副作用，但每项每帧仍空跑一次 `IsKeyPressed`，且按 D 在 MSVC 调试器下会触发 `__debugbreak`。属可清理项。
3. **anchor 的双重用途**：`Begin` 里 `Anchor` 存的是 `getChannelInv(vConfig.anchor)`（只取交叉轴分量，供 `CoolBarItem` 交叉轴居中用），而窗口整体定位 `new_pos` 用的是完整 `vConfig.anchor`（xy 双分量）。同一字段两种解读，阅读时易混淆。
4. **item 键用 ImGuiID 算术**：`GetID(window->ID + idx + 1)` 直接对 ImGuiID 做加法生成 per-item 键，理论上存在与其它控件 ID 碰撞的极小概率，属实现取巧。
5. **`assert(normal_size>0)`**（378）用的是标准 `assert`（依赖传递包含的 `<cassert>`），Release（NDEBUG）下被裁剪；`ImCoolBarConfig` 允许把 `normal_size` 设 0 从而在 Release 触发除零（`ItemCurrentScale = size/normal_size`）。
6. **非调试默认 `NoBackground`**：window 模式默认无背景，`ENABLE_IMCOOLBAR_DEBUG` 已注释；调试时若看不到 bar 背景属正常。
7. **无安全 / 逆向风险面**：此组不读写游戏内存、不加载 so、不联网，纯前端 UI，从 CTF/风险角度看是"干净"模块，审计可低优先级。

（可追溯：所有行为均对应上面标注的 cpp 行号；两文件均已逐行读完。）
